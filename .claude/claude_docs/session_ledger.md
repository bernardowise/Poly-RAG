# Session Ledger

Log of work sessions, most recent first. Each entry added by /end.

## 2026-08-18

- Reconciliacion completa del registry contra los 4 ciclos completos, construida en eda_mio_2: encontrado que el saldo corrido (entraron-salieron por ciclo) no cuadraba con el open real (210 vs 237) porque resolved_this_cycle en los payloads contaba resoluciones de markets AJENOS a la ventana de 4 ciclos (del registry viejo ya borrado) -- corregido filtrando salieron contra el set acumulado de ids que ya habian entrado, verificado exacto (saldo final 237 = open real). Extendido el mismo criterio de limpieza del dia anterior a un nivel mas: 15 registry items + 14 archivos odds que habian entrado por invocaciones de debugging fuera de los 4 ciclos (no por ellos) fueron identificados con evidencia exacta (market_id real de newly_tracked_markets, no aproximacion por hora -- un primer intento por prefijo de hora fallo por colision con invocaciones de debug en la misma hora que un ciclo real) y borrados. Registry final: 285 items, EXACTO igual a los market_ids que entraron via los 4 ciclos, 0 rastro de cualquier otra invocacion
- Agregado apartado nuevo "Polymarket -- conceptos de dominio" a knowledge.md (separado de la seccion tecnica de AI/RAG/NLP existente) -- primera entrada: mecanismo peer-to-peer de Polymarket (complete sets YES/NO, precio como probabilidad implicita, liquidacion via colateral, volume24hr vs volumeNum)
- poly_rag_exploration borrado (notebook original de Dia 3, ya contaminado con muestra parcial de un registry sucio) junto con sus tablas Delta viejas -- reemplazado por eda_mio_3, reconstruido desde cero sobre el registry ya limpio (285 items) y con pull COMPLETO de odds para los 237 markets abiertos (no una muestra de 25 como el notebook viejo, ya no hacia falta dado el tamano del dataset limpio)
- Bug real de duplicados encontrado en eda_mio_3 por el usuario: correr el notebook mas de una vez duplico odds_snapshots de 371 filas reales a 742 (append puro sin dedup, re-agregaba TODO lo leido de S3 cada corrida). Deduplicado en la tabla real (rebuild via ROW_NUMBER, con un intento fallido en el camino por dejar una columna auxiliar `rn` filtrada al schema, corregido). Fix aplicado al notebook: chequeo anti-join contra lo ya existente antes de escribir, con ALERTA visible del conteo de duplicados excluidos (decision explicita del usuario: filtrar si, pero nunca en silencio)
- Time travel real demostrado por primera vez en el proyecto: eda_mio_3 ya tenia 3 versiones genuinas de market_registry (por correrse varias veces) -- agregada Parte 4 que consulta VERSION AS OF version-vieja vs version-nueva y hace un diff real de market_ids entre ambas, cerrando el ultimo pendiente tecnico del Dia 3
- Dia 3 del sprint cerrado formalmente en gerdau/sprint_plan.md -- los 5 bloques completos incluyendo DAMA-DMBOK/Data Mesh (leido por el usuario fuera de esta sesion), con nota explicita de que el trabajo colateral del dia (el bug de diseno de ingest_polymarket y la limpieza mayor de AWS) no estaba en el plan original, surgio de auditar datos reales para estos mismos notebooks

- Arranque de Dia 4 (retrieval/RAG). Pregunta del usuario sobre CI/CD respondida y aterrizada: movido de Dia 6 bloque 2b (un one-liner opcional de `aws lambda update-function-code` en push, que habria bypasseado Terraform y driftado el state) a Dia 5 bloque 6 como bloque real con sub-lista ordenada por dependencia -- state remoto de Terraform primero (bloqueante: CI no puede aplicar contra un state gitignoreado en el Codespace), luego los primeros tests de Python, luego CI, luego CD via OIDC. Diferido de Dia 4 con razonamiento explicito, no por omision
- Pregunta de seguimiento del usuario ("me estoy equivocando al cambiar Lambdas ya desplegadas") aclarada como un tema DISTINTO de CI/CD: separacion de ambientes (donde aterriza el codigo) no es lo mismo que CI/CD (como se mueve), y uno no da el otro gratis. Un stack `poly-rag-dev-*` paralelo se evaluo y no se adopto -- duplicaria el gasto de Bedrock (~$8.85 -> ~$17.70/mes) y un ambiente dev con datos chicos/sinteticos no habria cachado casi ninguno de los bugs reales del proyecto (el de consistencia eventual de DynamoDB, el timeout de 900s con outlets lentos, el hallazgo de shared_event), que requirieron escala y red reales. Alternativas preferidas anotadas en tech_debt
- Diseño de RAG discutido contra datos REALES medidos en vivo (no contra lo que decian los docs): 2,638 articulos / ~4.15M tokens y 9,778 comentarios / ~200K tokens en 4 ciclos, registry en 515 markets (445 open), y un 5o ciclo (2026-08-18 00:00) que los docs aun no registraban. Tres hallazgos que cambian el diseño: mediana de articulo 4,015 chars (obliga chunking), mediana de comentario 44 chars con 47% bajo 40 (no se deben embeder individualmente), y sobre todo linkeo 100% articulo->exactamente-un-market
- Modelo de dos capas de retrieval (Layer 1 linkeado / Layer 2 ventana temporal ambiental) DEPRECADO por completo, con VoBo explicito del usuario, que identifico el punto el mismo: el rediseño de News a busqueda-por-market vacio el pool ambiental que Layer 2 existia para alcanzar. `retrieval/time_window.py` borrado (no parcheado -- ademas leia el prefijo `bluesky/` ya borrado, devolviendo 0 posts en silencio como si fuera resultado real, y nunca leyo `comments/`). Reemplazado por un solo camino: filtro por metadata (`market_id`, tiempo, source) + ranking semantico. El tiempo NO se deprecia, baja de capa arquitectonica a campo del envelope de metadata
- Al deprecar se preservo explicitamente la limitacion que Layer 2 SI señalaba y que sigue abierta (una noticia ambiental que nunca nombra al market jamas se llega a fetchear), nombrando a la busqueda semantica cross-market como su camino sucesor -- con la frontera honesta de que solo alcanza contenido ya en el corpus por causa de otro market, no temas nunca cubiertos
- Correccion importante aportada por el usuario: el corpus es ACUMULATIVO (~1M tokens/ciclo, 2 ciclos/dia), asi que el embedding tiene que ser incremental y automatizado dentro de la cadena de ingestion, no un backfill manual. Consecuencia de diseño real: la APPENDABILITY pasa a ser el criterio de seleccion del vector store, no la ergonomia -- un blob plano FAISS en S3 implicaria read-modify-write de un archivo creciente cada ciclo, exactamente el patron que ya causo problemas dos veces (archivos de odds, escrituras concurrentes de batches de News). Vector store deliberadamente NO decidido aun: candidatos FAISS-en-S3, ChromaDB (que el usuario ya uso en otro proyecto), Databricks Vector Search; OpenSearch Serverless descartado por costo (~$700/mes)
- README.md, architecture_canon.md y tech_debt.md actualizados en consecuencia (cifras reales del corpus, seccion de retrieval reescrita, entrada de deprecacion completa conservando el razonamiento historico sin borrarlo)

- Confirmados 6 ciclos completos (el ultimo, 12:00 UTC del 18) y re-medido el corpus completo sobre los 6 (no los 4 de la noche anterior): 3,315 articulos (~5.22M tokens) y 12,711 comentarios (~261K tokens), registry en 595 markets (502 open / 93 resolved). Hallazgo nuevo: News y Comments crecen distinto -- News sube sostenido (443->785 articulos/ciclo), Comments se aplano de 3,441 a ~350-400/ciclo una vez que la tabla de dedup alcanzo estado estable (no es una falla, `markets_with_comments`/`entities_queried` se mantuvieron normales)
- Bug real de diseno encontrado auditando la mediana de articulo: solo 23% de los articulos ingeridos son frescos (<=1 dia), mediana de 41 dias, 54% >30 dias, algunos hasta 17.6 anios de antiguedad -- confirmado sistemico en los 5 ciclos medidos, no un ciclo malo. Root cause: Google News RSS rankea por relevancia a la pregunta del market, no por fecha, y `ingest_news` nunca miro `pubDate`. Esto es un problema de RAG, no solo de higiene de datos -- la tesis del proyecto es correlacion CONCURRENTE, y sin distinguir edad, el retrieval citaria con confianza un articulo de hace un anio como explicacion de un movimiento de odds de esta semana
- Usuario decidio: no borrar nada (contexto legitimo del market aunque no haya movido odds), cap de 1 anio mas alla de la fecha de CREACION del market (no de la fecha de ingesta), 3 flags temporales para noticias segun su relacion con el market (antes de crearse / creado pero sin tracking todavia / creado y ya trackeado), y una captura extra 4-8 ciclos DESPUES de que un market resuelva para capturar reaccion post-resolucion
- Pregunta del usuario sobre la flag intermedia (si en verdad no traemos historico de odds, solo snapshots desde que empezamos a trackear) llevo a investigar y confirmar que Polymarket SI expone historia de precio completa por market via `clob.polymarket.com/prices-history`, gratis, sin auth -- hallazgo que cambia el diseño: la flag 2 deja de ser "no podemos correlacionar" y pasa a ser "podemos correlacionar, a resolucion diaria, con datos recuperados"
- Backfill de historia de odds implementado como script one-off (`scripts/backfill_odds_history.py`, deliberadamente FUERA de la cadena de 12h -- historia pre-tracking es inmutable) con dry-run por default. Dos bugs reales encontrados y corregidos ANTES de escribir nada, ninguno de los dos genero error, ambos se veian como hallazgos legitimos sobre los datos: (1) exigir que YES y NO reportaran el mismo timestamp descarto ~75% de la historia real (cada token opera su propio order book, casi nunca coinciden) -- corregido leyendo solo el token YES y derivando NO=1-YES, valido para markets binarios por el mecanismo de complete sets; (2) el endpoint `?id=` de Gamma devuelve lista vacia para markets con `closed:true`, lo cual habria saltado silenciosamente los 93 markets resueltos -- exactamente los arcos completos open-to-resolution que el proyecto reclama como diferenciador -- corregido usando el endpoint `/markets/{id}` que `ingest_polymarket` ya usa
- Antes del backfill se corrio un script separado (`scripts/tag_cycle_snapshots.py`) para taggear los ~1,368 snapshots existentes con `source: "cycle"` explicito -- el usuario rechazo la alternativa de dejarlo implicito (ausencia de campo = cycle-origin) por la misma razon que `link_type` paso de 2 a 3 tiers: una promesa no escrita no se puede verificar. Verificado aditivo puro (solo agrega la key, ningun valor cambia), idempotente, y probado explicitamente contra un caso mixto para confirmar que nunca reetiquetaria un punto `clob_backfill` como `cycle`
- Backfill aplicado en produccion sobre los 595 markets del registry (incluyendo resueltos, decision explicita del usuario -- son los arcos mas valiosos): 487 backfileados, 108 sin pre-historia (nacieron despues del 16 de agosto), 0 errores, 62,273 snapshots agregados. Verificado post-escritura en S3: 63,641 snapshots totales (62,273 clob_backfill + 1,368 cycle, aritmetica exacta), 0 violaciones del cutoff duro (nada escrito en o despues del primer ciclo real), 0 snapshots cycle sin volume, 0 snapshots backfill con volume, orden cronologico y empalme de precio limpio confirmados en un market de muestra (561980: 375 puntos desde 2025-07-10 hasta hoy, el ultimo precio backfill coincide con el primer precio de ciclo)
- Usuario establecio un limite explicito de alcance: recuperar el HISTORIAL DE NOTICIAS de la misma ventana pre-tracking es un proyecto aparte, no una extension de este -- Google News RSS no soporta busqueda por rango de fecha arbitrario. El backfill de odds es "lo mas atras que vamos a ir." Documentado en tech_debt.md para que no se reproponga como si fuera continuacion natural
- architecture_canon.md y tech_debt.md actualizados con el diseno completo del backfill (dos entradas nuevas), los dos bugs, los numeros verificados, y la nueva seccion Odds Time-Series describiendo los dos origenes de snapshot (`cycle`/`clob_backfill`) y como se distinguen
- Aparte, agregada una entrada nueva a tech_debt.md por peticion del usuario: publicar el digest generado por LLM en su perfil de LinkedIn 2-3 veces por semana como personal branding de AI engineer, explicitamente diferido hasta que el proyecto este 100% terminado -- documentadas las preguntas de diseno reales que faltarian (seleccion de que digest publicar dado que no todos los ciclos se publicarian, automatico vs. draft-then-approve dado el riesgo reputacional de postear bajo el nombre real del usuario, y verificar ToS de la API de LinkedIn antes de construir nada, mismo habito que Reddit/Google News/GDELT)

- Bloque B (flags temporales de News 3.1/3.2/3.3) completado. `created_at` real del market (distinto de `first_seen`, que es cuando NOSOTROS empezamos a trackear) backfileado a los 595 items del registry via `scripts/backfill_registry_created_at.py` -- prerequisito descubierto en vivo, no planeado: sin `created_at` no se puede distinguir 3.1 (antes de que el market existiera) de 3.2 (existia pero no lo trackeabamos). Mismo bug de endpoint que en el backfill de odds (`?id=` esconde markets `closed:true`) evitado desde el diseno, usando `/markets/{id}` desde el inicio -- 595/595 sin errores
- `scripts/tag_news_temporal_tier.py` clasifico retroactivamente los 3,315 articulos existentes (no solo hacia adelante, consistente con "no se borra nada" del backfill de odds). Distribucion real: 3.2=1,858 (56%, la mayoria rescatada por el backfill de odds de ayer), 3.1=860 (26%), unknown_market=305 (9%), 3.3=167 (5%), too_old=125 (4%)
- Hallazgo real de datos huerfanos, NO corregido por decision explicita del usuario ("dont delete yet, will handle later perhaps querying through the RAG"): 305 articulos (`unknown_market`) referencian market_ids que la limpieza del registry del 17 de agosto borro -- esa limpieza nunca toco `news/`, solo registry/odds. Confirmado con evidencia que es 100% historico, no una fuga activa: tasa de unknown_market por ciclo fue 85% -> 21% -> 13% -> 12% -> 0% exacto desde el 18 a las 00:00, el dia siguiente a que la limpieza se estabilizara
- Discusion real sobre por que 3.3 (la tier de mayor confianza, respaldada por snapshots completos de ciclo) es solo 5% hoy: la hipotesis inicial del usuario ("markets nuevos siempre caeran en 3.3") fue corregida en conjunto -- el primer pull de Google News para un market nuevo trae mayormente cobertura vieja rankeada por relevancia (ver el hallazgo de stale news de ayer), no noticias del dia. Lo que si es cierto: conforme el market sigue trackeado y corren mas ciclos, los pulls SUBSECUENTES si empiezan a traer noticias genuinamente del mismo dia, que si caen en 3.3 -- 3.3 crece con el TIEMPO DE OPERACION del pipeline, no con el tamano del registry, y es honesto que sea bajo a solo 3 dias de haber arrancado News
- Extension de `ingest_news`/`ingest_polymarket` para que nuevos markets entrantes traigan esto automaticamente (odds history + created_at + clasificacion de tier) deliberadamente diferida como "F" -- un solo deploy con un plan verificado, en vez de tocar las Lambdas varias veces el mismo dia por razones distintas. La logica de clasificacion (`classify_temporal_tier`) quedo escrita standalone en el script para poder copiarse directo a `ingest_news` cuando llegue ese momento -- este repo no tiene un modulo compartido entre Lambdas
- Ambos backfills de este bloque verificados aditivos puros en S3/DynamoDB antes y despues de aplicar (dry-run primero en ambos, sin excepcion) y documentados en tech_debt.md con dos entradas nuevas

## 2026-08-17

- Primera corrida canonica limpia de las 12 UTC verificada de punta a punta: las 4 Lambdas corrieron solas via EventBridge/chaining, cerrado el trial LLM-en-ingestion como canon (ya no experimento reversible -- el LLM es el filtro de calidad mismo, y su output alimenta el futuro RAG). Costo real medido del primer ciclo canonico: $0.147/ciclo (~$8.85/mes), actualizado en architecture_canon.md reemplazando la cifra historica de $1.10/mes pre-rediseno
- Cerrado un gap real de observabilidad: send_digest llamaba a Bedrock (executive summary) pero nunca escribia a poly-rag-architecture-metrics -- agregado write_metrics/estimate_cost_usd, permiso IAM PutItem scoped, verificado con invocacion manual real
- Limpieza mayor de datos obsoletos en dos rondas, siempre con S3 versioning + DynamoDB PITR activados PRIMERO (deliberado, para que el borrado fuera recuperable via restauracion, no irreversible -- decision explicita del usuario, "mas higienico aunque si sea recuperable"): (1) 329 registry items + sus 329 odds/<market_id>.json del pipeline pre-rediseno (Bluesky + News-por-keywords) + test/README.md + prefijo bluesky/ completo; (2) 13 archivos de ciclo (news/polymarket) del diseno viejo de 10-feeds-RSS/3-verticales que habian quedado fuera del alcance de la primera pasada
- Definido "ciclo completo" (las 4 etapas con archivo final bajo la misma fecha/hora, excluyendo batches intermedios de News) en el notebook eda_mio propio del usuario -- solo 4 ciclos completos verificados a la fecha. Extendido el mismo criterio a nivel snapshot individual dentro de odds/: purgados 4 snapshots de ruido (todos del mismo timestamp de un ciclo incompleto ya borrado) y el archivo de un market que quedo en 0 snapshots validos
- Bug real de diseno encontrado por el usuario mientras auditaba los 4 ciclos: un market abierto y trackeado en el registry podia tener huecos silenciosos en su serie de tiempo de odds (ej. market 1365861: presente en ciclo 1, ausente en ciclo 2, de vuelta en 3-4) porque el snapshot de odds dependia de que el market apareciera en el top-500 de ESE ciclo especifico -- coincidencia de dos responsabilidades que deberian estar desacopladas (descubrir candidatos nuevos vs. mantener viva la serie de tiempo de lo ya trackeado)
- Primer intento de fix (parche: mantener el concepto de dropped_ids, agregar contador recovered_snapshots_written) rechazado explicitamente por el usuario como arquitectonicamente incorrecto -- el usuario diseno la solucion correcta el mismo: desacoplar por completo, iterar sobre TODO market open del registry sin excepcion, usando el batch del top-500 como fuente barata cuando coincide y fetch individual por ID cuando no. Reescrito y desplegado (scoped a ingest_polymarket, 0 permisos/llamadas API nuevas -- reutiliza exactamente las mismas llamadas que ya existian para el chequeo de resolucion). Documentado en tech_debt.md con el razonamiento completo, incluyendo el intento rechazado
- Reescrito README.md desde cero usando architecture_canon.md como fuente de verdad (arquitectura real, flujo de datos, stack, estructura del repo verificada, seccion Pending/TODO) -- reemplaza la narrativa personal de origen que ya no reflejaba el estado actual del proyecto
- Corregido un error real en infra_design.md (decia que new_skills.md estaba trackeado en git; en realidad esta gitignorado, confirmado con git check-ignore) y reescrito ese documento por completo para reflejar la arquitectura real de .claude/ en este repo (antes era una tabla generica de features de Claude Code sin nada especifico a Poly-RAG)
- Instaladas 29 skills de Databricks (Unity Catalog, Delta Lake, jobs, etc.) via databricks aitools install --skills-only, scoped al repo (.claude/skills/ symlinks + .databricks/aitools/skills/ contenido real, ambos trackeados a peticion del usuario para que el futuro corpus self-referencial tenga acceso)
- Dia 3 (Databricks) avanzado con datos reales: notebook poly_rag_exploration reescrito para el schema actual (registry + odds time-series, ya no el payload viejo de markets completo), tablas Delta creadas en Unity Catalog (workspace.poly_rag.market_registry/odds_snapshots) y verificadas. Encontrado y corregido un bug real en el notebook mismo: odds_snapshots usaba append puro sobre una muestra parcial, dejando basura residual de markets ya borrados del pipeline real (ej. market 2252242, con 26 snapshots en Delta mucho despues de no existir ya en S3/DynamoDB) -- corregido con DROP TABLE de una sola vez (no permanente, para no perder la capacidad de demostrar time travel real mas adelante) + invariante de purga contra el registry completo en cada corrida futura
- Ultima llamada Bedrock de LLM-en-ingestion en ambos rediseños (world_snapshot ayer, y hoy el fix de odds) siguio la misma disciplina de costo cero-adicional del proyecto -- ningun cambio de hoy agrego llamadas nuevas a Bedrock ni a la Gamma API, solo reordeno/reutilizo trabajo que ya se pagaba

## 2026-08-16

- Aclarado el comportamiento real de ingest_polymarket ante cada corrida de EventBridge: el fetch del top-500 a la Gamma API SIEMPRE es completo cada ciclo (no hay forma de pedir "solo lo nuevo" sin la lista completa para diffear) -- el diff contra el registry solo protege el paso caro (clasificacion LLM/Bedrock), no el fetch en si ni el snapshot de odds (que se escribe cada ciclo para TODO market abierto, nuevo o no). architecture_canon.md actualizado con esta precision, incluyendo la interaccion con el bug ya documentado de horizonte de 48h (un market deportivo de corto horizonte se re-evalua como "nuevo" indefinidamente hasta envejecer o salir del top-500)
- Diseñada e implementada una nueva seccion `world_snapshot` en el digest (rapport explicito con el usuario antes de codear, dos preguntas via AskUserQuestion para cerrar criterio de seleccion y umbrales): a diferencia de `top_volatility` (que MOVIO este ciclo), esta es una foto de que CREE el mercado ahora mismo, independiente del movimiento -- `top_conviction` (top 5 por volume24hr, mayor conviccion real) y `most_disputed` (top 5 mas cercanos a 50/50 dentro de banda 40-60%, apuestas genuinamente divididas)
- Refactor de eficiencia en el mismo cambio: `compute_top_volatility` y `compute_world_snapshot` ahora comparten un solo pase de lectura (`_load_open_market_odds`) en vez de escanear el registry y releer S3 dos veces por ciclo para dos vistas del mismo pool de ~330 markets abiertos -- disciplina de costo real, no solo estilo
- Desplegado con `terraform apply -target` scoped SOLO a `aws_lambda_function.send_digest` (plan confirmado de antemano: 0 to add, 1 to change, 0 to destroy) sin tocar ingest_polymarket/news/comments ni ningun otro recurso -- a peticion explicita del usuario de no redesplegar las otras Lambdas
- Verificado con invocacion manual directa de `poly-rag-send-digest` (sin correr la cadena completa) contra datos reales de produccion: ambos grupos poblados 5/5, `most_disputed` mostro toss-ups politicos genuinos (balance de poder del Senado 2026 a 0.49, carrera de Ohio a 0.48, Netanyahu-out a 0.47) -- exactamente la señal que `top_volatility` no puede mostrar. Email confirmado recibido y legible por el usuario con las dos secciones nuevas
- Nota para revisitar (agregada a tech_debt.md): `top_conviction` mezcla conviccion real con markets deportivos casi resueltos (precio 1.00/0.00 de partidos ya terminados) porque `volume24hr` no distingue "el mercado esta seguro" de "el partido ya acabo" -- posible filtro futuro similar al horizonte minimo de 48h ya existente
- architecture_canon.md: seccion de Email Digest reescrita por completo -- estaba desactualizada desde el rediseno de "Strict Ingestion Chaining" (aun describia EventBridge con offset fijo de 5 min en vez del encadenamiento directo actual), aprovechado el mismo bloque de trabajo para corregirla junto con la adicion de world_snapshot
- Empaquetados dos zips de `.claude/claude_docs/` a peticion del usuario (uno con estructura de carpetas, otro con todos los .md en listado plano, `gerdau/*.md` prefijados `gerdau_` para evitar colision de nombres) -- entregados en la raiz del repo, no trackeados en git
- News: rediseno de self-chaining secuencial a fan-out paralelo desplegado y verificado en produccion (228/228 markets, 887 articulos, cycle_complete true) -- el diseno secuencial original habria tardado ~1.5h, el paralelo lo bajo a un puñado de minutos por corrida real
- Blocklist dinamico de dominios implementado y desplegado (tabla poly-rag-domain-failures, 5 fallos consecutivos = skip sin intentar el request, reset a 0 en cualquier exito) -- cierra el pendiente de egamersworld.com y similares detectados fallando 100% de las veces
- Limpieza de architecture_canon.md: removida la seccion "News Feed Inventory" obsoleta (los 10 feeds RSS viejos), corregidas cifras viejas del bootstrap (505/367 -> 228/887), corregida la descripcion del linkeo News->market_id para reflejar el diseno actual (busqueda dedicada por market, no AND-match)
- Auditoria de valor real de Bluesky como fuente: confirmado con datos reales que la mayoria de los 437 posts capturados eran bots republicando el propio feed de precios de Polymarket, no sentiment humano genuino -- senal circular, no independiente. Decision: pausar/reemplazar Bluesky
- Investigado y confirmado: gamma-api.polymarket.com/comments es un endpoint publico, sin auth, documentado oficialmente, con autor/texto/timestamp/reactions/threading -- mejor fuente de sentiment real (comentarios de traders reales verificados en el sample, ej. analisis tecnico real de un partido de La Liga)
- Verificado con datos reales que los comentarios cuelgan de Event (especifico a un market) o Series (compartido entre TODOS los markets de esa serie/liga) -- confirmado empiricamente que Series mezcla comentarios de partidos no relacionados bajo el mismo feed (dos juegos de MLB distintos mostraron el mismo comentario), asi que se disenio un campo comment_link_type (direct vs shared_series) en vez de tratar Series como simplemente "baja confianza"
- Bug real encontrado en el filtro de horizonte minimo de 48h: usa endDate del market, que para mercados deportivos NO es la hora real del partido sino un deadline administrativo posterior (ej. partido de MLB de hoy con endDate 8 dias despues) -- documentado como tech debt, no resuelto aun (fix candidato: usar event.eventDate en su lugar)
- Diseno e implementacion completa (no desplegada aun) del reemplazo de Bluesky por Comments: ingest_polymarket simplificado (LLM ya no genera search_query/news_match_terms, quedaron obsoletos porque News usa question verbatim y Comments usa lookup directo por ID), nueva funcion get_comment_link que extrae event_id/series_id directo de la respuesta de Gamma API sin LLM, nueva Lambda ingest_comments creada completa, Terraform actualizado (nueva Lambda + EventBridge, Bluesky removido) -- terraform apply pendiente de correr, nada desplegado a AWS todavia
- Sesion cerrada con /end sin desplegar el cambio de Comments -- el corte fue deliberado para dejar un checkpoint en el ledger antes de continuar, no un fin de sesion real
- Retomado y desplegado el reemplazo de Bluesky por Comments via terraform apply: ingest_bluesky (funcion, permiso, EventBridge rule/target) destruido de AWS, ingest_comments creado y activo, ingest_polymarket actualizado con el LLM simplificado -- confirmado con aws lambda get-function que ingest_bluesky ya no existe
- Backfill one-off corrido contra los 329 markets del registry que predataban el rediseño (solo los nuevos del dia ya tenian comment_entity_type): 187 con link directo a Event, 105 con candidato de Series, 37 sin comentarios en ningun nivel, 0 errores -- sin este paso ingest_comments habria tenido cobertura casi nula
- Primera prueba real de ingest_comments expuso un bug de diseño propio (no reportado por el usuario esta vez): el criterio original de dos niveles (direct=Event, shared_series=Series) resultaba incorrecto porque un Event puede agrupar VARIOS markets (ej. torneo de esports, un market por equipo, misma seccion de comments) -- 802/1698 comentarios marcados direct en realidad tenian multiples market_ids, rompiendo la garantia 1:1 que direct prometia. Corregido el mismo dia a 3 niveles (direct/shared_event/shared_series) y redesplegado
- Verificado en produccion tras el fix: 295/329 markets con cobertura de comentarios (89%), 2506 comentarios reales, 94 llamadas a la API (agrupadas por entidad compartida en vez de una por market), 0 fallos, distribucion final 896 direct / 802 shared_event / 808 shared_series
- Codigo fuente de ingest_bluesky borrado del repo (ya sin ningun Terraform que lo referencie, la historia completa queda preservada en tech_debt.md y session_ledger.md, no en codigo muerto)
- architecture_canon.md y tech_debt.md actualizados con el estado final real (no el diseño intermedio): tabla de Infrastructure Inventory, schema del Market Registry, seccion de Comments reemplazando la de Bluesky, entrada completa "Comments Source Replaces Bluesky" con la narrativa de los dos bugs encontrados y corregidos
- Sanitizacion de queries de Google News: comillas literales en question (ej. titulos de shows) forzaban busqueda de frase exacta en Google en vez de texto libre -- fix simple de sanitize_query, sin tocar el prompt del LLM (instruccion explicita del usuario)
- Digest de email completamente rediseñado: de texto plano concatenando 3 resumenes ciegos entre si, a JSON estructurado en S3 (digest/YYYY-MM-DD/HH.json, pensado para ingestion RAG futura) + email HTML con resumen ejecutivo sintetizado por un nuevo llamado LLM que ve las 3 fuentes juntas, top movimientos de odds por volatilidad real (delta entre snapshots), y citas verbatim de articulos/comentarios. Bugs reales encontrados y corregidos en el camino: falta de permiso s3:PutObject (el rol nunca habia escrito a S3), y un NameError real por una variable renombrada a medias
- Usuario detecto en vivo un bug real de orquestacion: las 3 Lambdas de ingestion disparaban por EventBridge al mismo tiempo (00:00/12:00 UTC), con el digest en un offset fijo de 5 min asumiendo que ya habian terminado -- confirmado con un caso real (un market nuevo de Polymarket nunca aparecio en News porque corrio contra un registry no actualizado ese ciclo)
- Rediseño a encadenamiento estricto: solo ingest_polymarket tiene EventBridge rule ahora: invoca a ingest_news al terminar, News invoca a Comments solo cuando su propio ciclo completo (no cada batch) termina, Comments invoca a send_digest -- las 3 EventBridge rules de News/Comments/digest se eliminaron de Terraform. Decision consciente de revertir el principio de aislamiento de fallos del Dia 2 (si Polymarket falla, nada mas corre ese ciclo) porque News/Comments nunca fueron realmente independientes del registry que Polymarket escribe
- Primera prueba real de la cadena expuso un bug de fondo: cada batch de News recalculaba total_markets con su propio scan() del registry, y como DynamoDB Scan es de consistencia eventual, dos scans de invocaciones distintas podian dar resultados distintos aunque los datos no hubieran cambiado -- causo que el merge final nunca encontrara el set de offsets esperado y la cadena se atorara silenciosamente sin ningun error visible. Root-caused con evidencia directa (timestamps de first_seen/resolution_date identicos al inicio del ciclo, descartando que el registry siguiera cambiando). Fix: total_markets se calcula UNA vez en el dispatcher y viaja fijo por el payload de cada batch
- Con el fix desplegado, un batch (offset=0) hizo timeout real de 900s extrayendo articulos de un outlet lento -- expuso un segundo problema real: sin nadie monitoreando, un solo batch atorado bloquea la cadena completa (Comments, digest) indefinidamente y sin aviso. Usuario correctamente escéptico: "no estamos ni tu ni yo para depurar" cada 12 horas
- Implementado poly-rag-watchdog-ingest-news: nueva Lambda en cron de 10 min que detecta un ciclo de News atorado (batches faltantes 20+ min despues de iniciado) y reintenta automaticamente solo los offsets faltantes, reusando el mismo total_markets ya usado por el ciclo (sin volver a escanear). El watchdog mismo tuvo un bug real en su primera corrida (extraia mal el prefijo de hora del key de S3, tomaba solo la fecha) -- corregido y reverificado con datos reales
- Cadena completa verificada funcionando end-to-end por primera vez: Polymarket -> News (cycle_complete true, 73 articulos) -> Comments (2587 comentarios) -> digest, disparada automaticamente sin invocacion manual de Comments ni digest. Resumen ejecutivo real coherente (identifico correctamente que los mayores movimientos de odds eran partidos deportivos en progreso, no trading predictivo)
- Dos bugs no bloqueantes encontrados en esa misma corrida -- usuario pidio explicitamente corregir el primero antes de seguir (no dejarlo solo documentado): Comments/digest escribian su particion de S3 con datetime.now() en vez de heredar cycle_started_at, y ademas ingest_comments invocaba a send_digest con un payload VACIO (el bug real empezaba ahi, no solo en la construccion del key). Corregido: cycle_started_at ahora viaja por toda la cadena hasta el ultimo eslabon, cada Lambda lo usa para su propia key de S3 y lo reenvia a la siguiente. Verificado con invocacion manual explicita (cycle_started_at de prueba a las 03:00) confirmando que tanto comments/03.json como digest/03.json se escribieron bajo la particion correcta -- archivos de prueba borrados despues de verificar
- Segundo bug (doble-disparo por invocaciones concurrentes de offset=0) queda documentado pero no corregido -- solo ocurre por intervencion manual de debugging, no en operacion normal
- Sesion continua (NO fue un fin de dia real) -- el trabajo de ingestion/orquestacion sigue siendo prioridad sobre Databricks/Delta Lake (Dia 3, pendiente desde hace 2 dias) hasta que los cimientos del pipeline queden solidos, por decision explicita del usuario

- Bootstrap del pipeline rediseñado corrido y verificado: ingest_polymarket con top 500 candidatos por volume24hr, filtro LLM de verificabilidad, y registry poblado en DynamoDB (480 mercados trackeados, 480 archivos de odds en S3) -- verificado directamente contra las tablas reales tras dos bugs de despliegue encontrados y corregidos en el camino (timeout de Lambda insuficiente para el volumen de trabajo del bootstrap, y un AccessDenied de S3 causado por falta de permiso ListBucket a nivel bucket, no solo GetObject a nivel objeto)
- Logging de auditoria agregado al paso LLM: prompt y respuesta cruda de cada batch van a CloudWatch, y las respuestas crudas (capa Bronze) se guardan tambien en el metadata del payload S3 -- para poder re-inspeccionar exactamente que dijo el modelo si algo se ve mal en el registry parseado
- Revision de resultado del bootstrap detecto que ~96% de los candidatos pasaron el filtro de verificabilidad (no 2.4% como parecia inicialmente por una lectura enganosa del ultimo run aislado) -- confirma la hipotesis de que alto volumen en Polymarket ya correlaciona con mercados de resolucion clara
- Bug de diseno real encontrado en las keywords generadas por el LLM: una lista de keywords sueltas (ej. "Elon Musk", "tweets", "August 2026") pierde señal si se buscan por separado -- termino identificado por el usuario, no how solo un capricho de estilo
- Corregido a dos representaciones de busqueda distintas en el mismo llamado LLM (sin costo adicional de Bedrock), porque News y Bluesky matchean texto de formas distintas: search_query (string combinado, para el search API de Bluesky) y news_match_terms (lista corta de 1-3 frases distintivas con logica AND, para matching por substring contra el texto de RSS ya descargado por News, que no tiene API de busqueda)
- Verificado con llamadas reales a Bedrock (fuera del handler, muestra de 20 mercados) antes de desplegar -- confirmado que ambos formatos salen bien calibrados (ej. Federal Reserve + September 2026 en vez de Fed/rate/2026 sueltos)
- Reflexion de producto real: se discutio honestamente que tipo de analisis estadistico/predictivo es alcanzable con odds+news (lead-lag, calibracion de Polymarket via reliability diagram) vs. lo que NO lo es (prediccion causal validada, dado el volumen de datos de solo 2 meses) -- y para que le serviria el RAG a un trader real (agregacion + historial propio + alertas de divergencia, no "tips de inversion")
- Corregida una limitacion mal planteada: el retrieval NO debe limitarse solo a items explicitamente linkeados por market_id -- toda noticia/post ya tiene timestamp de ingesta sin importar si matcheo keywords, asi que existe una segunda capa de retrieval por ventana temporal disponible ya (sin esperar a embeddings del Dia 4), como contexto secundario junto al linkeo de alta confianza
- Anotado en tech_debt como consideracion futura: acceso web en vivo para el agente de sintesis (Dia 5) via tool-use de Claude -- explicitamente no aplica a las Lambdas de ingestion (rompe el modelo de costo batch), pero es candidato real para cuando el usuario pregunte algo mas reciente que el ultimo ciclo de ingestion
- Pendiente para el siguiente bloque (dejado deliberadamente sin ejecutar para no gastar Bedrock de mas antes de dormir): limpiar los 480 registros del bootstrap viejo (schema con keywords obsoleto), re-correr el bootstrap con el prompt de dos formatos ya desplegado, luego migrar ingest_news e ingest_bluesky a leer del registry, reactivar el cron de 12h, construir el retrieval por ventana temporal, y actualizar architecture_canon.md con el diseno completo
- Bug logico real encontrado y corregido en el bootstrap: un market podia crearse y marcarse "resolved" en la misma sesion de trabajo (13 markets deportivos/esports resueltos minutos despues de trackearse) -- root cause: mercados con resolucion en minutos/horas (partidos en vivo) no encajan en absoluto con la tesis del proyecto (noticias/sentiment influyendo el resultado mientras el mercado esta abierto), asi que se agrego un filtro de horizonte minimo de 48h (endDate) antes del paso LLM, aplicado antes de gastar Bedrock. Registry viejo (505, con el bug) y ambos sets de archivos de odds (el nuevo y el respaldo de anoche) borrados permanentemente por instruccion explicita del usuario -- sin dejar rastro de las corridas rotas. Bootstrap limpio final: 228 markets (bajo de 505 tras excluir deportes/esports de corto horizonte)
- Auditoria real de calidad del linkeo News->market via AND-match: de 367 articulos solo 3 linkeaban a algun market, encontrado root cause con evidencia (no supuesto): el LLM generaba terminos formales de la pregunta de Polymarket ("Federal Reserve", "President of Russia") en vez de vocabulario periodistico real ("Fed", "Putin") -- confirmado contando matches reales contra los articulos del dia ("federal reserve" exacto: 0 matches; "fed ": 1; "putin" solo: 2 pero el segundo termino AND requerido nunca aparecia). Causa raiz: el prompt solo le pasa al LLM la question corta de Polymarket, sin description, un "nanocorpus" insuficiente para inferir vocabulario real
- Usuario identifico en vivo que Polymarket ya tiene una feature nativa (AI-generated summary, marcada "Experimental") correlacionando noticias con movimientos de odds en ciertos markets de alto perfil -- discutido como amenaza SWOT real a mediano plazo (Polymarket tiene datos privilegiados que este proyecto no tiene) pero no competencia directa hoy (feature limitada, experimental, sin peso en resolucion) -- PolyRAG mantiene ventaja en cobertura de cola larga + fuentes que Polymarket no tiene (Bluesky)
- Rediseno completo de la fuente de News: reemplazados los 10 feeds RSS curados por busqueda dirigida en Google News RSS usando la question de cada market TAL CUAL (sin keywords, sin LLM) como query -- delega el problema de sinonimos/parafraseo al motor de relevancia de Google en vez de un AND-match hecho a mano. Investigadas y descartadas alternativas: Brave Search (ToS prohibe explicitamente uso en RAG/AI, mismo tipo de error que Reddit), Bing/Azure Search (API retirada agosto 2025), DuckDuckGo (sin API real), Google Custom Search (cerrado a registros nuevos desde 2025), NewsAPI.org (tier gratis prohibe produccion), SerpApi/Serper (litigio activo con Google), GDELT (ToS limpio y verificado directo en gdeltproject.org, pero rate limit agresivo de 429 que persistio incluso esperando 20-40s, posiblemente compartido a nivel de rango de IP)
- Excepcion consciente de ToS documentada explicitamente: el copyright de Google News RSS prohibe literalmente "any other use" fuera de "personal feed reader" -- decision informada del usuario tras ver el paralelismo con el caso Reddit, no un descuido. Anotado en tech_debt.md con la justificacion completa y los criterios de revision (si Google bloquea el endpoint en la practica)
- Pipeline de extraccion nuevo construido: googlenewsdecoder (resuelve el link ofuscado de Google sin necesitar headless browser, confirmado contra un articulo real de Reuters) + trafilatura (extrae texto del articulo real). Confirmado con datos reales que medios grandes (Reuters) bloquean con 401 incluso con User-Agent de navegador -- extraccion fallida se DESCARTA, no cae a fallback de solo-titulo (correccion explicita del usuario a una regla que yo mismo inferi sin que la pidiera)
- Dedup por URL exacta (no por similitud de evento) en nueva tabla DynamoDB poly-rag-processed-urls, sin TTL -- decision justificada en vivo: pay-per-request no cobra por storage ocioso de items chicos, asi que TTL no aportaba nada real: el contenido ya extraido vive permanente en S3 sin importar el estado de esta tabla de dedup
- Empaquetado de Lambda con dependencias externas por primera vez en el proyecto (trafilatura + googlenewsdecoder, ~79MB sin comprimir / 21MB comprimido, dentro del limite de Lambda) -- requirio agregar provider null de Terraform y un null_resource con pip install --target antes de que archive_file empaquete el directorio
- Bug de produccion real encontrado y aun sin resolver del todo: 3 invocaciones consecutivas de ingest_news (228 markets) agotaron el timeout de 300s, con articulos siendo REPROCESADOS en cada reintento (mismos URLs de egamersworld.com/people.com repetidos) -- root cause parcial corregido (trafilatura.fetch_url usaba el timeout default de 30s, causando que un solo outlet bloqueado como washingtonpost.com consumiera 30s antes de fallar; bajado a 8s explicitamente vía DOWNLOAD_TIMEOUT config), pero el problema de fondo (228 markets secuenciales no caben en una sola invocacion, sin checkpoint entre reintentos) sigue sin resolver -- solucion identificada (batching con checkpoint de que markets ya se procesaron en el ciclo) pero no implementada aun
- Batching self-chaining implementado y desplegado (BATCH_SIZE=35, offset via payload de invoke, timeout 900s/memory 512MB) -- primera corrida real en produccion mostro ~10-13 min por batch, proyectando ~1.5h para los 7 batches secuenciales. Redisenado en vivo a mitad de la corrida: en vez de encadenar un batch a la vez, la primera invocacion dispara TODOS los batches restantes de una vez (fan-out paralelo), escalonados 3s entre cada uno para no golpear a Google/outlets con un burst simultaneo (se evaluo y descarto usar VPN/proxy rotativo por batch para ofuscar IP -- no hay forma barata de rotar la IP de salida de Lambda, NAT Gateway es fija y cara, proxies de terceros agregan costo/dependencia para lo que en realidad es un tema de concurrencia, no de identidad)
- Fix de race condition necesario para el paralelo: escritura concurrente al mismo S3 key (patron read-modify-write que funcionaba bien en secuencial) perderia datos entre batches simultaneos -- corregido dando a cada batch su propio S3 key, con un paso de merge idempotente al final que combina todos los archivos por-batch
- Snag real durante la transicion mid-deploy: los primeros 3 batches ya habian corrido/estaban corriendo bajo el codigo VIEJO (secuencial) cuando se desplego el codigo nuevo -- AWS deja que una invocacion en curso termine con la version con la que arranco, no la mata. Esos 3 batches escribieron al key compartido viejo en vez de sus propios archivos, asi que el merge automatico nunca los hubiera encontrado -- corregido con un merge manual unico (combinando el payload viejo + los 4 archivos nuevos, dedup por URL) que no aplica a corridas futuras (desde el proximo ciclo, todos los batches nacen bajo el codigo nuevo)
- Verificado en produccion: ciclo completo 228/228 markets, cycle_complete true, 887 articulos, 0 duplicados de URL pese a que offset=105 corrio dos veces por la transicion (dedup sostuvo). Batches paralelos completaron en 294s-611s cada uno vs. 600-800s secuenciales -- ahorro real de tiempo de reloj
- architecture_canon.md y tech_debt.md actualizados con el diseno final (fan-out paralelo, no self-chaining) reemplazando la descripcion del diseno intermedio ya superado

## 2026-08-14

- Dia 2 del sprint completado: 3 Lambdas de ingestion desplegadas y verificadas con datos reales (Polymarket, News, Bluesky), cada una escribiendo a S3 y registrando metricas de costo/latencia en DynamoDB (tabla poly-rag-architecture-metrics)
- Decision de alcance ajustada en vivo: 3 Lambdas independientes (no orquestador unico) para aislar fallos y proteger el limite de PUT requests en reintentos
- Reddit descartado como fuente (su Responsible Builder Policy prohibe explicitamente usar datos para IA/ML); X y Truth Social tambien evaluados y descartados; reemplazados por Bluesky (AT Protocol)
- Bug real encontrado y corregido en produccion: searchPosts de Bluesky requiere auth contra bsky.social (el PDS), no public.api.bsky.app como indicaba la investigacion inicial -- corregido tras 403s reales
- Filtro de verticales (Macro/Geopolitica/Regulatorio-Tech) definido con keywords especificas, mas exclusion explicita de mercados deportivos tras detectar falsos positivos por matching de substring (ej. sec matcheando dentro de second)
- Decision arquitectonica: LLM (Bedrock/Claude Sonnet 4.5) trial en ingestion para las 3 fuentes, midiendo costo/latencia real por 3-4 dias antes de decidir si se mantiene -- una sola llamada batched por corrida, no una por item, para controlar costo
- Costo real medido: ~0.018 USD por ciclo completo de las 3 fuentes (~1.10 USD/mes proyectado a cadencia de 12h), dentro del presupuesto
- Bloqueos resueltos en el camino: Sonnet 5 no disponible (se uso 4.5 via inference profile), suscripcion AWS Marketplace con delay de propagacion en cuenta nueva
- Agregado bloque metadata (schema_version, lambda_name, request_id, llm_used, model_id, latency_ms, cost) a cada JSON en S3 para linaje por-archivo, sin depender de cruzar con DynamoDB
- Terraform instalado e implementado: toda la infra AWS existente (S3, DynamoDB, IAM, 3 Lambdas, EventBridge) escrita en HCL e importada con terraform import -- cero destroy/recreate, verificado con terraform plan mostrando No changes contra el estado real
- Secrets de Bluesky mantenidos fuera de Terraform state/codigo via lifecycle.ignore_changes -- gestionados manualmente
- Creado /cv y new_skills.md (gitignored) para trackear skills reales aprendidas, de cara a actualizar CV
- Corregido /end: ahora explicito que se invoca por bloque de trabajo completado (a discrecion del usuario), no solo al cierre de sesion completa
- Agregada 4a Lambda permanente (poly-rag-send-digest): consolida los llm_summary de las 3 fuentes y los manda por correo via Amazon SES cada 12h+5min (00:05/12:05 UTC), con degradacion graceful si el LLM trial se revierte -- primera pieza de infra desplegada nativamente por Terraform (no importada) desde su creacion
- SES configurado y verificado desde cero (bernardolw@gmail.com), correo digest probado y confirmado recibido
- Aclarado el concepto de indexacion: los llm_summary en S3 hoy son raw storage, no indexados -- se definio el orden logico (Dia 3 Delta Lake/estructurado antes que Dia 4 embeddings/semantico) antes de arrancar Databricks
- Dia 3 del sprint iniciado: cuenta Databricks Free Edition creada con verificacion LinkedIn (desbloquea GPU serverless limitada); en progreso: configuracion de Databricks CLI para evitar copy-paste manual en notebooks, generando Personal Access Token
- Databricks CLI instalado y autenticado (token + host guardados en .secrets); secret scope poly-rag creado en Databricks para credenciales AWS (nunca en texto plano en el notebook)
- Notebook poly_rag_exploration construido y depurado hasta corrida limpia end-to-end (verificado corriendolo como Databricks Job): Serverless compute (Spark Connect) no soporta spark.conf.set para S3 ni sc.parallelize/.rdd -- se resolvio leyendo S3 via boto3 y construyendo el DataFrame con pandas (normalizando tipos numericos mixtos) antes de spark.createDataFrame, evitando por completo la inferencia de tipos de Spark y los bugs de read.json en Spark Connect
- Un intento de fix automatico via el Genie de Databricks fue revisado y rechazado (proponia cluster clasico o Unity Catalog external location, ambos mas caros/complejos que la solucion boto3+pandas ya funcionando) -- el notebook se re-importo por CLI para revertir el cambio no deseado
- Explorando datos reales se detecto que market_count puede venir en 0 (el filtro de vertical a veces no matchea nada en una corrida) -- esto disparo una revision de fondo de la arquitectura de ingestion, no solo un bug a ignorar
- Rediseno grande de ingestion decidido y documentado en tech_debt.md (no ejecutado aun, arranca en el siguiente bloque): el eje real de filtrado no es tema (Macro/Geopolitica/Regulatorio-Tech) sino verificabilidad objetiva del outcome de cada market; se reemplaza el filtro de keywords por un paso LLM que juzga verificabilidad Y extrae keywords dinamicas en un solo llamado batched (sin costo adicional de Bedrock)
- Diseno cerrado: universo candidato = top 500 markets por volume24hr (no volume total como hoy); un market registry en DynamoDB (metadata mutable, un item por market_id) separado de una serie de tiempo de odds en S3 (append-only, un archivo por market_id, read-modify-write cada ciclo); deteccion de resolved via consulta directa a markets/{id} (outcome real, no inferencia); News/Bluesky mantienen su cron independiente de 12h mismo principio de aislamiento de fallos del Dia 2, solo cambian de leer keywords de un dict estatico a leerlas del registry
- Medicion real (no supuesto) de la distribucion de volume24hr en Polymarket via Gamma API: universo activo real es de al menos ~2,100 markets (paginacion offset se cae en ~2100, API sugiere el endpoint markets/keyset para ir mas alla); con ese denominador correcto, top 100 captura solo 49.7% del volumen 24h total y top 500 captura 80.7% -- corrige dos supuestos previos en cascada (un estimado generico de ~90% en top 100-300, y un primer calculo propio inflado por usar un denominador incompleto de solo 500)
- Correccion de costo clave: el filtro de verificabilidad por LLM no escala con el tamano N del universo candidato sino con la tasa de aparicion de IDs nuevos por ciclo (una vez que existe el registry, solo se re-clasifican los IDs que no estaban ya trackeados) -- esto hace pagable un N amplio (500) en vez de forzar a mantenerlo chico
- Terraform aplicado: nueva tabla DynamoDB poly-rag-market-registry (pay-per-request, hash_key market_id) y permisos IAM ampliados para las 3 Lambdas de ingestion (s3:GetObject ademas de PutObject; dynamodb GetItem/PutItem/UpdateItem/Scan/Query sobre la tabla nueva) -- verificado con terraform plan/apply limpio
- Pendiente para el siguiente bloque: reescribir ingest_polymarket con el nuevo diseno completo, luego News/Bluesky, correr un bootstrap manual, verificar, y solo entonces reactivar el cron de 12h

## 2026-08-13

- Sprint de entrevista (gerdau/sprint_plan.md) Dia 1 cerrado: cuenta AWS creada desde cero, MFA en root, budget de $5/mes con alertas 20%/100%, usuario IAM admin (dejando de usar root), AWS CLI v2 instalada y configurada
- Confirmados $120 en creditos promocionales (100 signup + 20 por completar actividad de Budgets) -- CLAUDE.md actualizado para tratar el budget como disciplina real, no colchon para gastar libre
- Guardrail adicional (no estaba en el plan original): policy IAM Deny (PolyRAG-BudgetBreach-Deny) + role (PolyRAG-BudgetsActionRole) armados via CLI para bloquear Bedrock/Lambda/S3-writes/DynamoDB-writes automaticamente si el gasto cruza un budget separado de $10
- Primer contacto practico con S3: bucket creado (poly-rag-369970405415), upload/list confirmado via CLI
- Bloque 5 (Lambda hello-world) abandonado deliberadamente -- se prefirio ir directo a Lambda con trabajo real en Dia 2 en vez de un paso aislado
- Bloque 6 (Bedrock vs SageMaker) cubierto conceptualmente sin necesidad de profundizar mas -- diferencia clara: Bedrock para modelos fundacionales via API (nuestro caso), SageMaker para entrenar modelos propios desde cero (fuera de scope de Poly-RAG)
- Conceptos nuevos archivados en knowledge.md: mapeo AWS/GCP de storage, NoSQL (schema flexible no ausencia de estructura), pricing por request vs volumen, mecanismo de Budget Actions
- Siguiente: Dia 2 -- ingestion real de Polymarket via Lambda + S3 + EventBridge
