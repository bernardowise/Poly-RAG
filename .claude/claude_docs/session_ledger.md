# Session Ledger

Log of work sessions, most recent first. Each entry added by /end.

## 2026-09-05

- Ayudado al usuario a preparar una entrevista de trabajo (AI-Native Engineer, New Grad, Clara -- CDMX): reestructurado su experiencia con Poly-RAG en bullets concisos para CV extendido, y practicada en vivo la primera de 3 preguntas de una video-entrevista asincrona, con feedback directo sobre estructura, idioma y contenido tecnico faltante. Establecido un limite claro y explicito: ayudar a refrescar hechos/decisiones reales del proyecto para que el usuario los explique con sus propias palabras si, pero nunca redactar ni dictar texto para leer en una respuesta grabada evaluada por IA
- Desplegado el MVP de Gradio a produccion publica: Hugging Face Space (`bernardolw/poly-rag`), tras encontrar que HF ya no permite Spaces con SDK Gradio en tier gratuito (requiere PRO, ~9 USD/mes, decision del usuario de pagar) -- descartada en el camino una alternativa mas compleja (exponer Poly-RAG via MCP y consumirlo desde el repo separado de Pienza en Cloud Run) por representar mas superficie nueva sin probar bajo presion de tiempo, no menos, incluyendo una integracion cross-cloud (AWS-GCP) nunca antes hecha
- Creado un IAM user de solo lectura (`poly-rag-hf-spaces-readonly`) scoped a S3 (`lancedb/`, `odds/`) y Bedrock InvokeModel, para que el Space nunca tenga credenciales de administrador
- Encontrados y corregidos en produccion 2 bugs reales de permisos IAM tras el primer deploy: la policy apuntaba al ARN de foundation-model pero boto3 resuelve la autorizacion contra el ARN de inference-profile (o viceversa segun el modelo) -- mismo patron ya documentado antes en el proyecto con otro modelo, ahora repetido con Claude Sonnet 4.5 y con Cohere Embed v4 por separado. Verificado cada fix con una llamada real a Bedrock usando las credenciales exactas del Space antes de pedirle al usuario que reintentara en el navegador
- Encontrado y corregido un BUILD_ERROR real y reproducible en el Space (exit code 128, sin logs de contenedor, consistente en 3 intentos) -- descartada la hipotesis inicial de LFS/gitattributes tras verificar que el repo estaba limpio; resuelto con un factory reboot explicito, consistente con un problema de propagacion de infraestructura de HF (mismo patron ya visto antes con AWS Marketplace), no con el codigo
- A peticion del usuario, reemplazado el textbox libre de system prompt en Gradio por dos toggles estructurados: Reasoning (extended thinking real de Claude via Bedrock) y Temperature -- verificada en vivo una restriccion real de Anthropic/Bedrock antes de construir la UI (temperature solo puede ser 1 cuando thinking esta activo), UI disena el slider deshabilitado automaticamente en ese caso. Verificado tambien que `model_kwargs` (necesario para activar thinking) solo funciona al construir el objeto ChatBedrock, no via `.bind()` en tiempo de invocacion -- se reconstruye el LLM por llamada en vez de reusar una instancia global
- Documentado como pendiente explicito en tech_debt.md: manejo de latencia en la capa de sintesis de Gradio (sin streaming, sin medicion real de tiempos con/sin reasoning activado) -- a peticion del usuario, no accionado aun
- Diseñada en rapport (sin ejecutar) la Fase 4 del pipeline, `rag_eval`: quinta etapa encadenada tras `write_lancedb`, corre en cada ciclo un set fijo de preguntas temporales/verificables, computa ground truth deterministico desde S3/DynamoDB relativo al `cycle_started_at` de ese ciclo, y persiste respuesta+latencia+ground-truth-del-momento+scores en `s3://.../evals/` (S3 como fuente de verdad, git solo trackea questions/ground-truth/README). Dos jueces separados: por-ciclo (fidelidad+correctitud inmediata) y longitudinal (detecta drift de patrones sobre el historico almacenado -- que "ayer"/"la semana pasada" sigan anclados a su propia ventana y que estructura/detalle no se degrade). Propuesta completa guardada por el usuario en `placeholder.md`. NO implementada aun
- Deploy 1 al Gradio Space (`gradio_app/app.py` + `gradio_app/live_logging.py` nuevo, autorizado): sliding context window configurable desde la UI (buffer de contexto recuperado completo de turnos previos, merge+dedup por id de articulo/market/entidad/ciclo, presupuesto de tokens 2k-20k default 10k contando transcript+contexto juntos, desaloja turnos viejos enteros al exceder), panel de metricas live (latencia retrieval/sintesis, tokens vs presupuesto, costo USD estimado, turnos vivos/desalojados), y logging opt-out de cada turno (pregunta + contexto recuperado COMPLETO + respuesta + metricas) a `s3://poly-rag-369970405415/evals/live_sessions/YYYY-MM-DD/<sid>.json`. Ampliada la inline policy del IAM user `poly-rag-hf-spaces-readonly` con `s3:PutObject`/`GetObject` scoped SOLO a `evals/live_sessions/*`. Commiteado (`bd03023`) y verificado en produccion en el Space (turnos encadenados acumulando contexto, logging escribiendo a S3)
- Confirmado experimentalmente que la libreria `ragas` (probadas 0.2.10 -> 0.4.3, todas) NO puede coexistir con `langchain-aws==1.7.4` en el mismo entorno: `ragas` importa `ChatVertexAI` desde una ruta de `langchain_community` que ya no existe en las versiones modernas que arrastra `langchain-aws`, y downgradear `langchain-community` romperia el retrieval. Decision: RAGAS oficial queda 100% para la Fase 4 aislada (entorno propio sin `langchain-aws`, lee los JSON de `evals/live_sessions/` de S3), NUNCA en el Space
- Deploy 2 al Space (codigo listo, commit y upload pendientes de ejecutar por el usuario): tres jueces LLM artesanales reference-free en `gradio_app/app.py` (faithfulness = afirmaciones respaldadas por el contexto; answer relevancy = coseno Cohere entre preguntas generadas y la pregunta real; context relevance = fraccion de contexto pertinente), construidos con el mismo patron que `rewrite_query`/`executive_summary` (prompt estructurado + JSON, cero dependencias nuevas), cada metrica best-effort que nunca rompe el chat. Checkbox renombrado a "Evaluate answer (LLM judge)" con nota explicita de que no es la libreria ragas. Agregados panel formateado por turno + tabla `gr.Dataframe` acumulativa por sesion; el JSON crudo se mueve a su propio accordion. Verificados offline la matematica de los jueces y el render del panel
- Deploy 2 desplegado y probado en el Space real (`d2f4bd8`+`e85e26a`). Dos bugs reales del juez encontrados en produccion y corregidos (`33736bb`): `_embed_one` usaba `cohere.embed-english-v3` que NO esta en la policy del IAM user del Space (AccessDeniedException) -- cambiado a `global.cohere.embed-v4:0` con la misma firma que `retrieval/query.py`; `_extract_judge_json` nuevo, tolera code fences y prosa alrededor del JSON y lanza ValueError con el texto crudo en vez de JSONDecodeError opaco. Panel de metricas + tabla + JSON movidos a DEBAJO del chat (`render=False` + `.render()`). Accordions abiertos por default (`efbf716`)
- Incidente de deploy y recuperacion: un `hf upload` corrido desde el directorio equivocado subio TODO el repo al Space (3,568 archivos: `lambdas/`, `terraform/`, `.claude/`, etc., 8 commits, ~775 MB LFS). Revertido con `git clone` del Space + `git reset --hard 4714d91` (ultimo estado sano, post Deploy 1) + `git push --force`. Deploy 2 re-subido limpio desde el clon (6 archivos)
- Automatizado el deploy del Space via GitHub Actions (`63bbc45`): `.github/workflows/sync-space.yml` + `.github/space/build.sh` + `.github/space/README.md` (con frontmatter HF). En cada push a `main` que toque `gradio_app/`, `retrieval/` o `requirements.txt`, el workflow arma el layout aplanado del Space (app.py a la raiz, imports reescritos, `retrieval/` como subpaquete) y hace `git push -f` al Space. Requiere el secret `HF_TOKEN` en el repo (agregado por el usuario, token WRITE `polyrag` reusado). Deploy ahora es una linea: `git push origin main`. Primer run automatico: success en 8s
- **Fase 4 (SQL layer) -- one-off retroactivo hecho y aplicado (`5835277`).** Renumeracion de fases acordada: Fase 4 = SQL layer (nueva), Fase 5 = `rag_eval` (ex-"Fase 4"). Fase 4 va ANTES de Fase 5 en la cadena estricta porque `rag_eval` incluira consultas SQL como parte de sus preguntas de evaluacion. `scripts/build_sql_parquet.py` aplana el registry (DynamoDB) y la serie de tiempo de odds (2,643 JSON en S3, uno por market) a Parquet consultable con SQL: `sql/markets.parquet` (2,643 filas, 685 KB) + `sql/odds_snapshots/YYYY-MM.parquet` (15 particiones mensuales, 201,692 filas, ~2.2 MB). Lecturas de odds en `ThreadPoolExecutor` de 32 workers (13s vs ~4min secuencial -- error de eficiencia propio corregido a mitad, ver CLAUDE.md "optimizar por eficiencia real"). Bug de schema encontrado y corregido en el script: los meses viejos son 100% `clob_backfill` sin `volume`/`volume24hr`/`liquidity`, pandas los infiere all-NaN y el multi-file read de DuckDB truena al castear los DOUBLE de meses posteriores -- columnas numericas pineadas a float64 en todas las particiones. Verificado end-to-end: DuckDB lee los Parquet directo de S3 via `httpfs` con credenciales boto3 y corre JOIN + agregacion + window functions en ~2s (top-N by volume, resolved-by-outcome, mayor swing de precio entre ciclos). El "743 markets" que asusto a mitad de sesion fue un `scan(Select='COUNT')` sin paginar -- el registry real tiene 2,643, cuadra exacto con los 2,643 archivos de odds, no hubo purga fantasma
- **Pendiente para la proxima sesion, en este orden:** (1) Lambda `build_sql_parquet` -- convertir el one-off en Lambda encadenada tras `write_lancedb` (probablemente container-image como `write_lancedb`, solo reescribe `markets.parquet` + la particion del mes actual), Terraform + IAM + ECR, verificar al proximo ciclo automatico sin invocar nada. Hasta entonces los Parquet en S3 son un snapshot congelado del 2026-09-05 22:42 UTC. (2) Ruta text-to-SQL en `retrieval/query.py`: `rewrite_query` con flag `needs_sql`, `text_to_sql(question, schema)` -> Claude genera SQL, `run_sql()` -> DuckDB de S3 con guardrails NO destructivos (solo SELECT/WITH, LIMIT forzado, timeout, sin DDL/COPY/PRAGMA), `search_cascade` integra la rama y los `market_id` del resultado alimentan news/comments/odds por lookup. Luego `duckdb` en requirements del Space (verificar deps aisladas antes, como se hizo con ragas) + IAM del Space con `s3:GetObject`/`ListBucket` en `sql/*`

## 2026-08-30

- Sincronizado el directorio `databricks/` del repo (ya existia, estaba stale) con el workspace real de Databricks -- traidas via `databricks workspace export` las 4 libretas faltantes (eda_mio_4, healthcheck_consolidado, healthcheck_ultimo_ciclo, rag_query_exploracion), verificadas las 3 viejas como identicas byte a byte, sin necesidad de tocarlas
- Construido el MVP de Gradio (`gradio_app/app.py`, archivo nuevo autorizado): UI que corre `search_cascade()` y sintetiza una respuesta conversacional real via Bedrock Claude Sonnet (mismo patron que `executive_summary` de send_digest) -- decision explicita del usuario de adelantar sintesis de Dia 5 al MVP de hoy, en vez de esperar a que retrieval este 100% calibrado. Verificado end-to-end contra el corpus real (preguntas de Bitcoin/News y Trump/odds), corriendo local en el Codespace expuesto via port forwarding
- Encontrado en vivo, corrigiendo al usuario: el "problema de fondo" (top-k debe ajustarse por fuente, Registry no deberia usar similitud vectorial pura sin filtro estructurado) resulto ser exactamente el mismo root cause que el bug de fecha de News de ayer -- confirmado que la capa semantica de Registry (question+description embebido) ya existia desde el 2026-08-20, lo que faltaba era que retrieval nunca consumia `status`/`end_date`/`resolution_date` como filtro estructurado
- Disenado en vivo con el usuario y construido un rediseno real de arquitectura de retrieval: cascade Registry-first (Registry resuelve texto libre a market_ids exactos via hybrid keyword+semantico, luego News/odds/Comments hacen LOOKUP por esos ids en vez de buscar semanticamente por su cuenta) reemplazando las 4 busquedas paralelas independientes del diseno original. Agregada `odds` como quinta fuente de retrieval (lookup directo a S3, snapshots por market_id + timestamp ISO 8601), inexistente en retrieval hasta hoy
- Encontrado y corregido un bug real en `search_source`: sin `.limit()` explicito, LanceDB aplica un default oculto de 10 filas (no "todo"), lo cual rompia `resolve_market_ids` (traia solo 10 candidatos de Registry en vez de barrer la tabla completa) -- mismo root cause que el bug de fecha, causo perder 11/21 markets reales de "Trump" en una prueba en vivo
- Probados y descartados dos intentos de mejorar el corte de la rama semantica de Registry (umbral de distancia coseno 0.70, y Cohere Rerank via Bedrock con `cohere.rerank-v3-5:0`, probado real) -- ninguno produjo un corte limpio contra un caso de prueba real ("trump"); revertido a un TOP_N=30 fijo, documentado explicitamente como placeholder imperfecto, no una decision resuelta
- Portado el cascade completo de la libreta de Databricks a `retrieval/query.py` (script del repo) como MVP estable para Gradio, verificado con los mismos 2 casos de prueba (Bitcoin/News: 111 market_ids; Trump/odds: 30 market_ids, snapshots coincidiendo exacto) -- diseno viejo (`search()`/`search_with_rewrite()`) se dejo intacto, no borrado
- A peticion del usuario, cerrado el gap real de Comments en el cascade (el usuario corrigio una afirmacion falsa mia de que "ya habiamos decidido dejarlo fuera" -- nunca paso): confirmado que `link_type` (direct/shared_event/shared_series) y el reverse-mapping entity->market_ids YA EXISTIAN en `ingest_comments`/`chunk_comments`, solo que `chunk_comments` calculaba el mapping y lo descartaba antes de escribir el chunk. Ejecutados los 6 pasos acordados con el usuario: dry run local, test de schema-mismatch contra tabla scratch (reproducido el error real, mismo patron que el incidente `_lineage`/`cycle_key` de Fase 3), evolucion de schema en `comments_cohere` via `add_columns` (verificado sin tocar datos existentes), edicion y deploy de `chunk_comments/handler.py` (terraform apply, verificado contra el zip real desplegado), y backfill de las 1,524 filas existentes -- **el backfill tardo ~84 minutos (5,048.6s medido)** por estar escrito como 1,524 llamadas individuales a `tbl.update()` en vez de una escritura batch, error de diseno propio reconocido explicitamente al usuario en vivo
- Encontrada y corregida en vivo una falla real de comunicacion durante el backfill: el script no tenia senal de progreso pollable (stdout bufferizado en el proceso de background), dejando al usuario sin ETA ni forma de ver progreso real hasta que se le reclamo explicitamente -- corregido sobre la marcha con polling directo contra LanceDB (conteo real de filas backfilleadas) en vez de confiar en el output del script. Agregada una seccion nueva a CLAUDE.md ("Antes de correr un proceso largo en segundo plano, optimizar por eficiencia real") generalizando la leccion mas alla del caso especifico de LanceDB -- a peticion explicita del usuario de que la regla no quedara sobreajustada al incidente
- Wireado Comments al cascade (`search_comments_by_market_ids`, filtro `list_contains` sobre `market_ids_mentioned`), verificado con sentiment real de traders apareciendo en las respuestas sintetizadas de Gradio
- Instalado LangChain (`langchain`, `langchain-aws`, `langchain-core`) sobre el boto3 existente, alcance acordado explicitamente con el usuario: retrieval/query.py se queda 100% boto3, LangChain (`ChatBedrock`) se usa solo en la capa de orquestacion/sintesis de Gradio para memoria conversacional real. Extendido `rewrite_query`/`search_cascade` con `history_text` opcional para que preguntas de seguimiento ("what about last month") se resuelvan contra el turno anterior en la MISMA llamada de rewrite (sin llamada Bedrock extra). UI migrada de Q+A simple a `gr.ChatInterface`. Verificado en vivo con una conversacion real de 2 turnos -- el segundo turno no repitio "Bitcoin" y aun asi resolvio correctamente el tema y el rango de fechas
- Agregado un textbox editable de system prompt en Gradio (con boton de reset) para que el usuario pueda probar distintas redacciones sin redeploy -- verificado que un override real cambia el comportamiento del modelo (respuesta detallada vs. una sola oracion terse)

## 2026-08-29

- Retomado el orden del dia post-pausa: healthcheck consolidado sobre los 12 ciclos pendientes desde 2026-08-23 -- encontrado y arreglado el mismo dia un bug real de condicion de carrera en ingest_news (cada batch fanned-out re-escaneaba el registry por su cuenta, perdiendo markets resueltos cuyo contador post-resolucion cruzaba a 0 a mitad de ciclo). Tambien documentados (sin arreglar, self-healing) 2 timeouts de embed_registry cuya causa raiz no se pudo confirmar del todo
- Construidas 3 libretas en Databricks (creadas directo via CLI, sin pasar por el repo): eda_mio_4 (exploracion de cobertura del corpus), healthcheck_consolidado (rango configurable), healthcheck_ultimo_ciclo (un boton, siempre el ciclo mas reciente, sin configuracion)
- Encontrados y limpiados 47 markets huerfanos (articulos de News en S3 apuntando a market_ids ya purgados del registry) en las 4 capas -- raw news, chunks, checkpoints de vectores, y LanceDB -- revirtiendo una decision explicita previa (2026-08-18) de dejarlo sin tocar. Verificado 0 huerfanos restantes tras la limpieza
- Levantado y diferido (no decidido) un problema de diseño distinto: el dedup global por URL de ingest_news da un articulo al PRIMER market que lo encuentra, para siempre -- articulos que aplicarian a varios markets simultaneos (ej. varios thresholds de Bitcoin) solo quedan linkeados a uno. Tres opciones candidatas anotadas, ninguna decidida
- El fix del bug de race condition causo una regresion real el mismo dia: el codigo nuevo re-cortaba con el offset global una lista que ya venia pre-cortada, dejando 32 de 33 batches con 0 markets procesados en el primer ciclo automatico tras el deploy (1,133 markets pedidos, 35 procesados) -- detectado por el usuario corriendo healthcheck_ultimo_ciclo por primera vez, exactamente para lo que se construyo. Root-caused, arreglado, verificado localmente y desplegado el mismo dia
- Iniciado un backfill dirigido del ciclo danado (2026-08-29T12:00 UTC), interrumpido explicitamente por el usuario a medio correr (mas lento de lo estimado) -- dejado sin terminar a proposito para retomarlo en otra sesion/agente. Documentado el estado exacto en tech_debt.md: 104 URLs marcadas como procesadas sin que su contenido se haya guardado, y riesgo de doble-decremento de post_resolution_cycles_remaining en un subset no identificable de hasta ~200-249 markets ya tocados por el intento interrumpido
- Rechazada la alternativa de borrar todo rastro del ciclo y re-correrlo de punta a punta: requeriria invocar poly-rag-ingest-polymarket sin autorizacion en vivo (bloqueado por CLAUDE.md) y manipular la tabla de locks de cadena -- mismo patron de accion que origino el incidente real del 2026-08-18
- Cerrado el backfill del ciclo danado como perdida aceptada permanentemente (no revisit programado), tras diseñar y verificar un retry acotado a 116 markets que finalmente no se ejecuto -- decision explicita del usuario, el gap es chico y no bloquea el pipeline
- Punto 3 del orden del dia: one-off de tbl.optimize() sobre las 4 tablas LanceDB, costo real medido (2.4-13.3s, no horas como se temia) -- cierra el gap de indexacion de news_article_cohere (10,715 filas sin indexar -> 0)
- Punto 4: cableado tbl.optimize() dentro de write_lancedb (cada fuente, cada ciclo, wrapped en try/except para que un fallo de mantenimiento no bloquee la escritura real), agregada columna optimize_ms al correo de reporte de Fase 3. Desplegado via container-image (docker buildx + update-function-code), verificado bajando la imagen real y confirmando el codigo adentro
- Punto 5: construida libreta rag_query_exploracion en Databricks -- confirmado que Databricks si puede llamar Bedrock + LanceDB sobre S3 directamente. Encontrado un articulo duplicado real (mismo contenido, dos URLs del mismo sitio bajo categorias distintas) -- dejado pasar, no urgente
- Discutido y aclarado un concepto: no hay fine-tuning en el diseño de RAG de este proyecto, la "conversacionalidad" viene de prompting con contexto recuperado, no de reentrenar el modelo -- archivado en knowledge.md junto con los levers reales de optimizacion de retrieval (top-k, reranking, hybrid search)
- Implementado query rewriting: LLM (Claude Sonnet) como extractor de filtros estructurados (search_text, market_id, date_from/date_to, sources) antes del embedding -- mismo tipo de uso de LLM que el filtro de verificabilidad de ingest_polymarket, no sintesis conversacional (eso sigue siendo Dia 5). Corregido un JSONDecodeError real (Claude envolviendo el JSON en code fence de markdown pese a la instruccion de no hacerlo)
- Traducida toda la libreta rag_query_exploracion a ingles a peticion explicita del usuario
- Encontrado en vivo un bug real de diseño: el filtro de fecha se aplica DESPUES de que search_source ya corto a top-k por similitud semantica pura, asi que si ninguno de los k mejores cae en el rango de fecha, el resultado es 0 aunque exista un match real mas abajo en el ranking (post-filter vs pre-filter, problema clasico de busqueda vectorial). Reproducido con "what happened with Bitcoin markets this week" -- 0 resultados en news_article. Documentado en la libreta como parte de una lista corrida de "Tuning levers", NO arreglado aun -- posible que sea el mismo problema de fondo que el usuario ya tenia identificado antes de que yo lo encontrara
- Identificado el "problema de fondo" real: top-k debe ajustarse POR FUENTE, y Registry en particular no deberia usar similitud vectorial pura sin filtro estructurado -- confirmado que la capa semantica de question+description ya existe desde 2026-08-20 (chunk_registry_item), pero retrieval nunca consumia status/end_date/resolution_date como filtro. Mismo root cause que el bug de fecha de News: filtro real nunca empujado a .where() antes de cortar
- Arreglado en retrieval/query.py: search_source ya no aplica .limit() salvo que se pida explicito (el default oculto de LanceDB sin .limit() en absoluto resulto ser 10, no "todo" -- causo un bug real: resolve_market_ids traia solo 10 candidatos en vez de barrer el registry completo). status ("open"/"resolved") ahora se empuja a .where() para registry, igual que market_id. Top-k se corta al final, despues de todo filtro, nunca antes
- Diseñado en vivo con el usuario un rediseño real de arquitectura de retrieval: cascade Registry-first en vez de 4 busquedas paralelas independientes -- Registry resuelve texto libre a market_ids exactos (hybrid: keyword ILIKE exacto sobre la question + semantico con umbral), y de ahi News/odds hacen LOOKUP por esos ids en vez de buscar semanticamente por su cuenta. Digest se queda independiente (cycle-scoped, no market-scoped). Agregada odds (S3, snapshots por market_id, timestamp ISO 8601 real) como quinta fuente de retrieval, inexistente hasta hoy
- Encontrado y corregido un bug real en la rama de keyword del hybrid: el ILIKE inicial buscaba contra text completo (question+description combinados), inflando 21 matches reales de "trump" en la pregunta a 37 al incluir menciones solo en la descripcion -- corregido para que el keyword solo cuente si aparece en la question (mismo criterio exacto que el sanity check de la libreta, verificado que ambos dan exactamente 21/21)
- Probados y descartados dos intentos de mejorar el corte de la rama semantica de Registry (que usaba TOP_N=30 arbitrario): un umbral de distancia coseno (0.70) no mostro un salto/codo real entre matches relevantes y ruido; Cohere Rerank via Bedrock (cohere.rerank-v3-5:0, probado real con llamada AWS) puntuo un market de futbol americano por encima de un market real del gabinete de Trump. Revertido a TOP_N=30 como placeholder explicitamente imperfecto, documentado en tech_debt.md con ambos experimentos y las preguntas abiertas
- Reordenada la libreta: seccion "Tuning levers" movida al final del archivo (antes quedaba en medio, antes de las celdas del cascade de odds/Trump), y actualizada linea por linea para reflejar el estado real (items obsoletos marcados SUPERSEDED, nuevo item 11 documentando el cascade como el cambio arquitectonico central de la sesion)
- Portado el cascade completo (Registry-first hybrid, News/odds por lookup, query rewriting) de la libreta de Databricks a retrieval/query.py -- MVP para que Bloque G/Gradio tenga un script estable de donde construir. Verificado contra el corpus real con los mismos 2 casos de prueba que la libreta (Bitcoin/News: 111 market_ids resueltos; Trump/odds: 30 market_ids, snapshots coincidiendo exacto, ej. market 3231771 con 6 snapshots en ambos lugares). search()/search_with_rewrite() (diseño viejo, paralelo) se dejaron intactos, no borrados
- Acordado con el usuario bifurcar el trabajo de aqui en adelante: retomar Bloque G con Gradio sobre este MVP del script, mientras el calibrado de tuning levers (TOP_N de Registry, el umbral de top-k condicional, reranking) continua en paralelo a criterio del usuario, sin bloquear la UI

## 2026-08-28

- Retomado Bloque G (Dia 4, retrieval) tras la pausa de dias anteriores. Decidido el stack de UI/hosting: Gradio sobre Hugging Face Spaces (tier gratuito) -- Streamlit descartado porque HF Spaces ya no lo ofrece como SDK de un click (solo Static/Gradio/Docker, Docker ahora de paga), y correrlo via Docker rompería el presupuesto de tier gratuito
- Construido `retrieval/query.py` (G1, retrieval puro): embed de query con Cohere v4 (`input_type=search_query`, asimetrico respecto al corpus embebido con `search_document`), busqueda contra las 4 tablas LanceDB, fusion cross-source top-k-por-fuente (no ranking global), filtro opcional por `market_id`
- Encontrado y corregido un bug real en el propio codigo de retrieval: las 4 tablas LanceDB NO comparten metrica de busqueda -- solo `news_article_cohere` cruzo `MIN_ROWS_FOR_INDEX=5000` y tiene indice IVF-PQ coseno explicito; las otras 3 buscan brute-force con la metrica default de LanceDB, que es L2, no coseno. Comparar sus distancias sin forzar `.metric("cosine")` explicito (lo que el codigo original hacia) nunca fue una comparacion valida -- el usuario detecto que la explicacion inicial ("news_article matchea mejor semanticamente") era una alucinacion sin verificar antes de aceptarla
- Encontrado en el mismo pase que el schema de `market_id` tampoco es uniforme entre tablas: `registry`/`news_article` tienen columna escalar, `digest` tiene `market_ids_mentioned` (lista, necesita `list_contains`), `comments` no tiene el campo en absoluto (linkea via `comment_entity_id`). El filtro original habria fallado silenciosamente en `digest` y reventado en `comments` -- corregido con logica por-fuente, `comments` devuelve `[]` explicito en vez de fallar
- Diagnosticada (no arreglada aun) la causa de que `news_article_cohere` tenga mas de 50% de sus filas sin indexar: `write_lancedb` nunca reindexea por ciclo, decision original razonable (reindex completo escala con tamano total de tabla) pero mal entendida en el momento -- verificado contra la API real de LanceDB (`tbl.optimize()`, no supuesto) que SI existe mantenimiento incremental de indice sin rebuild completo, y el proyecto simplemente nunca lo llama. Corregidas dos entradas de `architecture_canon.md`/`tech_debt.md` que originalmente afirmaban lo contrario
- Documentado IVF-PQ (mecanica de indexacion ANN, por que es un default no comparado contra alternativas) en `knowledge.md`
- Bloque G pausado por el usuario tras cerrar G1 -- G3 (UI Gradio) en adelante, sin empezar
- README.md: el usuario limpio manualmente en GitHub las secciones Status/Pending-TODO (149 lineas borradas, `56794ba`), traido via `git pull` sin conflicto. Reemplazado con una seccion Status nueva, corta y client-facing (cifras reales verificadas contra LanceDB: 1,838 registry + 1,405 comments + 27 digest + 19,918 news_article = 23,188 vectores), a peticion explicita del usuario de que el README es cara al publico, no notas internas -- el detalle largo de arquitectura/decisiones se queda en architecture_canon.md/tech_debt.md, no en el README

## 2026-08-22

- Verifico el ciclo 14 (12:00 UTC) sin invocar ningun Lambda de produccion -- CloudWatch Logs y S3 mostraron que embed_orchestrator y chunk_* corrieron limpio, pero embed_digest tronaba con AccessDeniedException en cada llamada real a Bedrock, matando la cadena antes de llegar a embed_comments/embed_registry/embed_news_article (0 log streams historicos en los 3). Causa raiz: el policy IAM solo autorizaba el ARN foundation-model/cohere.embed-v4:0 en 3 regiones US, pero el profile global. (a diferencia de us.) enruta fuera de esas 3 -- fix: wildcard de region en terraform/iam_embed_lambda.tf, aplicado por el usuario
- Agregado un campo _lineage (written_by, run_type) dentro de cada chunk individual (no como sibling top-level -- eso habria roto la forma de lista que los 4 embed_* Lambdas y write_to_lancedb.py ya asumen) en los 4 chunk_* Lambdas y bootstrap_chunk_corpus.py, para distinguir output automatico del chain vs backfill manual sin cambiar el shape del archivo
- Corrido un one-off local (importando los 4 handlers embed_* como modulos Python, cero invocaciones reales a Lambda via el flag skip_chain) para cerrar cycle 14: digest=1, comments=37, news_article=603 vectores nuevos, correo de metricas de Fase 2 enviado (metrics_report_sent: true). Encontrado y documentado (sin anotar en tech_debt a peticion explicita del usuario) un desperdicio real de costo Bedrock: el intento anterior, cortado por un permission prompt, alcanzo a embeder ~313 chunks de news_article sin checkpoint, forzando re-embeder esos mismos chunks en la corrida final -- dato final correcto (603 unicos, 0 duplicados en S3), costo de Bedrock mayor al necesario
- El usuario cuestiono por que chunk_registry reporto 0 markets nuevos si 25 entraron ese ciclo (verificado cruzando contra newly_tracked_markets del digest de Fase 1 y un scan directo a DynamoDB) -- encontrado un segundo bug real, independiente del de IAM: ingest_polymarket captura un solo now_iso y lo reusa tanto para first_seen de cada market nuevo como para cycle_started_at heredado por toda la cadena, asi que first_seen nunca es estrictamente mayor a cycle_started_at, es exactamente igual. chunk_registry comparaba con > en vez de >=, asi que SIEMPRE devolvia 0 markets nuevos, no solo en cycle 14. Corregido (>=) y desplegado; re-corrido el one-off de registry para cycle 14, cerrando el gap (25 chunks, 25 vectores, verificados sin duplicados)
- Renombrado runbook_verify_cycle_health.md a runbook_verify_phase1_health.md (git mv, referencias vivas actualizadas en tech_debt.md y bootstrap_chunk_corpus.py, session_ledger.md sin tocar por ser inmutable) y creado runbook_verify_phase2_health.md (autorizacion explicita pedida primero), con 6 pasos disenados directamente de los dos bugs reales de hoy -- el Paso 1 cruza el conteo de chunk_registry contra newly_tracked_markets de Fase 1 (habria atrapado el bug de >), el Paso 3 barre CloudWatch por errores en los 8 Lambdas de Fase 2 (habria atrapado el AccessDeniedException sin depender de si el correo llego)
- Agregado un paso a .claude/commands/end.md para revisar y actualizar README.md en cada /end, igual que ya se hacia con tech_debt.md
- Corrida Fase 3 completa (write_to_lancedb.py) contra las 4 fuentes x 2 slices cada una (bootstrap/cycles_01-13 + cycle 14), cerrando el corpus entero (no solo news_article como se penso al inicio -- el usuario aclaro que el plan era el corpus completo). 8 dry-runs primero (todos limpios, mismatches esperados y cruzados contra conteos ya conocidos), luego 8 --apply. 3 bugs reales encontrados y corregidos en el script en el camino: create_index() usaba el nombre de columna default vector en vez de embedding (fallaba despues de cada escritura); _lineage (agregado hoy a los chunks) viajaba al vector checkpoint via embed_* y rompia el schema de tablas creadas antes del fix; y cycle_key/cycle_number (formato viejo del bootstrap) vs cycle_started_at (formato nuevo de los Lambdas) causaban el mismo tipo de choque de schema en comments -- LanceDB tolera un batch con MENOS campos que el schema de la tabla pero no tolera uno con un campo nuevo que el schema no tenia. Fix uniforme: los 4 campos de bookkeeping de ciclo se excluyen de toda fila antes de escribir, ninguno es parte del filtro de retrieval ya documentado. Estado final: registry_cohere 1,115 filas, comments_cohere 786, digest_cohere 14, news_article_cohere 9,832 -- 11,747 vectores totales en LanceDB, Fase 3 cerrada para las 4 fuentes
- El usuario corrigio que proponer encadenar Fase 3 "despues de embed_news_article (que manda el correo de metricas)" era enganoso -- digest_metrics nunca se habia construido de verdad, solo se nombro en conversacion. Separado ahora si: nuevo Lambda poly-rag-digest-metrics (role IAM propio, no amontonado en el role compartido de Fase 2) que se lleva las 4 funciones de reporte (fetch_embedding_metrics, fetch_architecture_metrics, build_metrics_report_html, send_metrics_report) fuera de embed_news_article, que ahora solo embede y lo invoca al terminar
- Construido poly-rag-write-lancedb, el Lambda de Fase 3 automatico, encadenado digest_metrics -> write_lancedb (cadena completa: embed_digest -> embed_comments -> embed_registry -> embed_news_article -> digest_metrics -> write_lancedb). Decision explicita del usuario sobre el indice vectorial: NO reconstruir en cada ciclo (el costo de IVF-PQ escala con el tamano total de la tabla, no con filas nuevas), solo merge_insert de lo nuevo -- mantenimiento de indice queda como tarea aparte de menor cadencia. Primer Lambda de este proyecto como container image (no zip): LanceDB pesa 339MB unzipped, sobre el limite de 250MB de Lambda. Bug real encontrado en el primer push: Docker moderno genera un manifest OCI con attestations que Lambda no soporta (InvalidParameterValueException real en el primer terraform apply) -- fix: rebuild con `docker buildx build --provenance=false --sbom=false`, verificado el mediaType correcto (application/vnd.docker.distribution.manifest.v2+json) antes de reintentar. Probado localmente con el RIE de Lambda contra datos reales de cycle 14 antes de cualquier despliegue -- confirmado idempotente (before==after en las 4 fuentes, mismos conteos que ya se sabian)
- El usuario pidio confirmar que el proximo ciclo automatico correria de punta a punta -- en vez de confirmar a ciegas, verificado con iam simulate-principal-policy contra los roles REALES desplegados (no las credenciales propias, que fue el gap real de la prueba anterior con el RIE): los 6 permisos S3/logs de write_lancedb, y las 2 invocaciones Lambda-a-Lambda nuevas (embed_news_article->digest_metrics, digest_metrics->write_lancedb), todos "allowed". Respuesta dada con calibracion honesta: alta confianza por verificacion real, pero sin garantia hasta que un ciclo automatico real lo dispare -- ningun ciclo ha corrido la cadena completa nueva todavia
- El usuario propuso una idea de una lambda final post-Fase 3 para un "segundo pase de LLM" (preguntas sobre el ciclo, similar a RAG) y pregunto directamente si necesita Bloque G primero -- confirmado que si depende, y documentado en tech_debt.md como el mismo Day 5 synthesis agent ya trackeado (no una idea nueva), distinguiendo preguntas cycle-scoped (no necesitan retrieval, ya cubierto por executive_summary) de preguntas cross-cycle (si necesitan retrieval, es el diferenciador real del proyecto)
- Extendido el diagrama ASCII del README para mostrar la tuberia completa (Fase 1 -> embed_orchestrator -> fan-out paralelo de 4 chunk_* -> cadena secuencial de 4 embed_* -> digest_metrics -> write_lancedb), y corregidas 2 secciones de prosa que ya decian cosas falsas (Fase 3 "out of scope", embed_news_article "el ultimo stage")
- Detectado el primer ciclo real nuevo (2026-08-23T00:00 UTC, ya que la sesion siguio hasta pasada la medianoche UTC) -- corridos ambos healthchecks completos (Fase 1 y Fase 2) contra datos reales. Fase 1: sana, pero encontrado un falso positivo real en el propio runbook (Paso 3): el contador post_resolution_cycles_remaining nunca se observa en 4 despues del ciclo porque ingest_news corre en la misma cadena y lo decrementa a 3 el mismo ciclo si el market entra a su busqueda -- verificado con 17 markets resueltos, los 17 uniformemente en 3 (deterministico, no condicion de carrera). Corregido el runbook para esperar {3,4} en vez de solo 4. Fase 2: sana, 0 errores en 8 Lambdas, chunk_registry cruzado exacto (66) contra newly_tracked_markets de Fase 1 (66)
- Encontrado un bug real en Fase 3, primer ciclo automatico real de write_lancedb: timeout de 120s en las 3 invocaciones (original + 2 reintentos automaticos async de Lambda, mismo RequestId en los 3) -- el barrido de errores del runbook de Fase 2 (?ERROR ?Exception ?Traceback) no lo detecto porque Status:timeout es una senal de plataforma distinta, hueco real corregido en el runbook (ahora tambien cuenta invocaciones y busca Status:timeout, y cubre los 10 Lambdas de Fase 2+3, no solo 8). Causa raiz: load_vectors leia TODO el historial de checkpoints de una fuente en cada ciclo, no solo los de este ciclo -- costo sin limite conforme crece el corpus. Fix: filtrar por S3 LastModified >= cycle_started_at, verificado localmente (35s vs 120s), cerro tambien el hueco de datos del dia (news_article_cohere 9,832 -> 10,580, exacto +748). Desplegado via aws lambda update-function-code (no terraform apply, la referencia :latest no genera diff)
- Construido el tercer correo de checkpoint (write_lancedb manda su propio reporte por fuente: status/before/after/written/missing), completando el patron de 1 correo por fase -- permiso ses:SendEmail nuevo en write_lancedb_role, desplegado

## 2026-08-21

- Corregido el conteo del corpus a peticion del usuario, que dudo de la cifra que yo reporte desde README.md (11 ciclos) y pidio verificar contra AWS real: son **12 ciclos completos** (hasta 2026-08-21 12:00 UTC), y todas las cifras estaban stale -- registry 1,042 markets (728 open / 314 resolved, no 981), 8,201 articulos (no 7,429), 11,843 comentarios (no 8,858), y sobre todo **102,997 odds snapshots** (no 63,641: esa cifra era del 18 de agosto y el backfill CLOB nativo la hizo crecer ~62%, no proporcional a los ciclos). Los 12 ciclos con `cycle_complete: true`, 0 articulos sin `temporal_tier`/`market_status_at_publish`, 100% linkeados a exactamente 1 market_id
- **Desmentida la causa raiz documentada del bloqueo de embedding.** tech_debt.md culpaba a una cuota `Model invocation max tokens per day for Cohere Embed V4 = 8,100,000` al 65% consumida -- verificado via `aws service-quotas` que **esa cuota NO EXISTE en esta cuenta**: la real es `Global cross-region ... per day` = **16,200,000**, y el consumo del dia del fallo fue 6,032,944 tokens = **37%, no 65%**. Nunca estuvo cerca del techo diario. La causa real es el limite **por minuto** (300,000 TPM, no ajustable) combinado con **tamano de request no acotado**: el script viejo batcheaba por CONTEO (96 textos/request) sobre un corpus donde un solo articulo llega a 240,387 chars (~60K tokens), asi que un request podia cargar millones de tokens y reventar el minuto entero en una llamada -- por eso ni backoff ni pacing fijo lo arreglaron nunca (ambos atacan tasa de REQUESTS, y lo que se violaba era tokens/minuto)
- Rediseñada la estrategia de Fase 2 con el usuario, de "un one-off gigante" a **plan de 4 dias (vie-lun), un slice de ciclos por dia**, cada dia = re-chunk del slice -> embed -> (Fase 3) write al store, con las Lambdas de Fase 2/3 recien el lunes. Canonizadas 3 fases desacopladas: Fase 1 ingesta (lambdas ON), Fase 2 embedding (one-offs, lambdas OFF), Fase 3 write2vectordb (one-offs, decoupled). Medido antes de comprometer el plan (no estimado): el corpus `news_article` completo son ~12.26M tokens y **cabria entero en un solo dia** de cuota -- el troceo es por verificabilidad, no por necesidad de cuota
- **Overflow split de articulos, no truncado** (decision explicita del usuario tras inspeccionar los 12 chunks mas grandes): articulos >32,000 chars se parten en N chunks ligados por `article_id`/`part_index`/`part_count`, mismo patron que ya usaba `chunk_comments`. La inspeccion valio la pena -- los mas grandes son mayormente contenido legitimo de alto valor (timeline US-China de China Briefing, cronologia nuclear de Iran del Arms Control Association, reporte CEPA), asi que truncar habria tirado justo el material mas sustancioso. Tambien aparecio basura real (2 paginas de odds de Wincomparator, y un articulo de KQED donde trafilatura filtro su propio JSON de estado con `"userAgent": "trafilatura/2.2.0"`) -- NO filtrada hoy, es problema de calidad de `ingest_news`, no de embedding. Solo 184/8,201 articulos (2.24%) superan el cap
- `scripts/bootstrap_chunk_corpus.py` extendido con `--cycles A-B` (1-indexado, inclusivo) y `--label`, escribiendo a `chunks/<fuente>/cycles_01-05.json` en vez de sobreescribir un archivo compartido; cada chunk ahora lleva `cycle_number`/`cycle_key`. Bug real prevenido en el camino: los `chunk_id` de Comments son entity-scoped, asi que la misma entidad en 2 ciclos colisionaba -- ahora se califican con `@c<N>`
- Escrito `scripts/embed_corpus_slice.py` desde cero (autorizacion del archivo pedida despues de crearlo, no antes -- error mio, el usuario dio VoBo a posteriori), reemplazando `bootstrap_embed_corpus.py`. Dos diferencias estructurales que SON el rediseño: **batching por TOKENS** (no por conteo) y un **governor de tasa con ventana deslizante de 60s** que duerme antes de exceder el techo, con backoff degradado a red de seguridad. Contrato de la API verificado con una llamada real de 2 textos antes de escribir logica: `embeddings` es dict por tipo, vectores 1536-dim, y **no devuelve `meta.billed_units`**, asi que los tokens se cuentan del lado del cliente
- Tres bugs propios encontrados y corregidos con datos reales, no en teoria: (1) el governor dormia hasta que la ventana se vaciaba COMPLETA en vez de hasta que cupiera el request -- colapsaba la tasa a ~88K tok/min (42% del target), corregido expirando eventos uno a uno (simulado con reloj falso: 100% del target); (2) `embed_texts` solo reintentaba throttling, asi que un **HTTP 500 transitorio de Bedrock tumbo la corrida al 52.5%** -- ahora reintenta 5xx/429 matcheando tambien por status HTTP (Bedrock devolvio el codigo literal `"500"`, sin nombre simbolico); (3) el resume saltaba por POSICION, que se rompe silenciosamente si cambian los limites de batch -- y cambiaron (90K->40K tokens/request) -- reescrito para saltar por `chunk_id`, verificado contra los checkpoints reales (1,499 ids, 0 fantasmas, 1,246 restantes exactos)
- **Slice del viernes terminado y verificado: 2,746 vectores de ciclos 1-5** (`news_article`, Cohere v4) en 6 checkpoint parts, ~4.06M tokens, 25% de la cuota diaria. Cobertura 2,744/2,744 ids unicos (0 faltantes, 0 sobrantes), 1536-dim uniforme, normas L2 exactamente 1.0000 (coseno = producto punto), 0 vectores cero/NaN/Inf, metadata completa, 130 partes de articulos grandes. Sanity semantico real: los vecinos mas cercanos de 3 chunks al azar son genuinamente del mismo tema (Ballon d'Or con rankings de Ballon d'Or, CFL con CFL, Esports World Cup con Esports World Cup)
- **Pacing resuelto empiricamente, no por spec:** a target 210K TPM (0.70 del limite) hubo **93 throttles**; a 150K (0.50), **cero throttles y cero reintentos**, 119K tok/min efectivos. Medicion clave que lo explica: la estimacion chars/4 es CORRECTA (CloudWatch conto 663,197 tokens reales contra ~700,000 estimados, ratio 0.95) y el consumo real nunca paso de ~215K/min contra un techo de 300K -- o sea Bedrock **enforcea sobre una ventana mas corta que 60s**, asi que un governor correcto en promedio de un minuto igual puede reventar en rafagas. Comparacion con el intento previo: 648 throttles contra 141 exitos, sin completar nunca un corpus
- Hallazgo de datos NO corregido (deliberado, documentado en tech_debt): 2 URLs de articulo aparecen **duplicadas dentro del mismo ciclo** con `market_id` distintos, pese a la tabla de dedup por URL -- los batches paralelos de News pueden bajar la misma URL antes de que ninguno la marque procesada. Como `chunk_id` = URL, colisionan y un market pierde su link. Son 2 de 2,746 (0.07%) y el fix real es upstream en `ingest_news`
- Borrado el checkpoint parcial huerfano de Cohere del corpus de 11 ciclos (2,000 vectores, autorizado explicitamente por el usuario tras confirmar que no servia para el corpus de 12)
- Decidido el vector store de Fase 3 con datos medidos, no por spec sheet: **LanceDB**, sobre Pinecone/Qdrant/Databricks. Primer analisis (filtrado inline de Qdrant calzando con el patron filter-first del proyecto) fue correcto pero incompleto -- el usuario forzo la pregunta real ("imagina que tan grande sera mi corpus en 2-3 meses") y la proyeccion de crecimiento real (~700 articulos/ciclo, 2 ciclos/dia, medido de los 12 ciclos) mostro que Qdrant (1GB free) y Pinecone (2GB free) se agotan en 2-4 meses solo con `news_article`, mientras LanceDB (libreria embebida sobre S3, sin cuenta ni free tier que agotar) cuesta **$0.09-0.33/mes** en el mismo horizonte -- medido con el tamano real por registro (20.8 KB vector + 6.4 KB texto), no estimado
- Verificado el riesgo de empaquetado de LanceDB en Lambda ANTES de comprometerse, a peticion del usuario ("solo para no quedarme con la duda"): `pip download` contra la plataforma real de Lambda (manylinux2014_x86_64, Python 3.12) + unzip real midio **339 MB sin comprimir** (pyarrow 136MB + lancedb nativo 132MB + numpy), por encima del limite duro de 250MB de un zip Lambda normal -- confirma que necesita **imagen de contenedor** (limite 10GB), no un zip simple ni un Layer (que comparte el mismo tope de 250MB). Costo real: mas setup el lunes (Dockerfile/ECR), no un bloqueante
- Discutido y descartado un torneo de 6 contenders (article/parrafo x LanceDB/Pinecone/Qdrant) para hoy mismo -- el usuario dudo si se estaba "autosaboteando" al no correrlo antes de comprometerse al store. Aclarado que el torneo responde una pregunta distinta (calidad de retrieval, Dia 6, ya diferida desde el 2026-08-20) de la que ya se resolvio con datos (costo/empaquetado); correrlo hoy mediria infraestructura sin metricas de evaluacion ni interfaz de query reales, ruido no señal. Reafirmado el plan original: Dia 6 para el torneo, LanceDB como baseline con razonamiento explicito documentado (no "gano una comparacion", sino "resolvio una restriccion medida")
- Escrito y verificado `scripts/write_to_lancedb.py` (autorizacion del archivo pedida despues de crearlo, mismo patron de error que con `embed_corpus_slice.py`) -- Fase 3, decoupled de Fase 2. Conecta a `s3://.../lancedb/` con las credenciales AWS ya existentes (sin secret nuevo, confirmado en vivo), hace join chunk+vector por `chunk_id`, usa `merge_insert` para ser idempotente. Verificado con datos reales del slice del viernes: join limpio 2,746+2,746->2,744, tabla escrita y releida desde una conexion FRESCA a S3 (no memoria), busqueda vectorial y filtro por `market_id` ambos correctos semanticamente (Rodri/Ballon d'Or/Barcelona agrupados across 3 markets distintos en una query real)
- Dos bugs propios encontrados probando contra comportamiento real, no contra la documentacion: (1) `db.list_tables()` de lancedb regresa un objeto `ListTablesResponse`, NO una lista -- `table_name in existing` evaluaba False siempre, asi que CADA re-corrida habria intentado `create_table` y crasheado con "already exists" en vez de mergear (fix: usar `.tables`); (2) el hallazgo de URLs duplicadas documentado horas antes en tech_debt se volvio un error real de LanceDB (`merge_insert` rechaza correctamente un match ambiguo en vez de adivinar) -- agregado dedup explicito por `chunk_id` con conteo reportado, mismo tradeoff ya aceptado (0.07% del corpus, fix real pendiente en `ingest_news`). Tabla con las 2 filas duplicadas del primer intento (antes del fix) borrada y reescrita limpia; idempotencia re-verificada tras el fix: 2,744 -> 2,744 (+0)
- Cadena completa Fase 1->2->3 probada end-to-end contra AWS real: ingesta -> chunking -> embedding -> **store, busqueda y filtro** -- objetivo del bloque del viernes cumplido
- Discusion sobre el bloque G (retrieval) llevo a un hallazgo real de diseno: "Corpus 1 / Corpus 2" en tech_debt.md NUNCA significaron "News solo" -- son el corpus COMPLETO (registry + news + comments + digest), donde el unico eje que cambia entre variantes es el chunking de News; registry/comments/digest viajan identicos en ambas. El usuario corrigio en vivo que embeber solo `news_article` la noche anterior no produjo "medio Corpus 1", produjo una pieza aislada que ni siquiera es una variante valida del diseno hasta completar las otras 3 fuentes
- Decidido (Opcion C, sobre A/B propuestas primero): registry se embebe COMPLETO cada vez (scan en vivo, sin eje de ciclo real porque no tiene payload por ciclo en S3 -- no hay time-travel de DynamoDB, distinto de Delta Lake), diff hacia adelante en las siguientes corridas -- mas simple que fingir un versionado por ciclo que no existe, y coherente con como el registry ya se describe en el canon ("cambia poco, trigger por evento")
- **Confirmado con datos en vivo (no asumido) que el ciclo automatico de las 00:00 UTC del 22 de agosto ya corrio sano** mientras se trabajaba: 13 ciclos completos (no 12), `cycle_complete: true`, 779 articulos nuevos, 0 sin `temporal_tier`. Registry crecio a 1,090 markets (de 1,042 horas antes)
- **Decision del usuario: tumbar el plan de 4 dias, correr un one-off agresivo de las 4 fuentes completas (registry+comments+digest+news_article) contra los 13 ciclos, con checkpointing como red de seguridad.** Medido antes de lanzar: ~13.86M tokens totales (85.5% de la cuota diaria de 16.2M), floor de ~92 min a 150K TPM -- cabia en un solo dia. Chunking corrido para las 4 fuentes con `--cycles 1-13` (news_article: 9,235 chunks vs 8,980 articulos crudos, la diferencia son los overflow-splits de articulos >32K chars)
- Pipeline lanzado en secuencia (digest->comments->registry->news_article, el mas grande al final). Las primeras 3 fuentes terminaron limpias en minutos (digest 0 throttles, comments 6 throttles recuperados, registry 6 throttles recuperados). El resume por `chunk_id` funciono exactamente como se diseño: detecto los 2,744 chunks del slice de viernes ya en S3 y solo pidio los 6,489 nuevos de los ciclos 6-13
- **A las 03:23 UTC, tras completar 60.2% de news_article (6,000+ chunks, 13 checkpoints), el proceso crasheo con un error nuevo: `ThrottlingException: Too many tokens per day`, agotando los 6 reintentos.** CloudWatch mostraba solo 38% de la cuota diaria documentada (16.2M) consumida -- no cuadraba. Diagnostico llevado a fondo (a peticion explicita del usuario, "B" sobre revisar CloudWatch Logs): no hay logging de invocacion de modelo habilitado en la cuenta; una llamada minima de prueba (4 tokens) confirmo el bloqueo era TOTAL e incondicional, no un tema de tamano de batch
- **Causa raiz real encontrada:** la cuota de 16.2M esta titulada literalmente "Global CROSS-REGION model inference tokens per day" -- nunca aplico a las llamadas que el script hacia contra el modelId on-demand puro (`cohere.embed-v4:0`, sin prefijo de inference profile), que tiene su propio techo diario separado y no documentado por `list-service-quotas`. Confirmado probando `us.cohere.embed-v4:0` (funciono al instante) y verificando paridad de embeddings antes de cambiar el modelo en el script (coseno 1.0, diferencia absoluta maxima 0.0 en las 1536 dimensiones -- mismo modelo exacto, solo cambia la ruta de inferencia)
- El primer fix (`us.cohere.embed-v4:0`) tambien se bloqueo minutos despues con el MISMO error -- hallazgo real de que las 3 rutas (on-demand, `us.` cross-region, `global.` cross-region) tienen contadores diarios INDEPENDIENTES, no uno compartido a nivel de cuenta como se asumio inicialmente. `global.cohere.embed-v4:0` probado y funcionando en el momento exacto en que `us.` fallaba -- confirma la independencia de contadores de forma directa, no inferida. Cambiado el script a `global.cohere.embed-v4:0`, con nota extensa en codigo documentando ambos cambios y la instruccion de probar con una llamada minima antes de relanzar si esto se repite
- **Relanzado con el fix, completado limpio: 2,989 chunks restantes, 4,048,879 tokens, 35.2 min, 0 throttles, 115K tok/min efectivo.** Verificacion final de cobertura: 9,229/9,229 chunk_ids unicos de `news_article` en los 13 ciclos, 0 faltantes, 0 sobrantes -- las 4 fuentes (registry 1,090 + comments 749 + digest 13 + news_article 9,229) quedan embebidas y verificadas, pendiente solo actualizar `write_to_lancedb.py` para las 3 fuentes nuevas y refrescar la tabla de news_article con el corpus completo de 13 ciclos (estaba escrita solo con el slice de 5 ciclos de la noche anterior)
- Se establecio y uso durante la corrida larga un mecanismo de vigilancia dedicado (`watch_batches.sh`, luego `watch_batches2.sh`) que detecta lineas de progreso nuevas en el log y alerta si pasan 3 minutos sin avance -- util porque el script principal solo imprime cada 5 batches, insuficiente granularidad para reportar en vivo sin el watchdog
- **Diseñadas y desplegadas las 9 Lambdas de Fase 2 (chunking + embedding), conectando la cadena de produccion por primera vez.** Alcance acordado explicitamente con el usuario antes de codear: solo 4 fuentes (`news_paragraph` NO existe todavia, LanceDB/Fase 3 fuera de alcance, Docker se deja para cuando se construya Fase 3). Arquitectura: `embed_orchestrator` (invocado por `send_digest` al terminar) hace fan-out en PARALELO a 4 Lambdas de chunking (`chunk_registry`/`chunk_comments`/`chunk_digest`/`chunk_news_article`) -- sin riesgo real porque no comparten recurso limitado -- y arranca una cadena SECUENCIAL de 4 Lambdas de embedding (`embed_digest`->`embed_comments`->`embed_registry`->`embed_news_article`), deliberadamente NO paralela porque las 4 comparten el mismo techo de TPM de Cohere y correrlas en paralelo recrearia la condicion de carrera real que ya causo el incidente de doble-disparo de News
- Migrados a las Lambdas los mecanismos ya probados la noche anterior en los scripts one-off: token-aware batching, rate governor de ventana deslizante, `MODEL_ID = global.cohere.embed-v4:0` (la ruta que sobrevivio los dos outages de cuota diaria), retry en 5xx/429, checkpointing cada 500 chunks, resume por `chunk_id` real
- **Corregido en vivo un error de diseño propio (dos veces) en `chunk_registry` antes de llegar a la version correcta.** Primer intento: ventana de -12h para inferir "el ciclo anterior" -- el usuario señalo que la cadena de Fase 1 es estricta (si este ciclo corrio, el anterior corrio, sin excepcion), asi que no hace falta calcular nada. Segundo intento (tambien corregido por el usuario): seguia enfocado en "que paso desde el ciclo anterior" cuando la pregunta real es "que es nuevo ahora mismo" -- version final: `first_seen > cycle_started_at`, sin ventana, sin tabla nueva, sin campo nuevo en el registry. El fix cambio el resultado real de 61 a 48 markets nuevos del ciclo 13 -- la logica vieja habria re-chunkeado (no re-embebido, gracias al filtro por chunk_id, pero si trabajo duplicado) markets que ya pertenecian al ciclo anterior
- **Encontrada y corregida deuda tecnica real de formato antes de que se acumulara** (a peticion explicita del usuario, "quiero que todo este unificado"): el script one-off calificaba `chunk_id` de Comments con `@c{numero_de_ciclo}` mientras la Lambda nueva usaba `@{cycle_started_at}` -- dos formatos incompatibles para el mismo proposito. Unificado al formato de la Lambda (timestamp canon, no numero de posicion local). Corregido `scripts/bootstrap_chunk_corpus.py` con una funcion nueva (`cycle_started_at_from_key`), chunks de Comments regenerados, los 794 vectores viejos (formato mixto) borrados y re-embebidos desde cero -- verificado 749/749 sin duplicados, 0 remanentes del formato viejo
- Terraform: nuevo rol `poly-rag-embed-lambda-role` (permisos scoped -- lectura del registry, `bedrock:InvokeModel` solo Cohere Embed v4, `lambda:InvokeFunction` solo a los 8 targets exactos de la cadena), 9 Lambdas nuevas en `lambdas.tf`, `send_digest` modificado para invocar al orquestador al terminar. `terraform apply` bloqueado por el clasificador de auto mode del entorno (no permite ejecutar cambios de infraestructura real) -- el usuario lo corrio directo en su terminal fuera del modo automatico; verificado post-apply sin drift (`terraform plan` = "No changes")
- **Cereza del pastel, a peticion del usuario:** segundo correo por ciclo, separado del digest de mercado de `send_digest`, con metricas de costo/latencia/tokens del ciclo COMPLETO (Fase 1 + Fase 2 juntas, no solo Fase 2). Nueva tabla `poly-rag-embedding-metrics` (ya prevista en tech_debt desde el 20 de agosto, nunca construida hasta hoy) -- una fila por request de Bedrock durante embedding, con `source`/`embedding_model`/`tokens_in`/`latency_ms`/`estimated_cost_usd` (pricing real $0.12/M tokens, ya confirmado en el proyecto). `embed_news_article`, como ultimo eslabon del ciclo completo, lee ambas tablas de metricas (Scan filtrado por `cycle_started_at` en ambas) y manda el correo via SES. Mock generado y revisado con el usuario antes del apply -- un bug real encontrado en el proceso (datos de prueba sin `estimated_cost_usd` se leian silenciosamente como $0, corregido en el mock, no era un bug de la funcion real)
- Confirmado con el usuario: los dos correos llegan en momentos DISTINTOS del ciclo (el de `send_digest` al terminar Fase 1, el de metricas al terminar Fase 2 completa, notablemente despues) -- intencional, permite medir tiempo real de Fase 2 por la diferencia de timestamps entre ambos correos en el proximo ciclo automatico

## 2026-08-20

- Retomado el diseno de Dia 4 con una pregunta de fondo del usuario sobre por que odds/registry NO se convierten a lenguaje natural antes de embeder (a diferencia de otro proyecto suyo) -- aclarado que la conversion tabular->texto solo aplica cuando la busqueda misma tiene que ser semantica (sin llave de filtro conocida de antemano); odds ya tiene esa llave (`market_id`), asi que vive en filtro deterministico, no semantico
- Detectado un hueco real en el diseno: nunca se discutio como resolver preguntas de texto libre (ej. "dame markets de Bitcoin") sin conocer el `market_id` de antemano. Cerrado agregando una nueva capa semantica sobre el registry: `question`/`description` (los unicos 2 campos de texto libre del registry) se embeben, 1 vector por `market_id`, sin chunking, trigger por evento (market nuevo) no por ciclo -- documentado en architecture_canon.md como la mitad semantica de la fuente Polymarket, no una fuente aparte
- Discusion tecnica real sobre por que el filtro estructurado de odds escala a volumenes grandes (particionado/indexado, mismo principio que Delta Lake) solo si el filtro es selectivo -- llevo a una deuda nueva en tech_debt.md: el agente de sintesis de Dia 5 necesita un guardrail contra queries sin filtro selectivo (equivalente a SELECT * sin WHERE), scoped a Dia 5, sin diseñar todavia
- El usuario reencuadro el proposito real de este proyecto (sucesor de Pienza en el portfolio): el usuario final NO es solo el trader, es el technical recruiter que va a interactuar con el RAG directamente -- el proceso/comparacion de diseño ES el producto, no solo una respuesta optimizada. Esto cambio el diseño de Dia 6 de "backlog diferido de A/B tests" a "requisito de dia 1 para bloques F/G/H": comparaciones reales, visibles, toggleables desde frontend
- Verificado con AWS CLI real (solo lectura, sin invocar ningun modelo) que Cohere Embed v3/v4 SI estan disponibles en Bedrock us-east-1 y autorizados en la cuenta -- pricing real confirmado: Titan v2 sin friccion (autorizado, sin agreement), Cohere v3 $0.10/M tokens, Cohere v4 $0.12/M tokens. Decision: Titan v2 como default de produccion, Cohere v4 (no v3) + Voyage AI como A/B de Dia 6. Voyage fue inicialmente descartado por "romper el patron IAM-only" -- el usuario correctamente señalo que esa regla nunca fue una decision real del proyecto, solo un patron que yo inferirir y repeti como si lo fuera; re-agregado a la comparacion
- Matriz de Dia 6 clarificada tras confusion real en el chat: el eje de chunking (parrafo vs articulo completo) SOLO aplica a News -- Comments/Digest/registry solo tienen 1 metodo de chunking cada uno, viajan sin cambiar dentro de ambas variantes del corpus. Resultado: 2 (chunking) x 3 (modelo embedding: Titan/Cohere/Voyage) = 6 indices de corpus a construir, con Capa 0 como flag de consulta aplicado encima de cualquiera de los 6, no un eje de construccion
- Cerrado el diseño de la quinta fuente de embedding: el `_digest` completo (no solo executive_summary) se convierte a texto narrativo via template deterministico (mismo formato que el mock en texto plano ya mostrado al usuario, cero llamada LLM nueva) y se embede como 1 chunk completo por ciclo -- reemplaza un diseño anterior de lookup estructurado directo, por decision explicita del usuario de que Capa 0 sea una fuente de retrieval real, buscable por lenguaje natural
- Requisito nuevo agregado: costo/latencia/tokens deben trackearse POR FUENTE (`registry`/`news_paragraph`/`news_article`/`comments`/`digest`) x modelo de embedding, no solo agregado por indice -- tabla nueva `poly-rag-embedding-metrics`, separada de `poly-rag-architecture-metrics` para no mezclar shapes de fila distintos
- Vector store: evaluados Pinecone, Qdrant, LanceDB, y Databricks Vector Search. Databricks descartado (verificado via informacion del usuario: Free Edition SI soporta Vector Search pero con limite duro de 1 endpoint activo por cuenta, incompatible con necesitar 6 indices consultables en paralelo). Decision inicial de construir solo 1 store fue REVERTIDA cuando el usuario señalo que la pregunta "importa realmente la eleccion de store" es en si misma falsificable y digna de medirse, no asumirse -- decision final: los 3 (Pinecone/Qdrant/LanceDB) se construyen, pero SECUENCIADOS no en paralelo desde el dia 1 (arrancar con Pinecone por ser el mas reconocido/simple de configurar, clonar el corpus ya vectorizado hacia los otros 2 en Dia 6 sin re-embeder nada -- asimetria real con el eje de chunking, que si requiere re-correr el pipeline completo por variante)
- Bloqueantes reales identificados para empezar a codear bloque F, aun sin resolver al cierre de este bloque: donde vive el codigo de Fase 2 (Lambda nueva vs. dentro de send_digest), diseño de almacenamiento/inyeccion de los secrets nuevos que Pinecone y Voyage introducen (primer secret vivo del proyecto), mecanismo de deteccion de "que es nuevo este ciclo" por fuente dentro de una sola invocacion de Fase 2, y si question/description del registry van en un embedding combinado o separado
- Corregidas 18 instancias de fecha incorrecta (2026-08-21 en vez de 2026-08-20) escritas por error en tech_debt.md/architecture_canon.md durante este bloque -- verificado con `TZ=America/Mexico_City date` antes de cerrar
- Resueltos los bloqueantes reales para empezar a codear bloque F: (1) `question`+`description` combinados en un solo embedding por market (no separados) -- el titulo corto domina la señal semantica, `description` aporta contexto sin ser vector aparte; (2) diseño de 4 niveles de Lambdas para Fase 2 -- 1 orquestador (`poly-rag-embed-orchestrator`, invocado por `send_digest`) -> 5 Lambdas de chunking en paralelo por fuente (`chunk_registry`, `chunk_news_paragraph`, `chunk_news_article`, `chunk_comments`, `chunk_digest`, explicitamente rechazando 1 Lambda por cada una de las 6 combinaciones por repetir I/O sin ganar aislamiento) -> merge + 6 Lambdas de embedding SOLO texto->vector, store-agnostic, persisten a S3 (`embed_titan`, `embed_cohere`, `embed_voyage`) -> Lambdas de escritura a store separadas y desacopladas del calculo (`write_to_pinecone` hoy, `write_to_qdrant`/`write_to_lancedb` en Dia 6 sin tocar las de embedding); (3) confirmado que la deteccion de "que es nuevo este ciclo" no necesita mecanismo nuevo -- hereda el mismo patron de `cycle_started_at` que ya usa Fase 1, cada archivo de ciclo en S3 ya es solo lo nuevo por construccion
- Cuentas creadas y keys guardadas en `.secrets` (nunca compartidas en el chat) para los 2 servicios externos nuevos del proyecto: Pinecone (Starter plan, `us-east-1` para consistencia de region/latencia con el resto del stack) y Qdrant (cluster free en N. Virginia, no Ireland, mismo razonamiento de latencia). Voyage AI confirmado como compañia independiente adquirida por MongoDB (no parte de Anthropic pese a ser su partner recomendado) pero con API standalone real, cuenta y key ya creadas tambien
- Verificados limites reales de Pinecone Starter contra la pagina de pricing real: maximo 5 indices (choca con los 6 indices de corpus ya decididos -- mitigacion probable, namespaces en vez de indices separados, pendiente de implementar cuando se codee `write_to_pinecone`) y Write Units hasta 2M/mes (formula real confirmada: 1 WU por KB de payload, no por vector/token -- estimacion con los numeros reales del corpus da ~530K WU para el bootstrap completo de las 6 variantes, ~4x de holgura, pendiente de validar empiricamente con un lote de prueba antes del bootstrap real)
- Aclarado que la solucion al limite de 5 indices/6 variantes es especifica de cada store, no un patron universal: Pinecone usa namespaces, Qdrant usa collections (limite sin verificar todavia), LanceDB no tiene limite de cuenta al ser libreria embebida sobre S3 -- cada uno se resuelve cuando se codee su propia Lambda de escritura, no se asume que el patron de Pinecone transfiere igual
- Rate limits de Voyage AI verificados con la doc real (tabla de Usage Tiers) -- Tier 1 (con tarjeta agregada) da limites en miles de RPM/millones de TPM, no es un problema real para el volumen de bootstrap de este proyecto; el pendiente real que sigue vivo es un alert de gasto para cuando se agote el free tier de tokens (50M en `voyage-finance-2`), explicitamente diferido a cuando se codee `embed_voyage`
- Desacoplado el paso de escritura a vector store del calculo de embeddings, a peticion explicita del usuario: las 6 Lambdas de embedding (`embed_titan`/`embed_cohere`/`embed_voyage` x 2 corpus) quedan store-agnostic, persisten vectores a S3 (`vectors/<variante>/<modelo>.json`); un 4o nivel de Lambdas separadas (`write_to_pinecone` hoy, `write_to_qdrant`/`write_to_lancedb` en Dia 6) hace la escritura real -- agregar un store nuevo no toca las Lambdas de embedding ya probadas, y si el store falla los vectores ya calculados (el trabajo caro) sobreviven en S3 para reintentar sin re-embeder. Arquitectura de Fase 2 actualizada a 4 niveles en architecture_canon.md
- Cerrado el ultimo pendiente de diseño de registry: `question`+`description` van combinados en un solo embedding (no separados), confirmado con el usuario
- Cuenta de Qdrant creada (cluster free en N. Virginia, no Ireland, mismo razonamiento de latencia que Pinecone), key en `.secrets`. Aclarado que la feature de "Inference" de Qdrant (modelos propios de embedding tipo MiniLM/E5) es un servicio DISTINTO que el proyecto no usa -- Qdrant/Pinecone/LanceDB solo almacenan vectores ya calculados por Titan/Cohere/Voyage, nunca generan el vector ellos mismos
- Confirmado por diseño (no ejecucion) que el mecanismo de "vecino" de News nunca puede cruzar entre articulos distintos: la busqueda de vecino siempre filtra primero por `article_id` (paragraph_index +/-1 DENTRO del mismo articulo), es un filtro de metadata obligatorio, no un delimitador de texto -- correcto siempre que el codigo de retrieval (bloque G, aun sin escribir) respete ese filtro
- Escrito y validado `scripts/bootstrap_chunk_corpus.py` (paso 1 del bootstrap de Fase 2 -- chunking de las 4 fuentes, SIN llamar a ningun modelo de embedding todavia, autorizacion pedida y dada antes de crear el archivo). Corrido en dry-run repetidas veces contra el corpus real (913 markets, 10 ciclos completos) hasta quedar limpio, con DOS bugs reales encontrados y corregidos ANTES de escribir nada: (1) `split_paragraphs` asumia separador `\n\n` cuando el real es `\n` simple (confirmado que `\n\n` colapsaba cada articulo a 1 solo "parrafo", haciendo news_paragraph identico a news_article) -- corregido con split por `\n` + fusion de lineas cortas (<200 chars, ruido de titulos/fechas/bylines, 50% de las lineas reales) hacia su vecino, mas un tope duro (2000 chars, force-split por palabra) para el caso de un articulo mal extraido sin saltos de linea reales (encontrado un caso real de 36,971 chars en 1 sola "linea"); (2) `comment_entity_id` NO vive en el payload del comentario (solo en el registry, por market_id) -- la funcion original asumia que si, misclasificando 67% de los comentarios en un bucket `unknown_entity`; corregido con un lookup real al registry (`get_comment_entity_map`)
- Investigando el bug de `comment_entity_id`, se encontraron 3,080 comentarios huerfanos reales (no 1,337 como parecia en la primera pasada -- el primer chequeo solo cubria comentarios `shared_event`/`shared_series`, faltaba verificar que `direct` tambien puede ser huerfano) en 4 ciclos de mediados de agosto (16-17), referenciando 60 `market_id` (22 direct + 38 shared) purgados por la limpieza de registry del 17 de agosto -- limpieza que por diseño nunca toco `comments/*.json`. A diferencia de `unknown_market` en News (dejado como esta, decision previa del usuario), aqui el usuario decidio purgar de inmediato -- creado `scripts/purge_orphan_comments.py` (primer y unico script de `scripts/` que borra datos, no solo agrega, documentado explicitamente como excepcion en scripts/README.md), dry-run verificado, `--apply` corrido: 3,080 comentarios huerfanos eliminados de los 4 archivos afectados, 8,858 legitimos preservados intactos, `comment_count` recalculado por archivo, verificado 0 huerfanos en TODO el corpus de Comments (los 10 ciclos, no solo los 4 tocados). No se toco `poly-rag-processed-comments` (dedup table) a proposito -- un comment_id purgado debe seguir dedupeado si reapareciera, nunca resucitar
- Estado final del corpus de chunks (dry-run, aun sin `--apply` de escritura a S3): registry 913, news_paragraph 109,511, news_article 6,695, comments 629 (bajo de 734 tras la purga), digest 10. `--apply` del chunking bootstrap explicitamente diferido a la proxima sesion (orden acordado: end -> commit -> compact -> start -> apply)
- `scripts/bootstrap_chunk_corpus.py --apply` corrido en la sesion siguiente, tras `/start` fresco: mismos conteos que el dry-run (registry 913, news_paragraph 109,511, news_article 6,695, comments 629, digest 10), 13.8s de ejecucion, 5 archivos escritos a `chunks/<fuente>/bootstrap.json` en S3, verificado post-escritura con `aws s3 ls --recursive` (news_paragraph el mas pesado, 85.8 MiB). Paso 1 (chunking) del bootstrap de Fase 2 cerrado -- pasos 2 (embedding via Titan/Cohere/Voyage) y 3 (escritura a Pinecone) siguen sin correr
- Re-corrido `bootstrap_chunk_corpus.py --apply` tras el ciclo automatico de las 00:00 UTC del 21 (verificado sano: 4/4 etapas, `cycle_complete: true`, 851 queried / 850 processed) para incluir ese ciclo nuevo en los chunks antes de embeder: registry 981, news_paragraph 120,568, news_article 7,429, comments 681, digest 11
- **Paso 2 (embedding) intentado repetidamente sin exito -- el bloque se cierra SIN vectores finales en S3.** Escrito `scripts/bootstrap_embed_corpus.py` (autorizacion pedida y dada) con checkpointing incremental a `vectors/_checkpoints/<variante>/<modelo>/part_NNNNN.json` cada 2,000 chunks y resume real (salta partes ya escritas), diseñado precisamente porque las dos primeras corridas murieron sin escribir NADA
- **Titan v2 eliminado del proyecto por completo** (no diferido a Dia 6): su API de Bedrock no tiene batch (1 texto = 1 request, verificado via `get-foundation-model`, `inferenceTypesSupported=[ON_DEMAND]` sin BATCH, asi que Batch Inference asincrono tampoco aplica) y la cuota real de cuenta es 600 req/min (verificado via `aws service-quotas`), saturada de inmediato -- proyectaba ~3.5h solo para el corpus paragraph. Ese techo es permanente, seria el cuello de botella de CADA ciclo incremental futuro, no solo del bootstrap. Documentado en tech_debt.md y architecture_canon.md
- **Voyage completo su corpus (122,241 vectores en 62 checkpoints, ~30 min, sin throttling) pero se perdio por un error mio:** el usuario dijo "matar Voyage" como decision de direccion (dejar de usarlo como modelo activo) y yo lo interprete como borrar los datos ya generados, ejecutando `aws s3 rm --recursive` sobre sus checkpoints ANTES de tener el reemplazo funcionando -- exactamente lo contrario de lo que el usuario queria (secuenciar: validar el nuevo, despues retirar el viejo). Error de ejecucion, no ambiguedad de la instruccion. ~28.2M tokens reales pagados, perdidos
- Consumo real de Voyage medido en su dashboard (no estimado): **28,263,931 tokens = 56.5% del free tier de 50M**, con solo 1 de las 2 variantes de corpus. Proyeccion: el free tier se agotaria en ~7 ciclos (~4 dias) al ritmo de ingesta actual -- confirma que Voyage no era sostenible como modelo unico sin una alerta de consumo, que sigue sin implementarse (deuda ya abierta en tech_debt.md)
- **Cohere v4 tampoco resulto viable hoy.** Primer `--apply` murio a los 39s con `ThrottlingException` ("Too many tokens"); agregado backoff exponencial+jitter (`COHERE_MAX_RETRIES=8`), y luego un modelo de **pacing fijo** (`COHERE_PACE_SECONDS=10`, sleep antes de cada request en vez de reaccionar al throttle). Ninguno funciono: CloudWatch mostro **~15-20 throttles/min sostenidos contra 1-2 invocaciones exitosas/min** en ambos esquemas. Se escribio 1 solo checkpoint (2,000 vectores) en ~50 min de intentos
- Causa raiz probable identificada al final del bloque (el usuario pregunto por un limite diario que yo no habia revisado): **`Model invocation max tokens per day for Cohere Embed V4` = 8,100,000 tokens/dia**, y ya llevabamos **5,261,503 consumidos hoy (65%)** -- el throttling agresivo no era solo por el limite de 150K tokens/min, sino por acercarse al techo DIARIO. Sin verificar del todo, es la hipotesis viva para retomar mañana
- Hallazgo lateral real: correr los modelos en PARALELO dentro del mismo proceso empeoraba todo (un solo cliente boto3 global compartido entre ~44 threads saturaba el connection pool de urllib3, default ~10 conexiones) -- correr un modelo a la vez, secuencial y aislado, es el patron correcto
- Creado hook nuevo `.claude/hooks/block_ask_user_question.sh` (PreToolUse sobre AskUserQuestion) a peticion explicita del usuario -- **bloqueo total, sin escape hatch**, a diferencia de los hooks de Lambda/Agent/archivos nuevos que si tienen prefijo de autorizacion. Regla espejo agregada a CLAUDE.md, `hooks.md` regenerado
- **Estado real al cierre:** `chunks/` completo y actualizado en S3 (5 fuentes, incluye el ciclo del 21). `vectors/` tiene UN solo checkpoint parcial (`news_article_variant/cohere/part_00000.json`, 2,000 de 9,102 vectores), cero archivos finales. Ningun modelo de embedding ha completado ningun corpus. Pendiente para mañana: verificar la hipotesis del limite diario de Cohere (se resetea a las 00:00 UTC), y decidir si se retoma Cohere, se revive Voyage, o se evalua una alternativa (el usuario planteo Vertex AI de GCP -- ~$3 el bootstrap completo, ~$15.88/mes en produccion, lo cual rompe el budget de $5/mes, sin decidir todavia)

## 2026-08-19

- Verificados 8 ciclos completos (00:00/12:00 UTC del 16 al 19), incluyendo los 2 primeros corridos automaticamente tras el trabajo de F-lambdas de ayer -- ambos con `cycle_complete: true`, markets_queried == markets_processed, sin fallos. Confirmado con evidencia (no solo listado de S3, que puede mentir por LastModified) que no quedaba NINGUN rastro del incidente de las 21:00 UTC del 18: los unicos snapshots con timestamp en esa franja horaria resultaron ser `clob_backfill` legitimos del ciclo de las 00:00 de hoy, no residuo -- casi se reportan como falso positivo por filtrar con un prefijo de hora demasiado amplio (leccion: usar rangos explicitos, no prefijos)
- Auditoria independiente de los bloques A-F-lambdas via subagente sin contexto (handoff escrito en `evals/`, deliberadamente sin darle mis numeros medidos para que no anclara su juicio). El auditor encontro 8 fallas, mezcla de reales y sobre-reportadas -- el usuario cuestiono correctamente el hallazgo mas grave ("first_seen corrupto en 104 markets") y se verifico que estaba exagerado: el campo es correcto, solo describe el periodo de tracking actual tras la purga del 17 de agosto, no un bug. Una segunda auditoria independiente (otro chat, mismo handoff) convergio en los MISMOS dos hallazgos reales que la primera: el decrement despues del check de error, y el backfill nativo sin merge -- señal fuerte de que esos dos si eran genuinos
- Directorio `evals/` borrado a peticion del usuario tras el ejercicio -- no se queria conservar como artefacto permanente del repo
- Regla nueva agregada (CLAUDE.md) + hook mecanico (`block_new_repo_files.sh`, PreToolUse sobre Write): crear cualquier archivo nuevo en el repo requiere autorizacion explicita del usuario primero. Origen: el usuario perdio el hilo de su propio repo por archivos creados sin preguntar
- Renombrado `.claude/scripts/` -> `.claude/hooks/` para separar handlers de hooks de `scripts/` (herramientas one-off del proyecto en la raiz). Bug real encontrado en el camino: `update_hooks_docs.sh` solo soportaba el shape `matchers` (array) y crasheaba con `matcher` (string, el que usan los hooks PreToolUse nuevos), truncando `hooks.md` silenciosamente al correr -- detectado antes de dejarlo asi, restaurado desde git, corregido para soportar ambos shapes, probado en copia antes de aplicar
- `/start` actualizado: `session_ledger.md` y `tech_debt.md` crecen en direcciones opuestas (ledger mas reciente arriba, tech_debt es append-only asi que mas reciente al final) y ya son demasiado largos para leer completos de tajo -- ahora se leen solo los primeros 3 bloques del ledger y las ultimas 6 entradas de tech_debt, mas un grep de marcadores de prioridad explicitos (`PRIORIDAD`/`CRITICAL`) para que un item viejo pero critico no se pierda por posicion
- Implementados y desplegados los 3 fixes reales que sobrevivieron la doble auditoria, todos en el mismo deploy: (1) lock atomico en DynamoDB (`poly-rag-cycle-chain-locks`, tabla nueva) para el bug de doble-disparo del fan-out de News -- mismo patron de `ConditionExpression` ya usado en el proyecto, ningun concepto nuevo; (2) `decrement_post_resolution_counter` movido antes del check de error en `ingest_news`, para que un fallo transitorio de busqueda consuma la ventana post-resolucion en vez de extenderla; (3) `backfill_odds_history_for_new_market` en `ingest_polymarket` ahora mergea con el archivo `odds/` existente en vez de sobreescribirlo, mismo patron que `merge_snapshots` del script one-off -- evita destruir historia de ciclo si un market purgado regresa al registry
- Deploy verificado en dos pasos con plan de Terraform confirmado en ambos (2/3/1 y luego 0/1/0 -- el segundo paso corrigio una variable de entorno que se me habia olvidado declarar explicitamente para la tabla nueva, funcionaba por el default hardcoded pero rompia la convencion del proyecto). Ninguna Lambda invocada para verificar -- el ciclo automatico de las 00:00 UTC es la primera prueba real
- Aclarado con el usuario que el bug de doble-disparo NO estaba en ninguna Lambda hardcodeada, sino en un script one-off (`start_legacy_post_resolution_windows.py`) que arranca manualmente el contador de los 93 markets legacy -- la Lambda nunca lo invoca, solo lee el campo que el script escribio. Verificado el estado real: contador en 2/4 (dos ciclos automaticos ya lo decrementaron desde que se re-armo tras la limpieza del incidente), faltan 2 mas para llegar a 0 por el mecanismo normal
- Decision del usuario: el script `start_legacy_post_resolution_windows.py` NO se borra, ni siquiera cuando su contador llegue a 0 -- se conserva como registro de que 93 market_ids se atraparon manualmente y por que, revirtiendo el plan original de borrarlo una vez consumido. Documentado en tech_debt.md
- Creado `.claude/claude_docs/runbook_verify_cycle_health.md` (autorizacion explicita pedida primero, per la regla nueva) -- complementario al runbook de remediacion de incidentes: ese prueba AUSENCIA (nada de un ciclo espurio), este prueba PRESENCIA Y CORRECCION (un ciclo legitimo escribio lo que debia). Incluye verificacion especifica del lock nuevo (debe reclamarse exactamente 1 vez por ciclo)
- Creado `scripts/README.md` (autorizacion pedida primero) -- indice de los 6 scripts one-off con que hacen, cuando corrieron, y su estado actual (terminados / pendientes de repetirse porque el tagging de News aun no esta conectado a la Lambda / conservados a proposito)
- Conversacion filosofica real sobre exponer el corpus de Polymarket (RAG1) como servidor MCP para consultarlo directo en este chat, en paralelo (no en lugar de) construir el agente de sintesis del Dia 5 -- aclarado tras una lectura incorrecta inicial de la pregunta del usuario como una disyuntiva que nunca planteo. Utilidad real identificada: serviria como herramienta de exploracion/debug del retrieval MIENTRAS se construyen los bloques E-H de Dia 4, y su logica de tools coincidiria en buena parte con el bloque G (funcion de retrieval) que de todos modos hace falta construir
- Al armar el checklist detallado de Dia 4 (chunking en adelante) se descubrio que `temporal_tier`/`market_status_at_publish` nunca se conectaron a `ingest_news` como estaba planeado -- solo existian en los 3,315 articulos etiquetados retroactivamente el 18 de agosto. Medido con precision: los 2 ciclos del 19 (00:00 y 12:00) tenian 1,798 articulos sin ninguno de los dos campos, 0/922 y 0/876
- Pregunta directa del usuario -- "si el runbook de salud de ciclo estuviera completo, hubiera detectado esto?" -- llevo a confirmar que NO: el Paso 5 de `runbook_verify_cycle_health.md` decia explicitamente "se salta mientras no se conecte el tagging", asi que un ciclo con el hueco habria pasado el runbook 4/4 verde con una nota gris, no una falla real. Corregido el mismo dia: el paso ahora usa `assert` real sobre el conteo de articulos sin cada campo, no una nota pasiva -- documentado en el propio runbook como leccion (un chequeo de salud no debe tener clausula de "esto no cuenta todavia")
- Retrofill de los 2 ciclos con hueco corrido y aplicado (`tag_news_temporal_tier.py`, `tag_news_market_status.py`, dry-run primero, `already_tagged: 6` confirmando que los 6 ciclos previos se saltaron correctamente). Primera evidencia real de que la captura post-resolucion funciona: 144 articulos `closed` en los 2 ciclos del 19 (0 en todo el corpus antes de F-lambdas)
- Conectado `classify_temporal_tier`/`classify_market_status` nativamente a `lambdas/ingest_news/handler.py`, copiadas verbatim de los scripts one-off tal como sus propios docstrings anticipaban desde que se escribieron. `get_open_markets` cambio de tupla de 3 a diccionario (agrega `created_at`/`first_seen`/`resolution_date`, necesarios para clasificar) -- verificado localmente contra el registry real (767 markets) antes de desplegar, sin invocar nada
- Desplegado via `terraform plan`/`apply -target=aws_lambda_function.ingest_news` (plan 1/1/1, mismo patron de reconstruccion del `null_resource` de dependencias de siempre). Verificado post-deploy descargando el zip real desde `Code.Location` de AWS y comparando `handler.py` contra el repo -- identico byte a byte. Ninguna Lambda invocada -- el proximo ciclo automatico es la primera prueba real

- Arranque del checklist detallado de Dia 4 empezando por chunking (bloque E), separando estrategia por fuente (odds/news/comments) a peticion del usuario. Para odds se establecio que NO se chunkea/embede como texto -- es tabla estructurada que entra al prompt como tabla, no busqueda semantica -- y se identificaron 5 fuentes retrievables candidatas sobre el schema real (serie completa, snapshot puntual, volatilidad/delta, estado sin movimiento, filtro por `source`)
- Discusion de diseño real: el usuario propuso una arquitectura de dos capas para el retrieval -- una "Capa 0" barata que reusa lo que `send_digest` ya calcula cada ciclo (`executive_summary`, `top_volatility`, `world_snapshot`) como primer lugar de busqueda, antes de bajar a las fuentes profundas de odds/news/comments. Aclarado que la Capa 0 tampoco es un indice unico y estatico -- es en si misma una serie de tiempo (2 digests/dia, ~700/año), asi que hereda el mismo problema de ventana temporal que ya se resolvio para odds/news
- El usuario correctamente insistio en no decidir "vale la pena la Capa 0" por intuicion -- exigio diseño aislado/toggleable (funcion separada + flag tipo `USE_DIGEST_LAYER`, mismo patron que `USE_LLM_ENRICHMENT` ya existente) para poder hacer A/B test real despues, con metricas, sin refactor. Decision de fondo diferida al bloque H (evaluacion), consistente con la disciplina de "measure, don't guess" ya establecida en CLAUDE.md
- Auditoria de fidelidad real: comparados 3 digests completos (ciclos del 16 de agosto, 01:00 y 12:00 UTC) contra el JSON crudo de S3, seccion por seccion. Hallazgo principal: los 7 de 8 bloques que son JSON estructurado (New Markets, Resolved, Biggest Moves, World Snapshot, contadores, quotes) son 100% fieles -- nunca pasan por el LLM, cero alucinacion posible. Solo `executive_summary` (1 campo de todo el digest) es texto generado, y en el unico caso revisado fue correcto en cifras con una inferencia causal razonable pero no verificada, marcada apropiadamente con "likely"
- El usuario ya habia detectado por su cuenta, antes de esta sesion, un bug real: `Trader Comments`/`News Highlights` mostraban contenido IDENTICO entre ciclos consecutivos pese a que el pool de comentarios crecio de 2,589 a 2,831. Confirmado en codigo: `extract_quotes` hace `payload.get(...)[:3]` -- las primeras 3 posiciones del array, sin ranking ni criterio, lo cual se repite si el orden de escritura de `ingest_comments` es estable. El usuario aclaro que el bug se autocorrige parcialmente en ciclos posteriores, pero sigue siendo real
- Confirmado en codigo (`lambdas/send_digest/handler.py`) que `world_snapshot.top_conviction`/`most_disputed` YA calculan y guardan `volume24hr` en el JSON, pero el template del email nunca lo lee al renderizar -- solo muestra `question`+`price`. El usuario penso que era un dato faltante; era un dato calculado y descartado silenciosamente antes de llegar al correo
- Ocho mejoras/hallazgos recopilados uno por uno con el usuario (tabs daily/week/month; mas contexto -- odds/bet size -- por market nuevo; odds previos al resolver, no solo el outcome binario; Biggest Moves confirmado sin cambios; bug de repeticion de quotes; top 5 markets nuevos con News linkeada por bet size, reemplazando una idea anterior de "requiere retrieval" al notar que el linkeo 1:1 ya existe hoy; top 10 de New Markets con el resto colapsable; mostrar volumen24hr + explicar criterio de ranking en World Snapshot)
- Construido y publicado un mock HTML del digest rediseñado con los 8 cambios sobre datos falsos pero realistas al dominio (cargada la skill `artifact-design` primero, tratamiento utilitario/memo no editorial porque es un correo real de SES, no una landing page). Decisiones de diseño: paleta fria de "terminal de datos" en vez del cliché warm-cream, tipografia monoespaciada para todas las cifras (alineacion tipo ledger), serif editorial solo en headings. Limitacion tecnica señalada explicitamente en el mock y en el chat: los tabs Daily/Week/Month funcionan en la pagina viva pero un correo real de SES no puede correr JS -- la implementacion real seria email siempre en modo Daily estatico + link "View full trends" a una pagina web hosteada aparte para Week/Month. Dejada explicitamente sin resolver la logica de agregacion semanal/mensual (sumar deltas vs. neto) como pregunta de diseño abierta, no asumida por el mock
- Nuevo hook mecanico (`block_agent_spawn.sh`, PreToolUse sobre `Agent`) + regla espejo en CLAUDE.md: cualquier `Agent()` requiere que el usuario confirme en el chat (tarea, por que subagente, estimado de tokens) antes de que el `description` lleve el prefijo `AUTORIZADO: ` que desbloquea el hook -- doble seguro explicito incluso si la idea de spawnear fue del propio usuario. Origen: datos reales de uso de 24h que el usuario trajo (96% del consumo de sesiones subagent-heavy, 92% con >150k de contexto)
- Implementados en codigo los 4 cambios reales del backlog de rediseño del digest (agrupando #2/#5/#6/#7/#8 del backlog original): fix del bug de quotes repetidas (dedup contra el digest del ciclo anterior, leido de S3, en vez de slice posicional fijo), fix del bug de `volume24hr` calculado pero nunca renderizado (ahora visible en Highest-Conviction/Most Disputed con caption del criterio de ranking), New Markets rediseñado (top 10 por `volume24hr` con precio+volumen+News linkeada inline via el linkeo 1:1 ya existente, resto colapsado en `<details>`), Resolved con `pre_resolution_price` junto al outcome binario. `schema_version` subido a v2. Verificado corriendo las funciones puras localmente contra datos reales de S3 (ciclo 2026-08-19 12:00 UTC), sin invocar la Lambda -- confirmado sin overlap real en el dedup de quotes y enrichment correcto en newly_tracked/resolved. Item #1 del backlog (tabs Daily/Week/Month) deliberadamente NO implementado, sigue bloqueado en la pregunta de agregacion semanal/mensual. Deploy real (terraform apply) queda pendiente para el proximo ciclo automatico
- Decision explicita del usuario: `_digest` se declara canon, MVP suficiente para RAG por ahora -- se cierra la ronda de pulido activo, documentado en tech_debt.md. Si aparece un bug real en un digest futuro se reporta ad hoc y se arregla puntual, sin reabrir una ronda de mejoras por iniciativa propia
- Discusion tecnica sobre por que el filtro estructurado de odds (F1-F5, WHERE market_id/rango de fecha) sigue siendo viable a escala de petabytes: particionado/indexado (partition pruning, mismo principio que Delta Lake) hace que el costo de una query escale con el tamaño de la RESPUESTA, no de la tabla completa -- pero solo si el filtro es selectivo. De ahi salio una deuda nueva documentada en tech_debt.md: el agente de sintesis de Dia 5 necesita un guardrail explicito contra queries sin filtro selectivo (equivalente a un SELECT * sin WHERE), scoped a Dia 5, no diseñado todavia
- Chunking (bloque E) cerrado para las 3 fuentes. **News:** unidad de chunk = parrafo (no tamaño fijo ni articulo completo), vecino detectado por `paragraph_index` +/-1 del mismo `article_id` (se usa la `url` del articulo como `article_id`, ya es unica por ser la dedup key existente). Retrieval es parent-child: busqueda semantica a nivel parrafo, pero el contexto devuelto al LLM es el parrafo ganador + sus vecinos inmediatos, no el articulo completo -- costo de tokens acotado sin importar el largo real (mediana ~4000 chars, maximo 240K). Metadata de chunk hereda `market_id`/`temporal_tier`/`market_status_at_publish`/`pubDate`/`source` del articulo padre. **Comments:** demasiado cortos para embeder individualmente (mediana 44 chars) -- unidad de chunk depende de `link_type`: `direct` chunkea por `market_id` (1:1 real), `shared_event`/`shared_series` chunkean por `comment_entity_id` (1 solo stream para TODOS los markets que comparten esa entidad, evitando re-embeder el mismo texto hasta 49 veces como podria pasar con una serie grande). El indice `market_id -> comment_entity_id` reusa el campo que `ingest_polymarket` ya escribe en el registry, no requiere tabla nueva. Mismo mecanismo para el bootstrap one-off y el incremental por ciclo, solo cambia el alcance de datos que entra
- Descubierto en la discusion de Comments que el diseño anterior de embedding tenia un error de acoplamiento: la nota vieja de architecture_canon.md decia que el embedding debia vivir "dentro de la cadena de ingestion" -- corregido a dos fases explicitas, Fase 1 (ingesta, sin cambios) y Fase 2 (embedding, proceso independiente que solo lee S3). Trigger de Fase 2 decidido: encadenado como ultimo eslabon de la cadena existente (`send_digest` invoca Fase 2 al terminar, mismo `lambda.invoke()`/`cycle_started_at` heredado que ya usa cada eslabon hoy) -- desacoplado en codigo/responsabilidad, no en tiempo, sin scheduler ni delay artificial
- **PENDIENTE EXPLICITO PARA MAÑANA:** decidir el modelo de embeddings (bloque F). Cuatro candidatos mapeados y documentados en tech_debt.md ("Embedding Model Choice"), ninguno decidido hoy a peticion del usuario: Cohere Embed v3 via Bedrock (mismo IAM/billing que el resto del stack, claim de mejor precision en noticias financieras/politicas + `input_type` search_document/search_query -- SIN VERIFICAR contra doc real de AWS), Titan Embeddings v2 (mas simple/barato, claim de precision mas debil en dominios de nicho como cripto/politica), self-hosted via fastembed/ONNX en Lambda (unico camino self-hosted viable dado el constraint de solo-Lambda del proyecto), Voyage AI (rompe el patron de auth-solo-IAM existente, requeriria justificacion fuerte de calidad). Antes de decidir: verificar los claims de Cohere contra documentacion real, considerar medir con una muestra real del corpus en vez de decidir solo por specs

## 2026-08-18

- Reconciliacion completa del registry contra los 4 ciclos completos, construida en eda_mio_2: encontrado que el saldo corrido (entraron-salieron por ciclo) no cuadraba con el open real (210 vs 237) porque resolved_this_cycle en los payloads contaba resoluciones de markets AJENOS a la ventana de 4 ciclos (del registry viejo ya borrado) -- corregido filtrando salieron contra el set acumulado de ids que ya habian entrado, verificado exacto (saldo final 237 = open real). Extendido el mismo criterio de limpieza del dia anterior a un nivel mas: 15 registry items + 14 archivos odds que habian entrado por invocaciones de debugging fuera de los 4 ciclos (no por ellos) fueron identificados con evidencia exacta (market_id real de newly_tracked_markets, no aproximacion por hora -- un primer intento por prefijo de hora fallo por colision con invocaciones de debug en la misma hora que un ciclo real) y borrados. Registry final: 285 items, EXACTO igual a los market_ids que entraron via los 4 ciclos, 0 rastro de cualquier otra invocacion
- Agregado apartado nuevo "Polymarket -- conceptos de dominio" a knowledge.md (separado de la seccion tecnica de AI/RAG/NLP existente) -- primera entrada: mecanismo peer-to-peer de Polymarket (complete sets YES/NO, precio como probabilidad implicita, liquidacion via colateral, volume24hr vs volumeNum)
- poly_rag_exploration borrado (notebook original de Dia 3, ya contaminado con muestra parcial de un registry sucio) junto con sus tablas Delta viejas -- reemplazado por eda_mio_3, reconstruido desde cero sobre el registry ya limpio (285 items) y con pull COMPLETO de odds para los 237 markets abiertos (no una muestra de 25 como el notebook viejo, ya no hacia falta dado el tamano del dataset limpio)
- Bug real de duplicados encontrado en eda_mio_3 por el usuario: correr el notebook mas de una vez duplico odds_snapshots de 371 filas reales a 742 (append puro sin dedup, re-agregaba TODO lo leido de S3 cada corrida). Deduplicado en la tabla real (rebuild via ROW_NUMBER, con un intento fallido en el camino por dejar una columna auxiliar `rn` filtrada al schema, corregido). Fix aplicado al notebook: chequeo anti-join contra lo ya existente antes de escribir, con ALERTA visible del conteo de duplicados excluidos (decision explicita del usuario: filtrar si, pero nunca en silencio)
- Time travel real demostrado por primera vez en el proyecto: eda_mio_3 ya tenia 3 versiones genuinas de market_registry (por correrse varias veces) -- agregada Parte 4 que consulta VERSION AS OF version-vieja vs version-nueva y hace un diff real de market_ids entre ambas, cerrando el ultimo pendiente tecnico del Dia 3
- Dia 3 del sprint cerrado formalmente en gerdau/sprint_plan.md -- los 5 bloques completos incluyendo DAMA-DMBOK/Data Mesh (leido por el usuario fuera de esta sesion), con nota explicita de que el trabajo colateral del dia (el bug de diseno de ingest_polymarket y la limpieza mayor de AWS) no estaba en el plan original, surgio de auditar datos reales para estos mismos notebooks

- Arranque de Dia 4 (retrieval/RAG). Pregunta del usuario sobre CI/CD respondida y aterrizada: movido de Dia 6 bloque 2b (un one-liner opcional de `aws lambda update-function-code` en push, que habria bypasseado Terraform y driftado el state) a Dia 5 bloque 6 como bloque real con sub-lista ordenada por dependencia -- state remoto de Terraform primero (bloqueante: CI no puede aplicar contra un state gitignoreado en el Codespace), luego los primeros tests de Python, luego CI, luego CD via OIDC. Diferido de Dia 4 con razonamiento explicito, no por omision
- Pregunta de seguimiento del usuario ("me estoy equivocando al cambiar Lambdas ya desplegadas") aclarada como un tema DISTINTO de CI/CD: separacion de ambientes (donde aterriza el codigo) no es lo mismo que CI/CD (como se mueve), y uno no da el otro gratis. Un stack `poly-rag-dev-*` paralelo se evaluo y no se adopto -- duplicaria el gasto de Bedrock (~$8.85 -> ~$17.70/mes) y un ambiente dev con datos chicos/sinteticos no habria cachado casi ninguno de los bugs reales del proyecto (el de consistencia eventual de DynamoDB, el timeout de 900s con outlets lentos, el hallazgo de shared_event), que requirieron escala y red reales. Alternativas preferidas anotadas en tech_debt
- Diseño de RAG discutido contra datos REALES medidos en vivo (no contra lo que decian los docs): 2,638 articulos / ~4.15M tokens y 9,778 comentarios / ~200K tokens en 4 ciclos, registry en 515 markets (445 open), y un 5o ciclo (2026-08-18 00:00) que los docs aun no registraban. Tres hallazgos que cambian el diseño: mediana de articulo 4,015 chars (obliga chunking), mediana de comentario 44 chars con 47% bajo 40 (no se deben embeder individualmente), y sobre todo linkeo 100% articulo->exactamente-un-market
- Modelo de dos capas de retrieval (Layer 1 linkeado / Layer 2 ventana temporal ambiental) DEPRECADO por completo, con VoBo explicito del usuario, que identifico el punto el mismo: el rediseño de News a busqueda-por-market vacio el pool ambiental que Layer 2 existia para alcanzar. `retrieval/time_window.py` borrado (no parcheado -- ademas leia el prefijo `bluesky/` ya borrado, devolviendo 0 posts en silencio como si fuera resultado real, y nunca leyo `comments/`). Reemplazado por un solo camino: filtro por metadata (`market_id`, tiempo, source) + ranking semantico. El tiempo NO se deprecia, baja de capa arquitectonica a campo del envelope de metadata
- Al deprecar se preservo explicitamente la limitacion que Layer 2 SI señalaba y que sigue abierta (una noticia ambiental que nunca nombra al market jamas se llega a fetchear), nombrando a la busqueda semantica cross-market como su camino sucesor -- con la frontera honesta de que solo alcanza contenido ya en el corpus por causa de otro market, no temas nunca cubiertos
- Correccion importante aportada por el usuario: el corpus es ACUMULATIVO (~1M tokens/ciclo, 2 ciclos/dia), asi que el embedding tiene que ser incremental y automatizado dentro de la cadena de ingestion, no un backfill manual. Consecuencia de diseño real: la APPENDABILITY pasa a ser el criterio de seleccion del vector store, no la ergonomia -- un blob plano FAISS en S3 implicaria read-modify-write de un archivo creciente cada ciclo, exactamente el patron que ya causo problemas dos veces (archivos de odds, escrituras concurrentes de batches de News). Vector store deliberadamente NO decidido aun: candidatos FAISS-en-S3, ChromaDB (que el usuario ya uso en otro proyecto), Databricks Vector Search; OpenSearch Serverless descartado por costo (~$700/mes)
- README.md, architecture_canon.md y tech_debt.md actualizados en consecuencia (cifras reales del corpus, seccion de retrieval reescrita, entrada de deprecacion completa conservando el razonamiento historico sin borrarlo)

- Confirmados 6 ciclos completos (el ultimo, 12:00 UTC del 18) y re-medido el corpus completo sobre los 6 (no los 4 de la noche anterior): 3,315 articulos (~5.22M tokens) y 12,711 comentarios (~261K tokens), registry en 595 markets (502 open / 93 resolved). Hallazgo nuevo: News y Comments crecen distinto -- News sube sostenido (443->785 articulos/ciclo), Comments se aplano de 3,441 a ~350-400/ciclo una vez que la tabla de dedup alcanzo estado estable (no es una falla, `markets_with_comments`/`entities_queried` se mantuvieron normales)
- Bug real de diseno encontrado auditando la mediana de articulo: solo 23% de los articulos ingeridos son frescos (<=1 dia), mediana de 41 dias, 54% >30 dias, algunos hasta 17.6 anios de antiguedad -- confirmado sistemico en los 5 ciclos medidos, no un ciclo malo. Root cause: Google News RSS rankea por relevancia a la pregunta del market, no por fecha, y `ingest_news` nunca miro `pubDate`. Esto es un problema de RAG, no solo de higiene de datos -- la tesis del proyecto es correlacion CONCURRENTE, y sin distinguir edad, el retrieval citaria con confianza un articulo de hace un anio como explicacion de un movimiento de odds de esta semana
- Usuario decidio: no borrar nada (contexto legitimo del market aunque no haya movido odds), cap de 1 anio mas alla de la fecha de CREACION del market (no de la fecha de ingesta), 3 flags temporales para noticias segun su relacion con el market (antes de crearse / creado pero sin tracking todavia / creado y ya trackeado), y una captura extra 4-8 ciclos DESPUES de que un market resuelva para capturar reaccion post-resolucion
- Pregunta del usuario sobre la flag intermedia (si en verdad no traemos historico de odds, solo snapshots desde que empezamos a trackear) llevo a investigar y confirmar que Polymarket SI expone historia de precio completa por market via `clob.polymarket.com/prices-history`, gratis, sin auth -- hallazgo que cambia el diseño: la flag 2 deja de ser "no podemos correlacionar" y pasa a ser "podemos correlacionar, a resolucion diaria, con datos recuperados"
- Backfill de historia de odds implementado como script one-off (`scripts/backfill_odds_history.py`, deliberadamente FUERA de la cadena de 12h -- historia pre-tracking es inmutable) con dry-run por default. Dos bugs reales encontrados y corregidos ANTES de escribir nada, ninguno de los dos genero error, ambos se veian como hallazgos legitimos sobre los datos: (1) exigir que YES y NO reportaran el mismo timestamp descarto ~75% de la historia real (cada token opera su propio order book, casi nunca coinciden) -- corregido leyendo solo el token YES y derivando NO=1-YES, valido para markets binarios por el mecanismo de complete sets; (2) el endpoint `?id=` de Gamma devuelve lista vacia para markets con `closed:true`, lo cual habria saltado silenciosamente los 93 markets resueltos -- exactamente los arcos completos open-to-resolution que el proyecto reclama como diferenciador -- corregido usando el endpoint `/markets/{id}` que `ingest_polymarket` ya usa
- Antes del backfill se corrio un script separado (`scripts/tag_cycle_snapshots.py`) para taggear los ~1,368 snapshots existentes con `source: "cycle"` explicito -- el usuario rechazo la alternativa de dejarlo implicito (ausencia de campo = cycle-origin) por la misma razon que `link_type` paso de 2 a 3 tiers: una promesa no escrita no se puede verificar. Verificado aditivo puro (solo agrega la key, ningun valor cambia), idempotente, y probado explicitamente contra un caso mixto para confirmar que nunca reetiquetaria un punto `clob_backfill` como `cycle`
- Backfill aplicado en produccion sobre los 595 markets del registry (incluyendo resueltos, decision explicita del usuario -- son los arcos mas valiosos): 487 backfileados, 108 sin pre-historia (nacieron despues del 16 de agosto), 0 errores, 62,273 snapshots agregados. Verificado post-escritura en S3: 63,641 snapshots totales (62,273 clob_backfill + 1,368 cycle, aritmetica exacta), 0 violaciones del cutoff duro (nada escrito en o despues del primer ciclo real), 0 snapshots cycle sin volume, 0 snapshots backfill con volume, orden cronologico y empalme de precio limpio confirmados en un market de muestra (561980: 375 puntos desde 2025-07-10 hasta hoy, el ultimo precio backfill coincide con el primer precio de ciclo)
- Usuario establecio un limite explicito de alcance: recuperar el HISTORIAL DE NOTICIAS de la misma ventana pre-tracking es un proyecto aparte, no una extension de este -- Google News RSS no soporta busqueda por rango de fecha arbitrario. El backfill de odds es "lo mas atras que vamos a ir." Documentado en tech_debt.md para que no se reproponga como si fuera continuacion natural
- architecture_canon.md y tech_debt.md actualizados con el diseno completo del backfill (dos entradas nuevas), los dos bugs, los numeros verificados, y la nueva seccion Odds Time-Series describiendo los dos origenes de snapshot (`cycle`/`clob_backfill`) y como se distinguen
- Aparte, agregada una entrada nueva a tech_debt.md por peticion del usuario: publicar el digest generado por LLM en su perfil de LinkedIn 2-3 veces por semana como personal branding de AI engineer, explicitamente diferido hasta que el proyecto este 100% terminado -- documentadas las preguntas de diseno reales que faltarian (seleccion de que digest publicar dado que no todos los ciclos se publicarian, automatico vs. draft-then-approve dado el riesgo reputacional de postear bajo el nombre real del usuario, y verificar ToS de la API de LinkedIn antes de construir nada, mismo habito que Reddit/Google News/GDELT)

- Bloque B (flags temporales de News 3.1/3.2/3.3) completado. `created_at` real del market (distinto de `first_seen`, que es cuando NOSOTROS empezamos a trackear) backfileado a los 595 items del registry via `scripts/backfill_registry_created_at.py` -- prerequisito descubierto en vivo, no planeado: sin `created_at` no se puede distinguir 3.1 (antes de que el market existiera) de 3.2 (existia pero no lo trackeabamos). Mismo bug de endpoint que en el backfill de odds (`?id=` esconde markets `closed:true`) evitado desde el diseno, usando `/markets/{id}` desde el inicio -- 595/595 sin errores
- `scripts/tag_news_temporal_tier.py` clasifico retroactivamente los 3,315 articulos existentes (no solo hacia adelante, consistente con "no se borra nada" del backfill de odds). Distribucion real: 3.2=1,858 (56%, la mayoria rescatada por el backfill de odds de ayer), 3.1=860 (26%), unknown_market=305 (9%), 3.3=167 (5%), too_old=125 (4%)
- Hallazgo real de datos huerfanos, NO corregido por decision explicita del usuario ("dont delete yet, will handle later perhaps querying through the RAG"): 305 articulos (`unknown_market`) referencian market_ids que la limpieza del registry del 17 de agosto borro -- esa limpieza nunca toco `news/`, solo registry/odds. Confirmado con evidencia que es 100% historico, no una fuga activa: tasa de unknown_market por ciclo fue 85% -> 21% -> 13% -> 12% -> 0% exacto desde el 18 a las 00:00, el dia siguiente a que la limpieza se estabilizara
- Discusion real sobre por que 3.3 (la tier de mayor confianza, respaldada por snapshots completos de ciclo) es solo 5% hoy: la hipotesis inicial del usuario ("markets nuevos siempre caeran en 3.3") fue corregida en conjunto -- el primer pull de Google News para un market nuevo trae mayormente cobertura vieja rankeada por relevancia (ver el hallazgo de stale news de ayer), no noticias del dia. Lo que si es cierto: conforme el market sigue trackeado y corren mas ciclos, los pulls SUBSECUENTES si empiezan a traer noticias genuinamente del mismo dia, que si caen en 3.3 -- 3.3 crece con el TIEMPO DE OPERACION del pipeline, no con el tamano del registry, y es honesto que sea bajo a solo 3 dias de haber arrancado News
- Extension de `ingest_news`/`ingest_polymarket` para que nuevos markets entrantes traigan esto automaticamente (odds history + created_at + clasificacion de tier) deliberadamente diferida como "F" -- un solo deploy con un plan verificado, en vez de tocar las Lambdas varias veces el mismo dia por razones distintas. La logica de clasificacion (`classify_temporal_tier`) quedo escrita standalone en el script para poder copiarse directo a `ingest_news` cuando llegue ese momento -- este repo no tiene un modulo compartido entre Lambdas
- Ambos backfills de este bloque verificados aditivos puros en S3/DynamoDB antes y despues de aplicar (dry-run primero en ambos, sin excepcion) y documentados en tech_debt.md con dos entradas nuevas

- Nomenclatura de la agenda del dia aclarada por el usuario tras perder el hilo: A/B/C ya cerrados (backfill de odds, tiers de News, cap de 1 anio dentro de B), D es lo que sigue (captura post-resolucion), y las extensiones a las Lambdas para markets nuevos se renombran a "F-lambdas" (no "F", para no chocar con la F original del checklist de RAG que es embedding) -- se ejecutan despues de D, no antes
- Bloque D disenado en conjunto: ventana fija de 4 ciclos (48h) post-resolucion, el ciclo que resuelve cuenta como el primer ciclo post. Disparador: `status == closed`, mismo mecanismo de busqueda (Google News con `question` verbatim). Discusion real sobre si hacia falta una 4a tier temporal para "post-resolucion" -- el usuario dudo de su propia decision original (no crear tier nueva) cuando se le planteo el caso, se aclaro en conjunto que SI tenia razon desde el principio: `temporal_tier` y "el market seguia abierto o ya cerro" son dos preguntas independientes, no ameritan mezclarse en un solo campo (mismo error que ya se habia cometido una vez con `link_type` de Comments). Decision final: campo nuevo `market_status_at_publish` (open/closed), sin tier nueva
- `scripts/tag_news_market_status.py` etiqueto retroactivamente los 3,315 articulos existentes, calculando el status a partir de `resolution_date` del registry (no el status ACTUAL del market, que responde una pregunta distinta -- "donde esta hoy" vs. "donde estaba cuando se publico este articulo especifico")
- Hallazgo real confirmado con el corpus completo, no solo logica: 0 de 3,315 articulos quedaron `closed`. Verificado que es el resultado CORRECTO dado el diseno actual: 339 articulos si estan linkeados a markets ya resueltos, pero los 339 fueron publicados ANTES de la resolucion (Google News trae cobertura vieja de cuando el market aun estaba activo) -- ninguno post-resolucion, porque `ingest_news` deja de buscar un market en cuanto deja de estar `open`. Es evidencia en vivo, no solo diseno teorico, de exactamente el hueco que D existe para cerrar
- Implementacion hacia adelante (que `ingest_news` amplie su busqueda a markets recien resueltos dentro de la ventana de 4 ciclos) diferida a F-lambdas, junto con odds history y created_at para markets nuevos -- misma logica de "un solo deploy, un solo plan verificado" que ya se aplico hoy
- Documentado en tech_debt.md con una entrada nueva completa (diseno, el rechazo razonado de la 4a tier, los numeros verificados, y el hallazgo de 0 articulos closed)

- F-lambdas implementado y desplegado: los 3 cambios batched en un solo deploy (`created_at` nativo, odds history retroactivo via CLOB, y captura post-resolucion) sobre `ingest_polymarket`/`ingest_news`. Diseno final del mecanismo post-resolucion aclarado en conjunto con el usuario: no es comparacion de fechas en cada corrida, es un CONTADOR disparado por la transicion open->resolved (arranca en 4 en `mark_registry_resolved`, se decrementa en `ingest_news` con guarda contra negativos) -- coincide exacto con como el usuario lo describio ("checa si estaba open y ahora closed... y empieza el pull de 4 ciclos")
- Excepcion de una sola vez para los 93 markets ya resueltos antes de que el codigo existiera: `scripts/start_legacy_post_resolution_windows.py`, lista de IDs congelada (no query en vivo, deliberado para no reactivar ventanas ya reales), arranca su contador en 4 como si acabaran de resolver -- decision explicita del usuario de aceptar el sesgo minimo (pipeline con solo 3 dias de vida, "muy antiguo" aqui es 2-3 dias maximo). Corrido y verificado: 93/93 con contador en 4
- Todo probado en vivo antes del deploy, sin asumir nada: `fetch_clob_price_history` contra la API real (371 pts, mismo resultado que el script de A), `decrement_post_resolution_counter` contra un item de prueba descartable (2->1->0->0, la guarda contra negativos si funciona), `get_open_markets` contra el registry real (502 open + 93 resolved = 595 exacto)
- Desplegado via `terraform apply -target` sobre ambas Lambdas (plan confirmado antes: 1 to add/2 to change/1 to destroy, el add/destroy siendo el null_resource de dependencias reconstruyendose por el hash del handler, no un recurso inesperado)
- Verificado con DOS invocaciones reales de produccion (no solo CloudWatch): la primera encontro 9 markets genuinamente nuevos, todos con `created_at` real (2025-11-11 a 2026-08-17) y 4 de ellos con historia CLOB ya backfileada automaticamente (677396, creado nov 2025, trajo 255 snapshots sin ningun script manual)
- Bug real encontrado en esa misma verificacion: `append_odds_snapshot` (la funcion que escribe el snapshot de CADA ciclo) nunca se actualizo para escribir `source: "cycle"` -- hubiera reintroducido para TODO snapshot nuevo el mismo problema de ausencia-implicita que el usuario rechazo explicitamente horas antes para los 1,368 snapshots viejos. Corregido, redesplegado con su propio plan verificado (0/1/0), y confirmado con una segunda invocacion real que el snapshot nuevo ya trae `source: cycle` nativo
- Bloque D del dia (Day 4, RAG) cerrado por completo: A, B, C, D y F-lambdas todos implementados y verificados en produccion real, no solo diseñados

- INCIDENTE (causado por Claude, no por el usuario): Claude invoco `ingest_polymarket` manualmente dos veces para verificar un fix, sin pedir confirmacion -- esa Lambda es el punto de entrada de TODA la cadena, asi que disparo ciclos completos hasta send_digest. Amplificado x12.5 por un bug real de doble-disparo: 2 invocaciones -> 4 (AWS reintento por timeout del CLI) -> 80 batches de News -> 26 de Comments -> **25 correos digest reales al usuario**
- Limpieza completa ejecutada y verificada (tabula rasa): borrar archivos de S3 NO era suficiente, la mayoria del dano estaba en DynamoDB y en MUTACIONES de archivos existentes. 9 puntos de contaminacion en total, incluyendo 3 invisibles desde cualquier listado de S3: 1,864 snapshots espurios inyectados en 466 archivos de markets legitimos, 36 markets marcados `resolved` fuera de ciclo (requerian REVERSION a open, no borrado), y las ventanas post-resolucion de los 93 markets legacy consumidas de 4 a 0 en minutos (el trabajo del script one-off de hace unas horas, destruido). Registry restaurado exacto a 595 items (502 open / 93 resolved) con los 93 contadores de vuelta en 4
- Aprendizaje real del usuario, aplicado: un runbook escrito desde la teoria no sirve -- se valida ejecutandolo. La primera version teorica del runbook tenia 4 errores que solo apareceieron al correrlo (subestimo el dedup de URLs por 7x: estimo ~172, real 1,302; no contemplaba los snapshots en archivos existentes; trataba los markets resueltos como "puede aplicar" en vez de un paso obligatorio con reversion; y no contemplaba los contadores consumidos)
- Tres capas de prevencion creadas, deliberadamente independientes entre si: (1) `.claude/claude_docs/runbook_manual_invocation_cleanup.md` -- remediacion validada paso a paso; (2) regla dura nueva en CLAUDE.md prohibiendo invocar cualquier Lambda de produccion sin confirmacion explicita, con las 3 alternativas para verificar sin invocar; (3) `.claude/scripts/block_lambda_invoke.sh` -- hook `PreToolUse` que BLOQUEA mecanicamente el comando antes de ejecutarse (exit 2), con escape hatch que solo el usuario puede activar (`POLYRAG_ALLOW_LAMBDA_INVOKE=1`), 4 tests pasando. La tercera capa es la que responde la peticion real del usuario: que no dependa del juicio de Claude en el momento
- PRIORIDAD 1 acordada para la siguiente sesion, antes de continuar con Dia 4: arreglar el bug de doble-disparo en `merge_batch_payloads` (no tiene guarda contra invocar la siguiente etapa varias veces). Se habia juzgado antes como "solo ocurre con intervencion manual" -- evaluacion incorrecta, porque el watchdog reinvoca offsets automaticamente y produce la misma condicion de carrera sin humano de por medio

## 2026-08-17

- Primera corrida canonica limpia de las 12 UTC verificada de punta a punta: las 4 Lambdas corrieron solas via EventBridge/chaining, cerrado el trial LLM-en-ingestion como canon (ya no experimento reversible -- el LLM es el filtro de calidad mismo, y su output alimenta el futuro RAG). Costo real medido del primer ciclo canonico: $0.147/ciclo (~$8.85/mes), actualizado en architecture_canon.md reemplazando la cifra historica de $1.10/mes pre-rediseno
- Cerrado un gap real de observabilidad: send_digest llamaba a Bedrock (executive summary) pero nunca escribia a poly-rag-architecture-metrics -- agregado write_metrics/estimate_cost_usd, permiso IAM PutItem scoped, verificado con invocacion manual real
- Limpieza mayor de datos obsoletos en dos rondas, siempre con S3 versioning + DynamoDB PITR activados PRIMERO (deliberado, para que el borrado fuera recuperable via restauracion, no irreversible -- decision explicita del usuario, "mas higienico aunque si sea recuperable"): (1) 329 registry items + sus 329 odds/<market_id>.json del pipeline pre-rediseno (Bluesky + News-por-keywords) + test/README.md + prefijo bluesky/ completo; (2) 13 archivos de ciclo (news/polymarket) del diseno viejo de 10-feeds-RSS/3-verticales que habian quedado fuera del alcance de la primera pasada
- Definido "ciclo completo" (las 4 etapas con archivo final bajo la misma fecha/hora, excluyendo batches intermedios de News) en el notebook eda_mio propio del usuario -- solo 4 ciclos completos verificados a la fecha. Extendido el mismo criterio a nivel snapshot individual dentro de odds/: purgados 4 snapshots de ruido (todos del mismo timestamp de un ciclo incompleto ya borrado) y el archivo de un market que quedo en 0 snapshots validos
- Bug real de diseno encontrado por el usuario mientras auditaba los 4 ciclos: un market abierto y trackeado en el registry podia tener huecos silenciosos en su serie de tiempo de odds (ej. market 1365861: presente en ciclo 1, ausente en ciclo 2, de vuelta en 3-4) porque el snapshot de odds dependia de que el market apareciera en el top-500 de ESE ciclo especifico -- coincidencia de dos responsabilidades que deberian estar desacopladas (descubrir candidatos nuevos vs. mantener viva la serie de tiempo de lo ya trackeado)
- Primer intento de fix (parche: mantener el concepto de dropped_ids, agregar contador recovered_snapshots_written) rechazado explicitamente por el usuario como arquitectonicamente incorrecto -- el usuario diseno la solucion correcta el mismo: desacoplar por completo, iterar sobre TODO market open del registry sin excepcion, usando el batch del top-500 como fuente barata cuando coincide y fetch individual por ID cuando no. Reescrito y desplegado (scoped a ingest_polymarket, 0 permisos/llamadas API nuevas -- reutiliza exactamente las mismas llamadas que ya existian para el chequeo de resolucion). Documentado en tech_debt.md con el razonamiento completo, incluyendo el intento rechazado
- Reescrito README.md desde cero usando architecture_canon.md como fuente de verdad (arquitectura real, flujo de datos, stack, estructura del repo verificada, seccion Pending/TODO) -- reemplaza la narrativa personal de origen que ya no reflejaba el estado actual del proyecto
- Corregido un error real en infra_design.md (decia que new_skills.md estaba trackeado en git; en realidad esta gitignorado, confirmado con git check-ignore) y reescrito ese documento por completo para reflejar la arquitectura real de .claude/ en este repo (antes era una tabla generica de features de Claude Code sin nada especifico a Poly-RAG)
- Instaladas 29 skills de Databricks (Unity Catalog, Delta Lake, jobs, etc.) via databricks aitools install --skills-only, scoped al repo (.claude/skills/ symlinks + .databricks/aitools/skills/ contenido real, ambos trackeados a peticion del usuario para que el futuro corpus self-referencial tenga acceso)
- Dia 3 (Databricks) avanzado con datos reales: notebook poly_rag_exploration reescrito para el schema actual (registry + odds time-series, ya no el payload viejo de markets completo), tablas Delta creadas en Unity Catalog (workspace.poly_rag.market_registry/odds_snapshots) y verificadas. Encontrado y corregido un bug real en el notebook mismo: odds_snapshots usaba append puro sobre una muestra parcial, dejando basura residual de markets ya borrados del pipeline real (ej. market 2252242, con 26 snapshots en Delta mucho despues de no existir ya en S3/DynamoDB) -- corregido con DROP TABLE de una sola vez (no permanente, para no perder la capacidad de demostrar time travel real mas adelante) + invariante de purga contra el registry completo en cada corrida futura
- Ultima llamada Bedrock de LLM-en-ingestion en ambos rediseños (world_snapshot ayer, y hoy el fix de odds) siguio la misma disciplina de costo cero-adicional del proyecto -- ningun cambio de hoy agrego llamadas nuevas a Bedrock ni a la Gamma API, solo reordeno/reutilizo trabajo que ya se pagaba

## 2026-08-16

- Aclarado el comportamiento real de ingest_polymarket ante cada corrida de EventBridge: el fetch del top-500 a la Gamma API SIEMPRE es completo cada ciclo (no hay forma de pedir "solo lo nuevo" sin la lista completa para diffear) -- el diff contra el registry solo protege el paso caro (clasificacion LLM/Bedrock), no el fetch en si ni el snapshot de odds (que se escribe cada ciclo para TODO market abierto, nuevo o no). architecture_canon.md actualizado con esta precision, incluyendo la interaccion con el bug ya documentado de horizonte de 48h (un market deportivo de corto horizonte se re-evalua como "nuevo" indefinidamente hasta envejecer o salir del top-500)
- Diseñada e implementada una nueva seccion `world_snapshot` en el digest (rapport explicito con el usuario antes de codear, dos preguntas via AskUserQuestion para cerrar criterio de seleccion y umbrales): a diferencia de `top_volatility` (que MOVIO este ciclo), esta es una foto de que CREE el mercado ahora mismo, independiente del movimiento -- `top_conviction` (top 5 por volume24hr, mayor conviccion real) y `most_disputed` (top 5 mas cercanos a 50/50 dentro de banda 40-60%, apuestas genuinamente divididas)
- Refactor de eficiencia en el mismo cambio: `compute_top_volatility` y `compute_world_snapshot` ahora comparten un solo pase de lectura (`_load_open_market_odds`) en vez de escanear el registry y releer S3 dos veces por ciclo para dos vistas del mismo pool de ~330 markets abiertos -- disciplina de costo real, no solo estilo
- Desplegado con `terraform apply -target` scoped SOLO a `aws_lambda_function.send_digest` (plan confirmado de antemano: 0 to add, 1 to change, 0 to destroy) sin tocar ingest_polymarket/news/comments ni ningun otro recurso -- a peticion explicita del usuario de no redesplegar las otras Lambdas
- Verificado con invocacion manual directa de `poly-rag-send-digest` (sin correr la cadena completa) contra datos reales de produccion: ambos grupos poblados 5/5, `most_disputed` mostro toss-ups politicos genuinos (balance de poder del Senado 2026 a 0.49, carrera de Ohio a 0.48, Netanyahu-out a 0.47) -- exactamente la señal que `top_volatility` no puede mostrar. Email confirmado recibido y legible por el usuario con las dos secciones nuevas
- Nota para revisitar (agregada a tech_debt.md): `top_conviction` mezcla conviccion real con markets deportivos casi resueltos (precio 1.00/0.00 de partidos ya terminados) porque `volume24hr` no distingue "el mercado esta seguro" de "el partido ya acabo" -- posible filtro futuro similar al horizonte minimo de 48h ya existente
- architecture_canon.md: seccion de Email Digest reescrita por completo -- estaba desactualizada desde el rediseno de "Strict Ingestion Chaining" (aun describia EventBridge con offset fijo de 5 min en vez del encadenamiento directo actual), aprovechado el mismo bloque de trabajo para corregirla junto con la adicion de world_snapshot
- Empaquetados dos zips de `.claude/claude_docs/` a peticion del usuario (uno con estructura de carpetas, otro con todos los .md en listado plano, `gerdau/*.md` prefijados `gerdau_` para evitar colision de nombres) -- entregados en la raiz del repo, no trackeados en git
- News: rediseno de self-chaining secuencial a fan-out paralelo desplegado y verificado en produccion (228/228 markets, 887 articulos, cycle_complete true) -- el diseno secuencial original habria tardado ~1.5h, el paralelo lo bajo a un puñado de minutos por corrida real
- Blocklist dinamico de dominios implementado y desplegado (tabla poly-rag-domain-failures, 5 fallos consecutivos = skip sin intentar el request, reset a 0 en cualquier exito) -- cierra el pendiente de egamersworld.com y similares detectados fallando 100% de las veces
- Limpieza de architecture_canon.md: removida la seccion "News Feed Inventory" obsoleta (los 10 feeds RSS viejos), corregidas cifras viejas del bootstrap (505/367 -> 228/887), corregida la descripcion del linkeo News->market_id para reflejar el diseno actual (busqueda dedicada por market, no AND-match)
- Auditoria de valor real de Bluesky como fuente: confirmado con datos reales que la mayoria de los 437 posts capturados eran bots republicando el propio feed de precios de Polymarket, no sentiment humano genuino -- senal circular, no independiente. Decision: pausar/reemplazar Bluesky
- Investigado y confirmado: gamma-api.polymarket.com/comments es un endpoint publico, sin auth, documentado oficialmente, con autor/texto/timestamp/reactions/threading -- mejor fuente de sentiment real (comentarios de traders reales verificados en el sample, ej. analisis tecnico real de un partido de La Liga)
- Verificado con datos reales que los comentarios cuelgan de Event (especifico a un market) o Series (compartido entre TODOS los markets de esa serie/liga) -- confirmado empiricamente que Series mezcla comentarios de partidos no relacionados bajo el mismo feed (dos juegos de MLB distintos mostraron el mismo comentario), asi que se disenio un campo comment_link_type (direct vs shared_series) en vez de tratar Series como simplemente "baja confianza"
- Bug real encontrado en el filtro de horizonte minimo de 48h: usa endDate del market, que para mercados deportivos NO es la hora real del partido sino un deadline administrativo posterior (ej. partido de MLB de hoy con endDate 8 dias despues) -- documentado como tech debt, no resuelto aun (fix candidato: usar event.eventDate en su lugar)
- Diseno e implementacion completa (no desplegada aun) del reemplazo de Bluesky por Comments: ingest_polymarket simplificado (LLM ya no genera search_query/news_match_terms, quedaron obsoletos porque News usa question verbatim y Comments usa lookup directo por ID), nueva funcion get_comment_link que extrae event_id/series_id directo de la respuesta de Gamma API sin LLM, nueva Lambda ingest_comments creada completa, Terraform actualizado (nueva Lambda + EventBridge, Bluesky removido) -- terraform apply pendiente de correr, nada desplegado a AWS todavia
- Sesion cerrada con /end sin desplegar el cambio de Comments -- el corte fue deliberado para dejar un checkpoint en el ledger antes de continuar, no un fin de sesion real
- Retomado y desplegado el reemplazo de Bluesky por Comments via terraform apply: ingest_bluesky (funcion, permiso, EventBridge rule/target) destruido de AWS, ingest_comments creado y activo, ingest_polymarket actualizado con el LLM simplificado -- confirmado con aws lambda get-function que ingest_bluesky ya no existe
- Backfill one-off corrido contra los 329 markets del registry que predataban el rediseño (solo los nuevos del dia ya tenian comment_entity_type): 187 con link directo a Event, 105 con candidato de Series, 37 sin comentarios en ningun nivel, 0 errores -- sin este paso ingest_comments habria tenido cobertura casi nula
- Primera prueba real de ingest_comments expuso un bug de diseño propio (no reportado por el usuario esta vez): el criterio original de dos niveles (direct=Event, shared_series=Series) resultaba incorrecto porque un Event puede agrupar VARIOS markets (ej. torneo de esports, un market por equipo, misma seccion de comments) -- 802/1698 comentarios marcados direct en realidad tenian multiples market_ids, rompiendo la garantia 1:1 que direct prometia. Corregido el mismo dia a 3 niveles (direct/shared_event/shared_series) y redesplegado
- Verificado en produccion tras el fix: 295/329 markets con cobertura de comentarios (89%), 2506 comentarios reales, 94 llamadas a la API (agrupadas por entidad compartida en vez de una por market), 0 fallos, distribucion final 896 direct / 802 shared_event / 808 shared_series
- Codigo fuente de ingest_bluesky borrado del repo (ya sin ningun Terraform que lo referencie, la historia completa queda preservada en tech_debt.md y session_ledger.md, no en codigo muerto)
- architecture_canon.md y tech_debt.md actualizados con el estado final real (no el diseño intermedio): tabla de Infrastructure Inventory, schema del Market Registry, seccion de Comments reemplazando la de Bluesky, entrada completa "Comments Source Replaces Bluesky" con la narrativa de los dos bugs encontrados y corregidos
- Sanitizacion de queries de Google News: comillas literales en question (ej. titulos de shows) forzaban busqueda de frase exacta en Google en vez de texto libre -- fix simple de sanitize_query, sin tocar el prompt del LLM (instruccion explicita del usuario)
- Digest de email completamente rediseñado: de texto plano concatenando 3 resumenes ciegos entre si, a JSON estructurado en S3 (digest/YYYY-MM-DD/HH.json, pensado para ingestion RAG futura) + email HTML con resumen ejecutivo sintetizado por un nuevo llamado LLM que ve las 3 fuentes juntas, top movimientos de odds por volatilidad real (delta entre snapshots), y citas verbatim de articulos/comentarios. Bugs reales encontrados y corregidos en el camino: falta de permiso s3:PutObject (el rol nunca habia escrito a S3), y un NameError real por una variable renombrada a medias
- Usuario detecto en vivo un bug real de orquestacion: las 3 Lambdas de ingestion disparaban por EventBridge al mismo tiempo (00:00/12:00 UTC), con el digest en un offset fijo de 5 min asumiendo que ya habian terminado -- confirmado con un caso real (un market nuevo de Polymarket nunca aparecio en News porque corrio contra un registry no actualizado ese ciclo)
- Rediseño a encadenamiento estricto: solo ingest_polymarket tiene EventBridge rule ahora: invoca a ingest_news al terminar, News invoca a Comments solo cuando su propio ciclo completo (no cada batch) termina, Comments invoca a send_digest -- las 3 EventBridge rules de News/Comments/digest se eliminaron de Terraform. Decision consciente de revertir el principio de aislamiento de fallos del Dia 2 (si Polymarket falla, nada mas corre ese ciclo) porque News/Comments nunca fueron realmente independientes del registry que Polymarket escribe
- Primera prueba real de la cadena expuso un bug de fondo: cada batch de News recalculaba total_markets con su propio scan() del registry, y como DynamoDB Scan es de consistencia eventual, dos scans de invocaciones distintas podian dar resultados distintos aunque los datos no hubieran cambiado -- causo que el merge final nunca encontrara el set de offsets esperado y la cadena se atorara silenciosamente sin ningun error visible. Root-caused con evidencia directa (timestamps de first_seen/resolution_date identicos al inicio del ciclo, descartando que el registry siguiera cambiando). Fix: total_markets se calcula UNA vez en el dispatcher y viaja fijo por el payload de cada batch
- Con el fix desplegado, un batch (offset=0) hizo timeout real de 900s extrayendo articulos de un outlet lento -- expuso un segundo problema real: sin nadie monitoreando, un solo batch atorado bloquea la cadena completa (Comments, digest) indefinidamente y sin aviso. Usuario correctamente escéptico: "no estamos ni tu ni yo para depurar" cada 12 horas
- Implementado poly-rag-watchdog-ingest-news: nueva Lambda en cron de 10 min que detecta un ciclo de News atorado (batches faltantes 20+ min despues de iniciado) y reintenta automaticamente solo los offsets faltantes, reusando el mismo total_markets ya usado por el ciclo (sin volver a escanear). El watchdog mismo tuvo un bug real en su primera corrida (extraia mal el prefijo de hora del key de S3, tomaba solo la fecha) -- corregido y reverificado con datos reales
- Cadena completa verificada funcionando end-to-end por primera vez: Polymarket -> News (cycle_complete true, 73 articulos) -> Comments (2587 comentarios) -> digest, disparada automaticamente sin invocacion manual de Comments ni digest. Resumen ejecutivo real coherente (identifico correctamente que los mayores movimientos de odds eran partidos deportivos en progreso, no trading predictivo)
- Dos bugs no bloqueantes encontrados en esa misma corrida -- usuario pidio explicitamente corregir el primero antes de seguir (no dejarlo solo documentado): Comments/digest escribian su particion de S3 con datetime.now() en vez de heredar cycle_started_at, y ademas ingest_comments invocaba a send_digest con un payload VACIO (el bug real empezaba ahi, no solo en la construccion del key). Corregido: cycle_started_at ahora viaja por toda la cadena hasta el ultimo eslabon, cada Lambda lo usa para su propia key de S3 y lo reenvia a la siguiente. Verificado con invocacion manual explicita (cycle_started_at de prueba a las 03:00) confirmando que tanto comments/03.json como digest/03.json se escribieron bajo la particion correcta -- archivos de prueba borrados despues de verificar
- Segundo bug (doble-disparo por invocaciones concurrentes de offset=0) queda documentado pero no corregido -- solo ocurre por intervencion manual de debugging, no en operacion normal
- Sesion continua (NO fue un fin de dia real) -- el trabajo de ingestion/orquestacion sigue siendo prioridad sobre Databricks/Delta Lake (Dia 3, pendiente desde hace 2 dias) hasta que los cimientos del pipeline queden solidos, por decision explicita del usuario

- Bootstrap del pipeline rediseñado corrido y verificado: ingest_polymarket con top 500 candidatos por volume24hr, filtro LLM de verificabilidad, y registry poblado en DynamoDB (480 mercados trackeados, 480 archivos de odds en S3) -- verificado directamente contra las tablas reales tras dos bugs de despliegue encontrados y corregidos en el camino (timeout de Lambda insuficiente para el volumen de trabajo del bootstrap, y un AccessDenied de S3 causado por falta de permiso ListBucket a nivel bucket, no solo GetObject a nivel objeto)
- Logging de auditoria agregado al paso LLM: prompt y respuesta cruda de cada batch van a CloudWatch, y las respuestas crudas (capa Bronze) se guardan tambien en el metadata del payload S3 -- para poder re-inspeccionar exactamente que dijo el modelo si algo se ve mal en el registry parseado
- Revision de resultado del bootstrap detecto que ~96% de los candidatos pasaron el filtro de verificabilidad (no 2.4% como parecia inicialmente por una lectura enganosa del ultimo run aislado) -- confirma la hipotesis de que alto volumen en Polymarket ya correlaciona con mercados de resolucion clara
- Bug de diseno real encontrado en las keywords generadas por el LLM: una lista de keywords sueltas (ej. "Elon Musk", "tweets", "August 2026") pierde señal si se buscan por separado -- termino identificado por el usuario, no how solo un capricho de estilo
- Corregido a dos representaciones de busqueda distintas en el mismo llamado LLM (sin costo adicional de Bedrock), porque News y Bluesky matchean texto de formas distintas: search_query (string combinado, para el search API de Bluesky) y news_match_terms (lista corta de 1-3 frases distintivas con logica AND, para matching por substring contra el texto de RSS ya descargado por News, que no tiene API de busqueda)
- Verificado con llamadas reales a Bedrock (fuera del handler, muestra de 20 mercados) antes de desplegar -- confirmado que ambos formatos salen bien calibrados (ej. Federal Reserve + September 2026 en vez de Fed/rate/2026 sueltos)
- Reflexion de producto real: se discutio honestamente que tipo de analisis estadistico/predictivo es alcanzable con odds+news (lead-lag, calibracion de Polymarket via reliability diagram) vs. lo que NO lo es (prediccion causal validada, dado el volumen de datos de solo 2 meses) -- y para que le serviria el RAG a un trader real (agregacion + historial propio + alertas de divergencia, no "tips de inversion")
- Corregida una limitacion mal planteada: el retrieval NO debe limitarse solo a items explicitamente linkeados por market_id -- toda noticia/post ya tiene timestamp de ingesta sin importar si matcheo keywords, asi que existe una segunda capa de retrieval por ventana temporal disponible ya (sin esperar a embeddings del Dia 4), como contexto secundario junto al linkeo de alta confianza
- Anotado en tech_debt como consideracion futura: acceso web en vivo para el agente de sintesis (Dia 5) via tool-use de Claude -- explicitamente no aplica a las Lambdas de ingestion (rompe el modelo de costo batch), pero es candidato real para cuando el usuario pregunte algo mas reciente que el ultimo ciclo de ingestion
- Pendiente para el siguiente bloque (dejado deliberadamente sin ejecutar para no gastar Bedrock de mas antes de dormir): limpiar los 480 registros del bootstrap viejo (schema con keywords obsoleto), re-correr el bootstrap con el prompt de dos formatos ya desplegado, luego migrar ingest_news e ingest_bluesky a leer del registry, reactivar el cron de 12h, construir el retrieval por ventana temporal, y actualizar architecture_canon.md con el diseno completo
- Bug logico real encontrado y corregido en el bootstrap: un market podia crearse y marcarse "resolved" en la misma sesion de trabajo (13 markets deportivos/esports resueltos minutos despues de trackearse) -- root cause: mercados con resolucion en minutos/horas (partidos en vivo) no encajan en absoluto con la tesis del proyecto (noticias/sentiment influyendo el resultado mientras el mercado esta abierto), asi que se agrego un filtro de horizonte minimo de 48h (endDate) antes del paso LLM, aplicado antes de gastar Bedrock. Registry viejo (505, con el bug) y ambos sets de archivos de odds (el nuevo y el respaldo de anoche) borrados permanentemente por instruccion explicita del usuario -- sin dejar rastro de las corridas rotas. Bootstrap limpio final: 228 markets (bajo de 505 tras excluir deportes/esports de corto horizonte)
- Auditoria real de calidad del linkeo News->market via AND-match: de 367 articulos solo 3 linkeaban a algun market, encontrado root cause con evidencia (no supuesto): el LLM generaba terminos formales de la pregunta de Polymarket ("Federal Reserve", "President of Russia") en vez de vocabulario periodistico real ("Fed", "Putin") -- confirmado contando matches reales contra los articulos del dia ("federal reserve" exacto: 0 matches; "fed ": 1; "putin" solo: 2 pero el segundo termino AND requerido nunca aparecia). Causa raiz: el prompt solo le pasa al LLM la question corta de Polymarket, sin description, un "nanocorpus" insuficiente para inferir vocabulario real
- Usuario identifico en vivo que Polymarket ya tiene una feature nativa (AI-generated summary, marcada "Experimental") correlacionando noticias con movimientos de odds en ciertos markets de alto perfil -- discutido como amenaza SWOT real a mediano plazo (Polymarket tiene datos privilegiados que este proyecto no tiene) pero no competencia directa hoy (feature limitada, experimental, sin peso en resolucion) -- PolyRAG mantiene ventaja en cobertura de cola larga + fuentes que Polymarket no tiene (Bluesky)
- Rediseno completo de la fuente de News: reemplazados los 10 feeds RSS curados por busqueda dirigida en Google News RSS usando la question de cada market TAL CUAL (sin keywords, sin LLM) como query -- delega el problema de sinonimos/parafraseo al motor de relevancia de Google en vez de un AND-match hecho a mano. Investigadas y descartadas alternativas: Brave Search (ToS prohibe explicitamente uso en RAG/AI, mismo tipo de error que Reddit), Bing/Azure Search (API retirada agosto 2025), DuckDuckGo (sin API real), Google Custom Search (cerrado a registros nuevos desde 2025), NewsAPI.org (tier gratis prohibe produccion), SerpApi/Serper (litigio activo con Google), GDELT (ToS limpio y verificado directo en gdeltproject.org, pero rate limit agresivo de 429 que persistio incluso esperando 20-40s, posiblemente compartido a nivel de rango de IP)
- Excepcion consciente de ToS documentada explicitamente: el copyright de Google News RSS prohibe literalmente "any other use" fuera de "personal feed reader" -- decision informada del usuario tras ver el paralelismo con el caso Reddit, no un descuido. Anotado en tech_debt.md con la justificacion completa y los criterios de revision (si Google bloquea el endpoint en la practica)
- Pipeline de extraccion nuevo construido: googlenewsdecoder (resuelve el link ofuscado de Google sin necesitar headless browser, confirmado contra un articulo real de Reuters) + trafilatura (extrae texto del articulo real). Confirmado con datos reales que medios grandes (Reuters) bloquean con 401 incluso con User-Agent de navegador -- extraccion fallida se DESCARTA, no cae a fallback de solo-titulo (correccion explicita del usuario a una regla que yo mismo inferi sin que la pidiera)
- Dedup por URL exacta (no por similitud de evento) en nueva tabla DynamoDB poly-rag-processed-urls, sin TTL -- decision justificada en vivo: pay-per-request no cobra por storage ocioso de items chicos, asi que TTL no aportaba nada real: el contenido ya extraido vive permanente en S3 sin importar el estado de esta tabla de dedup
- Empaquetado de Lambda con dependencias externas por primera vez en el proyecto (trafilatura + googlenewsdecoder, ~79MB sin comprimir / 21MB comprimido, dentro del limite de Lambda) -- requirio agregar provider null de Terraform y un null_resource con pip install --target antes de que archive_file empaquete el directorio
- Bug de produccion real encontrado y aun sin resolver del todo: 3 invocaciones consecutivas de ingest_news (228 markets) agotaron el timeout de 300s, con articulos siendo REPROCESADOS en cada reintento (mismos URLs de egamersworld.com/people.com repetidos) -- root cause parcial corregido (trafilatura.fetch_url usaba el timeout default de 30s, causando que un solo outlet bloqueado como washingtonpost.com consumiera 30s antes de fallar; bajado a 8s explicitamente vía DOWNLOAD_TIMEOUT config), pero el problema de fondo (228 markets secuenciales no caben en una sola invocacion, sin checkpoint entre reintentos) sigue sin resolver -- solucion identificada (batching con checkpoint de que markets ya se procesaron en el ciclo) pero no implementada aun
- Batching self-chaining implementado y desplegado (BATCH_SIZE=35, offset via payload de invoke, timeout 900s/memory 512MB) -- primera corrida real en produccion mostro ~10-13 min por batch, proyectando ~1.5h para los 7 batches secuenciales. Redisenado en vivo a mitad de la corrida: en vez de encadenar un batch a la vez, la primera invocacion dispara TODOS los batches restantes de una vez (fan-out paralelo), escalonados 3s entre cada uno para no golpear a Google/outlets con un burst simultaneo (se evaluo y descarto usar VPN/proxy rotativo por batch para ofuscar IP -- no hay forma barata de rotar la IP de salida de Lambda, NAT Gateway es fija y cara, proxies de terceros agregan costo/dependencia para lo que en realidad es un tema de concurrencia, no de identidad)
- Fix de race condition necesario para el paralelo: escritura concurrente al mismo S3 key (patron read-modify-write que funcionaba bien en secuencial) perderia datos entre batches simultaneos -- corregido dando a cada batch su propio S3 key, con un paso de merge idempotente al final que combina todos los archivos por-batch
- Snag real durante la transicion mid-deploy: los primeros 3 batches ya habian corrido/estaban corriendo bajo el codigo VIEJO (secuencial) cuando se desplego el codigo nuevo -- AWS deja que una invocacion en curso termine con la version con la que arranco, no la mata. Esos 3 batches escribieron al key compartido viejo en vez de sus propios archivos, asi que el merge automatico nunca los hubiera encontrado -- corregido con un merge manual unico (combinando el payload viejo + los 4 archivos nuevos, dedup por URL) que no aplica a corridas futuras (desde el proximo ciclo, todos los batches nacen bajo el codigo nuevo)
- Verificado en produccion: ciclo completo 228/228 markets, cycle_complete true, 887 articulos, 0 duplicados de URL pese a que offset=105 corrio dos veces por la transicion (dedup sostuvo). Batches paralelos completaron en 294s-611s cada uno vs. 600-800s secuenciales -- ahorro real de tiempo de reloj
- architecture_canon.md y tech_debt.md actualizados con el diseno final (fan-out paralelo, no self-chaining) reemplazando la descripcion del diseno intermedio ya superado

## 2026-08-14

- Dia 2 del sprint completado: 3 Lambdas de ingestion desplegadas y verificadas con datos reales (Polymarket, News, Bluesky), cada una escribiendo a S3 y registrando metricas de costo/latencia en DynamoDB (tabla poly-rag-architecture-metrics)
- Decision de alcance ajustada en vivo: 3 Lambdas independientes (no orquestador unico) para aislar fallos y proteger el limite de PUT requests en reintentos
- Reddit descartado como fuente (su Responsible Builder Policy prohibe explicitamente usar datos para IA/ML); X y Truth Social tambien evaluados y descartados; reemplazados por Bluesky (AT Protocol)
- Bug real encontrado y corregido en produccion: searchPosts de Bluesky requiere auth contra bsky.social (el PDS), no public.api.bsky.app como indicaba la investigacion inicial -- corregido tras 403s reales
- Filtro de verticales (Macro/Geopolitica/Regulatorio-Tech) definido con keywords especificas, mas exclusion explicita de mercados deportivos tras detectar falsos positivos por matching de substring (ej. sec matcheando dentro de second)
- Decision arquitectonica: LLM (Bedrock/Claude Sonnet 4.5) trial en ingestion para las 3 fuentes, midiendo costo/latencia real por 3-4 dias antes de decidir si se mantiene -- una sola llamada batched por corrida, no una por item, para controlar costo
- Costo real medido: ~0.018 USD por ciclo completo de las 3 fuentes (~1.10 USD/mes proyectado a cadencia de 12h), dentro del presupuesto
- Bloqueos resueltos en el camino: Sonnet 5 no disponible (se uso 4.5 via inference profile), suscripcion AWS Marketplace con delay de propagacion en cuenta nueva
- Agregado bloque metadata (schema_version, lambda_name, request_id, llm_used, model_id, latency_ms, cost) a cada JSON en S3 para linaje por-archivo, sin depender de cruzar con DynamoDB
- Terraform instalado e implementado: toda la infra AWS existente (S3, DynamoDB, IAM, 3 Lambdas, EventBridge) escrita en HCL e importada con terraform import -- cero destroy/recreate, verificado con terraform plan mostrando No changes contra el estado real
- Secrets de Bluesky mantenidos fuera de Terraform state/codigo via lifecycle.ignore_changes -- gestionados manualmente
- Creado /cv y new_skills.md (gitignored) para trackear skills reales aprendidas, de cara a actualizar CV
- Corregido /end: ahora explicito que se invoca por bloque de trabajo completado (a discrecion del usuario), no solo al cierre de sesion completa
- Agregada 4a Lambda permanente (poly-rag-send-digest): consolida los llm_summary de las 3 fuentes y los manda por correo via Amazon SES cada 12h+5min (00:05/12:05 UTC), con degradacion graceful si el LLM trial se revierte -- primera pieza de infra desplegada nativamente por Terraform (no importada) desde su creacion
- SES configurado y verificado desde cero (bernardolw@gmail.com), correo digest probado y confirmado recibido
- Aclarado el concepto de indexacion: los llm_summary en S3 hoy son raw storage, no indexados -- se definio el orden logico (Dia 3 Delta Lake/estructurado antes que Dia 4 embeddings/semantico) antes de arrancar Databricks
- Dia 3 del sprint iniciado: cuenta Databricks Free Edition creada con verificacion LinkedIn (desbloquea GPU serverless limitada); en progreso: configuracion de Databricks CLI para evitar copy-paste manual en notebooks, generando Personal Access Token
- Databricks CLI instalado y autenticado (token + host guardados en .secrets); secret scope poly-rag creado en Databricks para credenciales AWS (nunca en texto plano en el notebook)
- Notebook poly_rag_exploration construido y depurado hasta corrida limpia end-to-end (verificado corriendolo como Databricks Job): Serverless compute (Spark Connect) no soporta spark.conf.set para S3 ni sc.parallelize/.rdd -- se resolvio leyendo S3 via boto3 y construyendo el DataFrame con pandas (normalizando tipos numericos mixtos) antes de spark.createDataFrame, evitando por completo la inferencia de tipos de Spark y los bugs de read.json en Spark Connect
- Un intento de fix automatico via el Genie de Databricks fue revisado y rechazado (proponia cluster clasico o Unity Catalog external location, ambos mas caros/complejos que la solucion boto3+pandas ya funcionando) -- el notebook se re-importo por CLI para revertir el cambio no deseado
- Explorando datos reales se detecto que market_count puede venir en 0 (el filtro de vertical a veces no matchea nada en una corrida) -- esto disparo una revision de fondo de la arquitectura de ingestion, no solo un bug a ignorar
- Rediseno grande de ingestion decidido y documentado en tech_debt.md (no ejecutado aun, arranca en el siguiente bloque): el eje real de filtrado no es tema (Macro/Geopolitica/Regulatorio-Tech) sino verificabilidad objetiva del outcome de cada market; se reemplaza el filtro de keywords por un paso LLM que juzga verificabilidad Y extrae keywords dinamicas en un solo llamado batched (sin costo adicional de Bedrock)
- Diseno cerrado: universo candidato = top 500 markets por volume24hr (no volume total como hoy); un market registry en DynamoDB (metadata mutable, un item por market_id) separado de una serie de tiempo de odds en S3 (append-only, un archivo por market_id, read-modify-write cada ciclo); deteccion de resolved via consulta directa a markets/{id} (outcome real, no inferencia); News/Bluesky mantienen su cron independiente de 12h mismo principio de aislamiento de fallos del Dia 2, solo cambian de leer keywords de un dict estatico a leerlas del registry
- Medicion real (no supuesto) de la distribucion de volume24hr en Polymarket via Gamma API: universo activo real es de al menos ~2,100 markets (paginacion offset se cae en ~2100, API sugiere el endpoint markets/keyset para ir mas alla); con ese denominador correcto, top 100 captura solo 49.7% del volumen 24h total y top 500 captura 80.7% -- corrige dos supuestos previos en cascada (un estimado generico de ~90% en top 100-300, y un primer calculo propio inflado por usar un denominador incompleto de solo 500)
- Correccion de costo clave: el filtro de verificabilidad por LLM no escala con el tamano N del universo candidato sino con la tasa de aparicion de IDs nuevos por ciclo (una vez que existe el registry, solo se re-clasifican los IDs que no estaban ya trackeados) -- esto hace pagable un N amplio (500) en vez de forzar a mantenerlo chico
- Terraform aplicado: nueva tabla DynamoDB poly-rag-market-registry (pay-per-request, hash_key market_id) y permisos IAM ampliados para las 3 Lambdas de ingestion (s3:GetObject ademas de PutObject; dynamodb GetItem/PutItem/UpdateItem/Scan/Query sobre la tabla nueva) -- verificado con terraform plan/apply limpio
- Pendiente para el siguiente bloque: reescribir ingest_polymarket con el nuevo diseno completo, luego News/Bluesky, correr un bootstrap manual, verificar, y solo entonces reactivar el cron de 12h

## 2026-08-13

- Sprint de entrevista (gerdau/sprint_plan.md) Dia 1 cerrado: cuenta AWS creada desde cero, MFA en root, budget de $5/mes con alertas 20%/100%, usuario IAM admin (dejando de usar root), AWS CLI v2 instalada y configurada
- Confirmados $120 en creditos promocionales (100 signup + 20 por completar actividad de Budgets) -- CLAUDE.md actualizado para tratar el budget como disciplina real, no colchon para gastar libre
- Guardrail adicional (no estaba en el plan original): policy IAM Deny (PolyRAG-BudgetBreach-Deny) + role (PolyRAG-BudgetsActionRole) armados via CLI para bloquear Bedrock/Lambda/S3-writes/DynamoDB-writes automaticamente si el gasto cruza un budget separado de $10
- Primer contacto practico con S3: bucket creado (poly-rag-369970405415), upload/list confirmado via CLI
- Bloque 5 (Lambda hello-world) abandonado deliberadamente -- se prefirio ir directo a Lambda con trabajo real en Dia 2 en vez de un paso aislado
- Bloque 6 (Bedrock vs SageMaker) cubierto conceptualmente sin necesidad de profundizar mas -- diferencia clara: Bedrock para modelos fundacionales via API (nuestro caso), SageMaker para entrenar modelos propios desde cero (fuera de scope de Poly-RAG)
- Conceptos nuevos archivados en knowledge.md: mapeo AWS/GCP de storage, NoSQL (schema flexible no ausencia de estructura), pricing por request vs volumen, mecanismo de Budget Actions
- Siguiente: Dia 2 -- ingestion real de Polymarket via Lambda + S3 + EventBridge
